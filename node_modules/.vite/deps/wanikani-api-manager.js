// node_modules/wanikani-api-manager/dist/wanikani-api-manager.es.js
var fetchPage = async (apiToken, page, updated, filter = true, callback) => {
  try {
    if (updated && filter) {
      page += page.includes("?") ? "&" : "?";
      page += `updated_after=${_convertToISO8601(updated)}`;
    }
    const response = await modifiedSince(apiToken, updated, page);
    if (response.status === 429) {
      const rateLimitReset = response.headers.get("Ratelimit-Reset");
      const currentTime = (/* @__PURE__ */ new Date()).getTime() / 1e3;
      const resetIn = rateLimitReset ? rateLimitReset - currentTime : 3;
      const retryAfter = parseInt(response.headers.get("Retry-After") || resetIn, 10);
      if (callback) callback(response.status, page, resetIn);
      await new Promise((resolve) => setTimeout(resolve, retryAfter * 1e3));
      return fetchPage(apiToken, page, updated);
    }
    if (response.status === 304) {
      return { pages: { next_url: null }, data: [] };
    }
    const data = await response.arrayBuffer();
    const responseSize = data.byteLength;
    const result = JSON.parse(new TextDecoder().decode(data));
    if (callback) callback(response.status, page, responseSize);
    if (response.status !== 200) {
      console.error("[API] Error fetching page:", response.status, result);
      return { error: { message: result.error || "Unknown error", code: response.status } };
    }
    if (result.total_count === 0) {
      return { pages: { next_url: null }, data: [] };
    }
    return result;
  } catch (error) {
    console.error("[API] Exception while fetching page:", error);
    return { error: { message: error.message || "Unknown error", code: 500 } };
  }
};
var fetchAllPages = async (apiToken, page, updated, filter, callback) => {
  if (!page) return [];
  const result = await fetchPage(apiToken, page, updated, filter, callback);
  if (result.error) return result;
  return [result].concat(
    await fetchAllPages(apiToken, result.pages.next_url, updated, filter, callback)
  );
};
var modifiedSince = async (apiKey, date, url) => {
  try {
    date = date ? date : (/* @__PURE__ */ new Date("1700 1 1")).toUTCString();
    var requestHeaders = new Headers();
    requestHeaders.append("Authorization", `Bearer ${apiKey}`);
    requestHeaders.append("Wanikani-Revision", "20170710");
    requestHeaders.append("If-Modified-Since", date);
    var requestInit = { method: "GET", headers: requestHeaders };
    const response = await fetch(url, requestInit);
    return response;
  } catch (error) {
    console.error("[API] Exception while checking modified status:", error);
    return false;
  }
};
var _convertToISO8601 = (dateString) => {
  const dateObj = new Date(dateString);
  const isoString = dateObj.toISOString();
  return isoString;
};
var commonjsGlobal = typeof globalThis !== "undefined" ? globalThis : typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : {};
function getDefaultExportFromCjs(x) {
  return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, "default") ? x["default"] : x;
}
var dexie_min$1 = { exports: {} };
var dexie_min = dexie_min$1.exports;
var hasRequiredDexie_min;
function requireDexie_min() {
  if (hasRequiredDexie_min) return dexie_min$1.exports;
  hasRequiredDexie_min = 1;
  (function(module, exports) {
    (function(e, t) {
      module.exports = t();
    })(dexie_min, function() {
      var s = function(e2, t2) {
        return (s = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(e3, t3) {
          e3.__proto__ = t3;
        } || function(e3, t3) {
          for (var n2 in t3) Object.prototype.hasOwnProperty.call(t3, n2) && (e3[n2] = t3[n2]);
        })(e2, t2);
      };
      var _ = function() {
        return (_ = Object.assign || function(e2) {
          for (var t2, n2 = 1, r2 = arguments.length; n2 < r2; n2++) for (var i2 in t2 = arguments[n2]) Object.prototype.hasOwnProperty.call(t2, i2) && (e2[i2] = t2[i2]);
          return e2;
        }).apply(this, arguments);
      };
      function i(e2, t2, n2) {
        for (var r2, i2 = 0, o2 = t2.length; i2 < o2; i2++) !r2 && i2 in t2 || ((r2 = r2 || Array.prototype.slice.call(t2, 0, i2))[i2] = t2[i2]);
        return e2.concat(r2 || Array.prototype.slice.call(t2));
      }
      var f = "undefined" != typeof globalThis ? globalThis : "undefined" != typeof self ? self : "undefined" != typeof window ? window : commonjsGlobal, x = Object.keys, k = Array.isArray;
      function a(t2, n2) {
        return "object" != typeof n2 || x(n2).forEach(function(e2) {
          t2[e2] = n2[e2];
        }), t2;
      }
      "undefined" == typeof Promise || f.Promise || (f.Promise = Promise);
      var c = Object.getPrototypeOf, n = {}.hasOwnProperty;
      function m(e2, t2) {
        return n.call(e2, t2);
      }
      function r(t2, n2) {
        "function" == typeof n2 && (n2 = n2(c(t2))), ("undefined" == typeof Reflect ? x : Reflect.ownKeys)(n2).forEach(function(e2) {
          l(t2, e2, n2[e2]);
        });
      }
      var u = Object.defineProperty;
      function l(e2, t2, n2, r2) {
        u(e2, t2, a(n2 && m(n2, "get") && "function" == typeof n2.get ? { get: n2.get, set: n2.set, configurable: true } : { value: n2, configurable: true, writable: true }, r2));
      }
      function o(t2) {
        return { from: function(e2) {
          return t2.prototype = Object.create(e2.prototype), l(t2.prototype, "constructor", t2), { extend: r.bind(null, t2.prototype) };
        } };
      }
      var h = Object.getOwnPropertyDescriptor;
      var d = [].slice;
      function b(e2, t2, n2) {
        return d.call(e2, t2, n2);
      }
      function p(e2, t2) {
        return t2(e2);
      }
      function y(e2) {
        if (!e2) throw new Error("Assertion Failed");
      }
      function v(e2) {
        f.setImmediate ? setImmediate(e2) : setTimeout(e2, 0);
      }
      function O(e2, t2) {
        if ("string" == typeof t2 && m(e2, t2)) return e2[t2];
        if (!t2) return e2;
        if ("string" != typeof t2) {
          for (var n2 = [], r2 = 0, i2 = t2.length; r2 < i2; ++r2) {
            var o2 = O(e2, t2[r2]);
            n2.push(o2);
          }
          return n2;
        }
        var a2 = t2.indexOf(".");
        if (-1 !== a2) {
          var u2 = e2[t2.substr(0, a2)];
          return null == u2 ? void 0 : O(u2, t2.substr(a2 + 1));
        }
      }
      function P(e2, t2, n2) {
        if (e2 && void 0 !== t2 && !("isFrozen" in Object && Object.isFrozen(e2))) if ("string" != typeof t2 && "length" in t2) {
          y("string" != typeof n2 && "length" in n2);
          for (var r2 = 0, i2 = t2.length; r2 < i2; ++r2) P(e2, t2[r2], n2[r2]);
        } else {
          var o2, a2, u2 = t2.indexOf(".");
          -1 !== u2 ? (o2 = t2.substr(0, u2), "" === (a2 = t2.substr(u2 + 1)) ? void 0 === n2 ? k(e2) && !isNaN(parseInt(o2)) ? e2.splice(o2, 1) : delete e2[o2] : e2[o2] = n2 : P(u2 = !(u2 = e2[o2]) || !m(e2, o2) ? e2[o2] = {} : u2, a2, n2)) : void 0 === n2 ? k(e2) && !isNaN(parseInt(t2)) ? e2.splice(t2, 1) : delete e2[t2] : e2[t2] = n2;
        }
      }
      function g(e2) {
        var t2, n2 = {};
        for (t2 in e2) m(e2, t2) && (n2[t2] = e2[t2]);
        return n2;
      }
      var t = [].concat;
      function w(e2) {
        return t.apply([], e2);
      }
      var e = "BigUint64Array,BigInt64Array,Array,Boolean,String,Date,RegExp,Blob,File,FileList,FileSystemFileHandle,FileSystemDirectoryHandle,ArrayBuffer,DataView,Uint8ClampedArray,ImageBitmap,ImageData,Map,Set,CryptoKey".split(",").concat(w([8, 16, 32, 64].map(function(t2) {
        return ["Int", "Uint", "Float"].map(function(e2) {
          return e2 + t2 + "Array";
        });
      }))).filter(function(e2) {
        return f[e2];
      }), K = new Set(e.map(function(e2) {
        return f[e2];
      }));
      var E = null;
      function S(e2) {
        E = /* @__PURE__ */ new WeakMap();
        e2 = (function e3(t2) {
          if (!t2 || "object" != typeof t2) return t2;
          var n2 = E.get(t2);
          if (n2) return n2;
          if (k(t2)) {
            n2 = [], E.set(t2, n2);
            for (var r2 = 0, i2 = t2.length; r2 < i2; ++r2) n2.push(e3(t2[r2]));
          } else if (K.has(t2.constructor)) n2 = t2;
          else {
            var o2, a2 = c(t2);
            for (o2 in n2 = a2 === Object.prototype ? {} : Object.create(a2), E.set(t2, n2), t2) m(t2, o2) && (n2[o2] = e3(t2[o2]));
          }
          return n2;
        })(e2);
        return E = null, e2;
      }
      var j = {}.toString;
      function A(e2) {
        return j.call(e2).slice(8, -1);
      }
      var C = "undefined" != typeof Symbol ? Symbol.iterator : "@@iterator", T = "symbol" == typeof C ? function(e2) {
        var t2;
        return null != e2 && (t2 = e2[C]) && t2.apply(e2);
      } : function() {
        return null;
      };
      function q(e2, t2) {
        t2 = e2.indexOf(t2);
        return 0 <= t2 && e2.splice(t2, 1), 0 <= t2;
      }
      var D = {};
      function I(e2) {
        var t2, n2, r2, i2;
        if (1 === arguments.length) {
          if (k(e2)) return e2.slice();
          if (this === D && "string" == typeof e2) return [e2];
          if (i2 = T(e2)) {
            for (n2 = []; !(r2 = i2.next()).done; ) n2.push(r2.value);
            return n2;
          }
          if (null == e2) return [e2];
          if ("number" != typeof (t2 = e2.length)) return [e2];
          for (n2 = new Array(t2); t2--; ) n2[t2] = e2[t2];
          return n2;
        }
        for (t2 = arguments.length, n2 = new Array(t2); t2--; ) n2[t2] = arguments[t2];
        return n2;
      }
      var B = "undefined" != typeof Symbol ? function(e2) {
        return "AsyncFunction" === e2[Symbol.toStringTag];
      } : function() {
        return false;
      }, R = ["Unknown", "Constraint", "Data", "TransactionInactive", "ReadOnly", "Version", "NotFound", "InvalidState", "InvalidAccess", "Abort", "Timeout", "QuotaExceeded", "Syntax", "DataClone"], F = ["Modify", "Bulk", "OpenFailed", "VersionChange", "Schema", "Upgrade", "InvalidTable", "MissingAPI", "NoSuchDatabase", "InvalidArgument", "SubTransaction", "Unsupported", "Internal", "DatabaseClosed", "PrematureCommit", "ForeignAwait"].concat(R), M = { VersionChanged: "Database version changed by other database connection", DatabaseClosed: "Database has been closed", Abort: "Transaction aborted", TransactionInactive: "Transaction has already completed or failed", MissingAPI: "IndexedDB API missing. Please visit https://tinyurl.com/y2uuvskb" };
      function N(e2, t2) {
        this.name = e2, this.message = t2;
      }
      function L(e2, t2) {
        return e2 + ". Errors: " + Object.keys(t2).map(function(e3) {
          return t2[e3].toString();
        }).filter(function(e3, t3, n2) {
          return n2.indexOf(e3) === t3;
        }).join("\n");
      }
      function U(e2, t2, n2, r2) {
        this.failures = t2, this.failedKeys = r2, this.successCount = n2, this.message = L(e2, t2);
      }
      function V(e2, t2) {
        this.name = "BulkError", this.failures = Object.keys(t2).map(function(e3) {
          return t2[e3];
        }), this.failuresByPos = t2, this.message = L(e2, this.failures);
      }
      o(N).from(Error).extend({ toString: function() {
        return this.name + ": " + this.message;
      } }), o(U).from(N), o(V).from(N);
      var z = F.reduce(function(e2, t2) {
        return e2[t2] = t2 + "Error", e2;
      }, {}), W = N, Y = F.reduce(function(e2, n2) {
        var r2 = n2 + "Error";
        function t2(e3, t3) {
          this.name = r2, e3 ? "string" == typeof e3 ? (this.message = "".concat(e3).concat(t3 ? "\n " + t3 : ""), this.inner = t3 || null) : "object" == typeof e3 && (this.message = "".concat(e3.name, " ").concat(e3.message), this.inner = e3) : (this.message = M[n2] || r2, this.inner = null);
        }
        return o(t2).from(W), e2[n2] = t2, e2;
      }, {});
      Y.Syntax = SyntaxError, Y.Type = TypeError, Y.Range = RangeError;
      var $ = R.reduce(function(e2, t2) {
        return e2[t2 + "Error"] = Y[t2], e2;
      }, {});
      var Q = F.reduce(function(e2, t2) {
        return -1 === ["Syntax", "Type", "Range"].indexOf(t2) && (e2[t2 + "Error"] = Y[t2]), e2;
      }, {});
      function G() {
      }
      function X(e2) {
        return e2;
      }
      function H(t2, n2) {
        return null == t2 || t2 === X ? n2 : function(e2) {
          return n2(t2(e2));
        };
      }
      function J(e2, t2) {
        return function() {
          e2.apply(this, arguments), t2.apply(this, arguments);
        };
      }
      function Z(i2, o2) {
        return i2 === G ? o2 : function() {
          var e2 = i2.apply(this, arguments);
          void 0 !== e2 && (arguments[0] = e2);
          var t2 = this.onsuccess, n2 = this.onerror;
          this.onsuccess = null, this.onerror = null;
          var r2 = o2.apply(this, arguments);
          return t2 && (this.onsuccess = this.onsuccess ? J(t2, this.onsuccess) : t2), n2 && (this.onerror = this.onerror ? J(n2, this.onerror) : n2), void 0 !== r2 ? r2 : e2;
        };
      }
      function ee(n2, r2) {
        return n2 === G ? r2 : function() {
          n2.apply(this, arguments);
          var e2 = this.onsuccess, t2 = this.onerror;
          this.onsuccess = this.onerror = null, r2.apply(this, arguments), e2 && (this.onsuccess = this.onsuccess ? J(e2, this.onsuccess) : e2), t2 && (this.onerror = this.onerror ? J(t2, this.onerror) : t2);
        };
      }
      function te(i2, o2) {
        return i2 === G ? o2 : function(e2) {
          var t2 = i2.apply(this, arguments);
          a(e2, t2);
          var n2 = this.onsuccess, r2 = this.onerror;
          this.onsuccess = null, this.onerror = null;
          e2 = o2.apply(this, arguments);
          return n2 && (this.onsuccess = this.onsuccess ? J(n2, this.onsuccess) : n2), r2 && (this.onerror = this.onerror ? J(r2, this.onerror) : r2), void 0 === t2 ? void 0 === e2 ? void 0 : e2 : a(t2, e2);
        };
      }
      function ne(e2, t2) {
        return e2 === G ? t2 : function() {
          return false !== t2.apply(this, arguments) && e2.apply(this, arguments);
        };
      }
      function re(i2, o2) {
        return i2 === G ? o2 : function() {
          var e2 = i2.apply(this, arguments);
          if (e2 && "function" == typeof e2.then) {
            for (var t2 = this, n2 = arguments.length, r2 = new Array(n2); n2--; ) r2[n2] = arguments[n2];
            return e2.then(function() {
              return o2.apply(t2, r2);
            });
          }
          return o2.apply(this, arguments);
        };
      }
      Q.ModifyError = U, Q.DexieError = N, Q.BulkError = V;
      var ie = "undefined" != typeof location && /^(http|https):\/\/(localhost|127\.0\.0\.1)/.test(location.href);
      function oe(e2) {
        ie = e2;
      }
      var ae = {}, ue = 100, e = "undefined" == typeof Promise ? [] : (function() {
        var e2 = Promise.resolve();
        if ("undefined" == typeof crypto || !crypto.subtle) return [e2, c(e2), e2];
        var t2 = crypto.subtle.digest("SHA-512", new Uint8Array([0]));
        return [t2, c(t2), e2];
      })(), R = e[0], F = e[1], e = e[2], F = F && F.then, se = R && R.constructor, ce = !!e;
      var le = function(e2, t2) {
        be.push([e2, t2]), he && (queueMicrotask(Se), he = false);
      }, fe = true, he = true, de = [], pe = [], ye = X, ve = { id: "global", global: true, ref: 0, unhandleds: [], onunhandled: G, pgp: false, env: {}, finalize: G }, me = ve, be = [], ge = 0, we = [];
      function _e(e2) {
        if ("object" != typeof this) throw new TypeError("Promises must be constructed via new");
        this._listeners = [], this._lib = false;
        var t2 = this._PSD = me;
        if ("function" != typeof e2) {
          if (e2 !== ae) throw new TypeError("Not a function");
          return this._state = arguments[1], this._value = arguments[2], void (false === this._state && Oe(this, this._value));
        }
        this._state = null, this._value = null, ++t2.ref, (function t3(r2, e3) {
          try {
            e3(function(n2) {
              if (null === r2._state) {
                if (n2 === r2) throw new TypeError("A promise cannot be resolved with itself.");
                var e4 = r2._lib && je();
                n2 && "function" == typeof n2.then ? t3(r2, function(e5, t4) {
                  n2 instanceof _e ? n2._then(e5, t4) : n2.then(e5, t4);
                }) : (r2._state = true, r2._value = n2, Pe(r2)), e4 && Ae();
              }
            }, Oe.bind(null, r2));
          } catch (e4) {
            Oe(r2, e4);
          }
        })(this, e2);
      }
      var xe = { get: function() {
        var u2 = me, t2 = Fe;
        function e2(n2, r2) {
          var i2 = this, o2 = !u2.global && (u2 !== me || t2 !== Fe), a2 = o2 && !Ue(), e3 = new _e(function(e4, t3) {
            Ke(i2, new ke(Qe(n2, u2, o2, a2), Qe(r2, u2, o2, a2), e4, t3, u2));
          });
          return this._consoleTask && (e3._consoleTask = this._consoleTask), e3;
        }
        return e2.prototype = ae, e2;
      }, set: function(e2) {
        l(this, "then", e2 && e2.prototype === ae ? xe : { get: function() {
          return e2;
        }, set: xe.set });
      } };
      function ke(e2, t2, n2, r2, i2) {
        this.onFulfilled = "function" == typeof e2 ? e2 : null, this.onRejected = "function" == typeof t2 ? t2 : null, this.resolve = n2, this.reject = r2, this.psd = i2;
      }
      function Oe(e2, t2) {
        var n2, r2;
        pe.push(t2), null === e2._state && (n2 = e2._lib && je(), t2 = ye(t2), e2._state = false, e2._value = t2, r2 = e2, de.some(function(e3) {
          return e3._value === r2._value;
        }) || de.push(r2), Pe(e2), n2 && Ae());
      }
      function Pe(e2) {
        var t2 = e2._listeners;
        e2._listeners = [];
        for (var n2 = 0, r2 = t2.length; n2 < r2; ++n2) Ke(e2, t2[n2]);
        var i2 = e2._PSD;
        --i2.ref || i2.finalize(), 0 === ge && (++ge, le(function() {
          0 == --ge && Ce();
        }, []));
      }
      function Ke(e2, t2) {
        if (null !== e2._state) {
          var n2 = e2._state ? t2.onFulfilled : t2.onRejected;
          if (null === n2) return (e2._state ? t2.resolve : t2.reject)(e2._value);
          ++t2.psd.ref, ++ge, le(Ee, [n2, e2, t2]);
        } else e2._listeners.push(t2);
      }
      function Ee(e2, t2, n2) {
        try {
          var r2, i2 = t2._value;
          !t2._state && pe.length && (pe = []), r2 = ie && t2._consoleTask ? t2._consoleTask.run(function() {
            return e2(i2);
          }) : e2(i2), t2._state || -1 !== pe.indexOf(i2) || (function(e3) {
            var t3 = de.length;
            for (; t3; ) if (de[--t3]._value === e3._value) return de.splice(t3, 1);
          })(t2), n2.resolve(r2);
        } catch (e3) {
          n2.reject(e3);
        } finally {
          0 == --ge && Ce(), --n2.psd.ref || n2.psd.finalize();
        }
      }
      function Se() {
        $e(ve, function() {
          je() && Ae();
        });
      }
      function je() {
        var e2 = fe;
        return he = fe = false, e2;
      }
      function Ae() {
        var e2, t2, n2;
        do {
          for (; 0 < be.length; ) for (e2 = be, be = [], n2 = e2.length, t2 = 0; t2 < n2; ++t2) {
            var r2 = e2[t2];
            r2[0].apply(null, r2[1]);
          }
        } while (0 < be.length);
        he = fe = true;
      }
      function Ce() {
        var e2 = de;
        de = [], e2.forEach(function(e3) {
          e3._PSD.onunhandled.call(null, e3._value, e3);
        });
        for (var t2 = we.slice(0), n2 = t2.length; n2; ) t2[--n2]();
      }
      function Te(e2) {
        return new _e(ae, false, e2);
      }
      function qe(n2, r2) {
        var i2 = me;
        return function() {
          var e2 = je(), t2 = me;
          try {
            return We(i2, true), n2.apply(this, arguments);
          } catch (e3) {
            r2 && r2(e3);
          } finally {
            We(t2, false), e2 && Ae();
          }
        };
      }
      r(_e.prototype, { then: xe, _then: function(e2, t2) {
        Ke(this, new ke(null, null, e2, t2, me));
      }, catch: function(e2) {
        if (1 === arguments.length) return this.then(null, e2);
        var t2 = e2, n2 = arguments[1];
        return "function" == typeof t2 ? this.then(null, function(e3) {
          return (e3 instanceof t2 ? n2 : Te)(e3);
        }) : this.then(null, function(e3) {
          return (e3 && e3.name === t2 ? n2 : Te)(e3);
        });
      }, finally: function(t2) {
        return this.then(function(e2) {
          return _e.resolve(t2()).then(function() {
            return e2;
          });
        }, function(e2) {
          return _e.resolve(t2()).then(function() {
            return Te(e2);
          });
        });
      }, timeout: function(r2, i2) {
        var o2 = this;
        return r2 < 1 / 0 ? new _e(function(e2, t2) {
          var n2 = setTimeout(function() {
            return t2(new Y.Timeout(i2));
          }, r2);
          o2.then(e2, t2).finally(clearTimeout.bind(null, n2));
        }) : this;
      } }), "undefined" != typeof Symbol && Symbol.toStringTag && l(_e.prototype, Symbol.toStringTag, "Dexie.Promise"), ve.env = Ye(), r(_e, { all: function() {
        var o2 = I.apply(null, arguments).map(Ve);
        return new _e(function(n2, r2) {
          0 === o2.length && n2([]);
          var i2 = o2.length;
          o2.forEach(function(e2, t2) {
            return _e.resolve(e2).then(function(e3) {
              o2[t2] = e3, --i2 || n2(o2);
            }, r2);
          });
        });
      }, resolve: function(n2) {
        return n2 instanceof _e ? n2 : n2 && "function" == typeof n2.then ? new _e(function(e2, t2) {
          n2.then(e2, t2);
        }) : new _e(ae, true, n2);
      }, reject: Te, race: function() {
        var e2 = I.apply(null, arguments).map(Ve);
        return new _e(function(t2, n2) {
          e2.map(function(e3) {
            return _e.resolve(e3).then(t2, n2);
          });
        });
      }, PSD: { get: function() {
        return me;
      }, set: function(e2) {
        return me = e2;
      } }, totalEchoes: { get: function() {
        return Fe;
      } }, newPSD: Ne, usePSD: $e, scheduler: { get: function() {
        return le;
      }, set: function(e2) {
        le = e2;
      } }, rejectionMapper: { get: function() {
        return ye;
      }, set: function(e2) {
        ye = e2;
      } }, follow: function(i2, n2) {
        return new _e(function(e2, t2) {
          return Ne(function(n3, r2) {
            var e3 = me;
            e3.unhandleds = [], e3.onunhandled = r2, e3.finalize = J(function() {
              var t3, e4 = this;
              t3 = function() {
                0 === e4.unhandleds.length ? n3() : r2(e4.unhandleds[0]);
              }, we.push(function e5() {
                t3(), we.splice(we.indexOf(e5), 1);
              }), ++ge, le(function() {
                0 == --ge && Ce();
              }, []);
            }, e3.finalize), i2();
          }, n2, e2, t2);
        });
      } }), se && (se.allSettled && l(_e, "allSettled", function() {
        var e2 = I.apply(null, arguments).map(Ve);
        return new _e(function(n2) {
          0 === e2.length && n2([]);
          var r2 = e2.length, i2 = new Array(r2);
          e2.forEach(function(e3, t2) {
            return _e.resolve(e3).then(function(e4) {
              return i2[t2] = { status: "fulfilled", value: e4 };
            }, function(e4) {
              return i2[t2] = { status: "rejected", reason: e4 };
            }).then(function() {
              return --r2 || n2(i2);
            });
          });
        });
      }), se.any && "undefined" != typeof AggregateError && l(_e, "any", function() {
        var e2 = I.apply(null, arguments).map(Ve);
        return new _e(function(n2, r2) {
          0 === e2.length && r2(new AggregateError([]));
          var i2 = e2.length, o2 = new Array(i2);
          e2.forEach(function(e3, t2) {
            return _e.resolve(e3).then(function(e4) {
              return n2(e4);
            }, function(e4) {
              o2[t2] = e4, --i2 || r2(new AggregateError(o2));
            });
          });
        });
      }), se.withResolvers && (_e.withResolvers = se.withResolvers));
      var De = { awaits: 0, echoes: 0, id: 0 }, Ie = 0, Be = [], Re = 0, Fe = 0, Me = 0;
      function Ne(e2, t2, n2, r2) {
        var i2 = me, o2 = Object.create(i2);
        o2.parent = i2, o2.ref = 0, o2.global = false, o2.id = ++Me, ve.env, o2.env = ce ? { Promise: _e, PromiseProp: { value: _e, configurable: true, writable: true }, all: _e.all, race: _e.race, allSettled: _e.allSettled, any: _e.any, resolve: _e.resolve, reject: _e.reject } : {}, t2 && a(o2, t2), ++i2.ref, o2.finalize = function() {
          --this.parent.ref || this.parent.finalize();
        };
        r2 = $e(o2, e2, n2, r2);
        return 0 === o2.ref && o2.finalize(), r2;
      }
      function Le() {
        return De.id || (De.id = ++Ie), ++De.awaits, De.echoes += ue, De.id;
      }
      function Ue() {
        return !!De.awaits && (0 == --De.awaits && (De.id = 0), De.echoes = De.awaits * ue, true);
      }
      function Ve(e2) {
        return De.echoes && e2 && e2.constructor === se ? (Le(), e2.then(function(e3) {
          return Ue(), e3;
        }, function(e3) {
          return Ue(), Xe(e3);
        })) : e2;
      }
      function ze() {
        var e2 = Be[Be.length - 1];
        Be.pop(), We(e2, false);
      }
      function We(e2, t2) {
        var n2, r2 = me;
        (t2 ? !De.echoes || Re++ && e2 === me : !Re || --Re && e2 === me) || queueMicrotask(t2 ? (function(e3) {
          ++Fe, De.echoes && 0 != --De.echoes || (De.echoes = De.awaits = De.id = 0), Be.push(me), We(e3, true);
        }).bind(null, e2) : ze), e2 !== me && (me = e2, r2 === ve && (ve.env = Ye()), ce && (n2 = ve.env.Promise, t2 = e2.env, (r2.global || e2.global) && (Object.defineProperty(f, "Promise", t2.PromiseProp), n2.all = t2.all, n2.race = t2.race, n2.resolve = t2.resolve, n2.reject = t2.reject, t2.allSettled && (n2.allSettled = t2.allSettled), t2.any && (n2.any = t2.any))));
      }
      function Ye() {
        var e2 = f.Promise;
        return ce ? { Promise: e2, PromiseProp: Object.getOwnPropertyDescriptor(f, "Promise"), all: e2.all, race: e2.race, allSettled: e2.allSettled, any: e2.any, resolve: e2.resolve, reject: e2.reject } : {};
      }
      function $e(e2, t2, n2, r2, i2) {
        var o2 = me;
        try {
          return We(e2, true), t2(n2, r2, i2);
        } finally {
          We(o2, false);
        }
      }
      function Qe(t2, n2, r2, i2) {
        return "function" != typeof t2 ? t2 : function() {
          var e2 = me;
          r2 && Le(), We(n2, true);
          try {
            return t2.apply(this, arguments);
          } finally {
            We(e2, false), i2 && queueMicrotask(Ue);
          }
        };
      }
      function Ge(e2) {
        Promise === se && 0 === De.echoes ? 0 === Re ? e2() : enqueueNativeMicroTask(e2) : setTimeout(e2, 0);
      }
      -1 === ("" + F).indexOf("[native code]") && (Le = Ue = G);
      var Xe = _e.reject;
      var He = String.fromCharCode(65535), Je = "Invalid key provided. Keys must be of type string, number, Date or Array<string | number | Date>.", Ze = "String expected.", et = [], tt = "__dbnames", nt = "readonly", rt = "readwrite";
      function it(e2, t2) {
        return e2 ? t2 ? function() {
          return e2.apply(this, arguments) && t2.apply(this, arguments);
        } : e2 : t2;
      }
      var ot = { type: 3, lower: -1 / 0, lowerOpen: false, upper: [[]], upperOpen: false };
      function at(t2) {
        return "string" != typeof t2 || /\./.test(t2) ? function(e2) {
          return e2;
        } : function(e2) {
          return void 0 === e2[t2] && t2 in e2 && delete (e2 = S(e2))[t2], e2;
        };
      }
      function ut() {
        throw Y.Type();
      }
      function st(e2, t2) {
        try {
          var n2 = ct(e2), r2 = ct(t2);
          if (n2 !== r2) return "Array" === n2 ? 1 : "Array" === r2 ? -1 : "binary" === n2 ? 1 : "binary" === r2 ? -1 : "string" === n2 ? 1 : "string" === r2 ? -1 : "Date" === n2 ? 1 : "Date" !== r2 ? NaN : -1;
          switch (n2) {
            case "number":
            case "Date":
            case "string":
              return t2 < e2 ? 1 : e2 < t2 ? -1 : 0;
            case "binary":
              return (function(e3, t3) {
                for (var n3 = e3.length, r3 = t3.length, i2 = n3 < r3 ? n3 : r3, o2 = 0; o2 < i2; ++o2) if (e3[o2] !== t3[o2]) return e3[o2] < t3[o2] ? -1 : 1;
                return n3 === r3 ? 0 : n3 < r3 ? -1 : 1;
              })(lt(e2), lt(t2));
            case "Array":
              return (function(e3, t3) {
                for (var n3 = e3.length, r3 = t3.length, i2 = n3 < r3 ? n3 : r3, o2 = 0; o2 < i2; ++o2) {
                  var a2 = st(e3[o2], t3[o2]);
                  if (0 !== a2) return a2;
                }
                return n3 === r3 ? 0 : n3 < r3 ? -1 : 1;
              })(e2, t2);
          }
        } catch (e3) {
        }
        return NaN;
      }
      function ct(e2) {
        var t2 = typeof e2;
        if ("object" != t2) return t2;
        if (ArrayBuffer.isView(e2)) return "binary";
        e2 = A(e2);
        return "ArrayBuffer" === e2 ? "binary" : e2;
      }
      function lt(e2) {
        return e2 instanceof Uint8Array ? e2 : ArrayBuffer.isView(e2) ? new Uint8Array(e2.buffer, e2.byteOffset, e2.byteLength) : new Uint8Array(e2);
      }
      var ft = (ht.prototype._trans = function(e2, r2, t2) {
        var n2 = this._tx || me.trans, i2 = this.name, o2 = ie && "undefined" != typeof console && console.createTask && console.createTask("Dexie: ".concat("readonly" === e2 ? "read" : "write", " ").concat(this.name));
        function a2(e3, t3, n3) {
          if (!n3.schema[i2]) throw new Y.NotFound("Table " + i2 + " not part of transaction");
          return r2(n3.idbtrans, n3);
        }
        var u2 = je();
        try {
          var s2 = n2 && n2.db._novip === this.db._novip ? n2 === me.trans ? n2._promise(e2, a2, t2) : Ne(function() {
            return n2._promise(e2, a2, t2);
          }, { trans: n2, transless: me.transless || me }) : (function t3(n3, r3, i3, o3) {
            if (n3.idbdb && (n3._state.openComplete || me.letThrough || n3._vip)) {
              var a3 = n3._createTransaction(r3, i3, n3._dbSchema);
              try {
                a3.create(), n3._state.PR1398_maxLoop = 3;
              } catch (e3) {
                return e3.name === z.InvalidState && n3.isOpen() && 0 < --n3._state.PR1398_maxLoop ? (console.warn("Dexie: Need to reopen db"), n3.close({ disableAutoOpen: false }), n3.open().then(function() {
                  return t3(n3, r3, i3, o3);
                })) : Xe(e3);
              }
              return a3._promise(r3, function(e3, t4) {
                return Ne(function() {
                  return me.trans = a3, o3(e3, t4, a3);
                });
              }).then(function(e3) {
                if ("readwrite" === r3) try {
                  a3.idbtrans.commit();
                } catch (e4) {
                }
                return "readonly" === r3 ? e3 : a3._completion.then(function() {
                  return e3;
                });
              });
            }
            if (n3._state.openComplete) return Xe(new Y.DatabaseClosed(n3._state.dbOpenError));
            if (!n3._state.isBeingOpened) {
              if (!n3._state.autoOpen) return Xe(new Y.DatabaseClosed());
              n3.open().catch(G);
            }
            return n3._state.dbReadyPromise.then(function() {
              return t3(n3, r3, i3, o3);
            });
          })(this.db, e2, [this.name], a2);
          return o2 && (s2._consoleTask = o2, s2 = s2.catch(function(e3) {
            return console.trace(e3), Xe(e3);
          })), s2;
        } finally {
          u2 && Ae();
        }
      }, ht.prototype.get = function(t2, e2) {
        var n2 = this;
        return t2 && t2.constructor === Object ? this.where(t2).first(e2) : null == t2 ? Xe(new Y.Type("Invalid argument to Table.get()")) : this._trans("readonly", function(e3) {
          return n2.core.get({ trans: e3, key: t2 }).then(function(e4) {
            return n2.hook.reading.fire(e4);
          });
        }).then(e2);
      }, ht.prototype.where = function(o2) {
        if ("string" == typeof o2) return new this.db.WhereClause(this, o2);
        if (k(o2)) return new this.db.WhereClause(this, "[".concat(o2.join("+"), "]"));
        var n2 = x(o2);
        if (1 === n2.length) return this.where(n2[0]).equals(o2[n2[0]]);
        var e2 = this.schema.indexes.concat(this.schema.primKey).filter(function(t3) {
          if (t3.compound && n2.every(function(e4) {
            return 0 <= t3.keyPath.indexOf(e4);
          })) {
            for (var e3 = 0; e3 < n2.length; ++e3) if (-1 === n2.indexOf(t3.keyPath[e3])) return false;
            return true;
          }
          return false;
        }).sort(function(e3, t3) {
          return e3.keyPath.length - t3.keyPath.length;
        })[0];
        if (e2 && this.db._maxKey !== He) {
          var t2 = e2.keyPath.slice(0, n2.length);
          return this.where(t2).equals(t2.map(function(e3) {
            return o2[e3];
          }));
        }
        !e2 && ie && console.warn("The query ".concat(JSON.stringify(o2), " on ").concat(this.name, " would benefit from a ") + "compound index [".concat(n2.join("+"), "]"));
        var a2 = this.schema.idxByName;
        function u2(e3, t3) {
          return 0 === st(e3, t3);
        }
        var r2 = n2.reduce(function(e3, t3) {
          var n3 = e3[0], r3 = e3[1], e3 = a2[t3], i2 = o2[t3];
          return [n3 || e3, n3 || !e3 ? it(r3, e3 && e3.multi ? function(e4) {
            e4 = O(e4, t3);
            return k(e4) && e4.some(function(e5) {
              return u2(i2, e5);
            });
          } : function(e4) {
            return u2(i2, O(e4, t3));
          }) : r3];
        }, [null, null]), t2 = r2[0], r2 = r2[1];
        return t2 ? this.where(t2.name).equals(o2[t2.keyPath]).filter(r2) : e2 ? this.filter(r2) : this.where(n2).equals("");
      }, ht.prototype.filter = function(e2) {
        return this.toCollection().and(e2);
      }, ht.prototype.count = function(e2) {
        return this.toCollection().count(e2);
      }, ht.prototype.offset = function(e2) {
        return this.toCollection().offset(e2);
      }, ht.prototype.limit = function(e2) {
        return this.toCollection().limit(e2);
      }, ht.prototype.each = function(e2) {
        return this.toCollection().each(e2);
      }, ht.prototype.toArray = function(e2) {
        return this.toCollection().toArray(e2);
      }, ht.prototype.toCollection = function() {
        return new this.db.Collection(new this.db.WhereClause(this));
      }, ht.prototype.orderBy = function(e2) {
        return new this.db.Collection(new this.db.WhereClause(this, k(e2) ? "[".concat(e2.join("+"), "]") : e2));
      }, ht.prototype.reverse = function() {
        return this.toCollection().reverse();
      }, ht.prototype.mapToClass = function(r2) {
        var e2, t2 = this.db, n2 = this.name;
        function i2() {
          return null !== e2 && e2.apply(this, arguments) || this;
        }
        (this.schema.mappedClass = r2).prototype instanceof ut && ((function(e3, t3) {
          if ("function" != typeof t3 && null !== t3) throw new TypeError("Class extends value " + String(t3) + " is not a constructor or null");
          function n3() {
            this.constructor = e3;
          }
          s(e3, t3), e3.prototype = null === t3 ? Object.create(t3) : (n3.prototype = t3.prototype, new n3());
        })(i2, e2 = r2), Object.defineProperty(i2.prototype, "db", { get: function() {
          return t2;
        }, enumerable: false, configurable: true }), i2.prototype.table = function() {
          return n2;
        }, r2 = i2);
        for (var o2 = /* @__PURE__ */ new Set(), a2 = r2.prototype; a2; a2 = c(a2)) Object.getOwnPropertyNames(a2).forEach(function(e3) {
          return o2.add(e3);
        });
        function u2(e3) {
          if (!e3) return e3;
          var t3, n3 = Object.create(r2.prototype);
          for (t3 in e3) if (!o2.has(t3)) try {
            n3[t3] = e3[t3];
          } catch (e4) {
          }
          return n3;
        }
        return this.schema.readHook && this.hook.reading.unsubscribe(this.schema.readHook), this.schema.readHook = u2, this.hook("reading", u2), r2;
      }, ht.prototype.defineClass = function() {
        return this.mapToClass(function(e2) {
          a(this, e2);
        });
      }, ht.prototype.add = function(t2, n2) {
        var r2 = this, e2 = this.schema.primKey, i2 = e2.auto, o2 = e2.keyPath, a2 = t2;
        return o2 && i2 && (a2 = at(o2)(t2)), this._trans("readwrite", function(e3) {
          return r2.core.mutate({ trans: e3, type: "add", keys: null != n2 ? [n2] : null, values: [a2] });
        }).then(function(e3) {
          return e3.numFailures ? _e.reject(e3.failures[0]) : e3.lastResult;
        }).then(function(e3) {
          if (o2) try {
            P(t2, o2, e3);
          } catch (e4) {
          }
          return e3;
        });
      }, ht.prototype.update = function(e2, t2) {
        if ("object" != typeof e2 || k(e2)) return this.where(":id").equals(e2).modify(t2);
        e2 = O(e2, this.schema.primKey.keyPath);
        return void 0 === e2 ? Xe(new Y.InvalidArgument("Given object does not contain its primary key")) : this.where(":id").equals(e2).modify(t2);
      }, ht.prototype.put = function(t2, n2) {
        var r2 = this, e2 = this.schema.primKey, i2 = e2.auto, o2 = e2.keyPath, a2 = t2;
        return o2 && i2 && (a2 = at(o2)(t2)), this._trans("readwrite", function(e3) {
          return r2.core.mutate({ trans: e3, type: "put", values: [a2], keys: null != n2 ? [n2] : null });
        }).then(function(e3) {
          return e3.numFailures ? _e.reject(e3.failures[0]) : e3.lastResult;
        }).then(function(e3) {
          if (o2) try {
            P(t2, o2, e3);
          } catch (e4) {
          }
          return e3;
        });
      }, ht.prototype.delete = function(t2) {
        var n2 = this;
        return this._trans("readwrite", function(e2) {
          return n2.core.mutate({ trans: e2, type: "delete", keys: [t2] });
        }).then(function(e2) {
          return e2.numFailures ? _e.reject(e2.failures[0]) : void 0;
        });
      }, ht.prototype.clear = function() {
        var t2 = this;
        return this._trans("readwrite", function(e2) {
          return t2.core.mutate({ trans: e2, type: "deleteRange", range: ot });
        }).then(function(e2) {
          return e2.numFailures ? _e.reject(e2.failures[0]) : void 0;
        });
      }, ht.prototype.bulkGet = function(t2) {
        var n2 = this;
        return this._trans("readonly", function(e2) {
          return n2.core.getMany({ keys: t2, trans: e2 }).then(function(e3) {
            return e3.map(function(e4) {
              return n2.hook.reading.fire(e4);
            });
          });
        });
      }, ht.prototype.bulkAdd = function(r2, e2, t2) {
        var o2 = this, a2 = Array.isArray(e2) ? e2 : void 0, u2 = (t2 = t2 || (a2 ? void 0 : e2)) ? t2.allKeys : void 0;
        return this._trans("readwrite", function(e3) {
          var t3 = o2.schema.primKey, n2 = t3.auto, t3 = t3.keyPath;
          if (t3 && a2) throw new Y.InvalidArgument("bulkAdd(): keys argument invalid on tables with inbound keys");
          if (a2 && a2.length !== r2.length) throw new Y.InvalidArgument("Arguments objects and keys must have the same length");
          var i2 = r2.length, t3 = t3 && n2 ? r2.map(at(t3)) : r2;
          return o2.core.mutate({ trans: e3, type: "add", keys: a2, values: t3, wantResults: u2 }).then(function(e4) {
            var t4 = e4.numFailures, n3 = e4.results, r3 = e4.lastResult, e4 = e4.failures;
            if (0 === t4) return u2 ? n3 : r3;
            throw new V("".concat(o2.name, ".bulkAdd(): ").concat(t4, " of ").concat(i2, " operations failed"), e4);
          });
        });
      }, ht.prototype.bulkPut = function(r2, e2, t2) {
        var o2 = this, a2 = Array.isArray(e2) ? e2 : void 0, u2 = (t2 = t2 || (a2 ? void 0 : e2)) ? t2.allKeys : void 0;
        return this._trans("readwrite", function(e3) {
          var t3 = o2.schema.primKey, n2 = t3.auto, t3 = t3.keyPath;
          if (t3 && a2) throw new Y.InvalidArgument("bulkPut(): keys argument invalid on tables with inbound keys");
          if (a2 && a2.length !== r2.length) throw new Y.InvalidArgument("Arguments objects and keys must have the same length");
          var i2 = r2.length, t3 = t3 && n2 ? r2.map(at(t3)) : r2;
          return o2.core.mutate({ trans: e3, type: "put", keys: a2, values: t3, wantResults: u2 }).then(function(e4) {
            var t4 = e4.numFailures, n3 = e4.results, r3 = e4.lastResult, e4 = e4.failures;
            if (0 === t4) return u2 ? n3 : r3;
            throw new V("".concat(o2.name, ".bulkPut(): ").concat(t4, " of ").concat(i2, " operations failed"), e4);
          });
        });
      }, ht.prototype.bulkUpdate = function(t2) {
        var h2 = this, n2 = this.core, r2 = t2.map(function(e2) {
          return e2.key;
        }), i2 = t2.map(function(e2) {
          return e2.changes;
        }), d2 = [];
        return this._trans("readwrite", function(e2) {
          return n2.getMany({ trans: e2, keys: r2, cache: "clone" }).then(function(c2) {
            var l2 = [], f2 = [];
            t2.forEach(function(e3, t3) {
              var n3 = e3.key, r3 = e3.changes, i3 = c2[t3];
              if (i3) {
                for (var o2 = 0, a2 = Object.keys(r3); o2 < a2.length; o2++) {
                  var u2 = a2[o2], s3 = r3[u2];
                  if (u2 === h2.schema.primKey.keyPath) {
                    if (0 !== st(s3, n3)) throw new Y.Constraint("Cannot update primary key in bulkUpdate()");
                  } else P(i3, u2, s3);
                }
                d2.push(t3), l2.push(n3), f2.push(i3);
              }
            });
            var s2 = l2.length;
            return n2.mutate({ trans: e2, type: "put", keys: l2, values: f2, updates: { keys: r2, changeSpecs: i2 } }).then(function(e3) {
              var t3 = e3.numFailures, n3 = e3.failures;
              if (0 === t3) return s2;
              for (var r3 = 0, i3 = Object.keys(n3); r3 < i3.length; r3++) {
                var o2, a2 = i3[r3], u2 = d2[Number(a2)];
                null != u2 && (o2 = n3[a2], delete n3[a2], n3[u2] = o2);
              }
              throw new V("".concat(h2.name, ".bulkUpdate(): ").concat(t3, " of ").concat(s2, " operations failed"), n3);
            });
          });
        });
      }, ht.prototype.bulkDelete = function(t2) {
        var r2 = this, i2 = t2.length;
        return this._trans("readwrite", function(e2) {
          return r2.core.mutate({ trans: e2, type: "delete", keys: t2 });
        }).then(function(e2) {
          var t3 = e2.numFailures, n2 = e2.lastResult, e2 = e2.failures;
          if (0 === t3) return n2;
          throw new V("".concat(r2.name, ".bulkDelete(): ").concat(t3, " of ").concat(i2, " operations failed"), e2);
        });
      }, ht);
      function ht() {
      }
      function dt(i2) {
        function t2(e3, t3) {
          if (t3) {
            for (var n3 = arguments.length, r2 = new Array(n3 - 1); --n3; ) r2[n3 - 1] = arguments[n3];
            return a2[e3].subscribe.apply(null, r2), i2;
          }
          if ("string" == typeof e3) return a2[e3];
        }
        var a2 = {};
        t2.addEventType = u2;
        for (var e2 = 1, n2 = arguments.length; e2 < n2; ++e2) u2(arguments[e2]);
        return t2;
        function u2(e3, n3, r2) {
          if ("object" != typeof e3) {
            var i3;
            n3 = n3 || ne;
            var o2 = { subscribers: [], fire: r2 = r2 || G, subscribe: function(e4) {
              -1 === o2.subscribers.indexOf(e4) && (o2.subscribers.push(e4), o2.fire = n3(o2.fire, e4));
            }, unsubscribe: function(t3) {
              o2.subscribers = o2.subscribers.filter(function(e4) {
                return e4 !== t3;
              }), o2.fire = o2.subscribers.reduce(n3, r2);
            } };
            return a2[e3] = t2[e3] = o2;
          }
          x(i3 = e3).forEach(function(e4) {
            var t3 = i3[e4];
            if (k(t3)) u2(e4, i3[e4][0], i3[e4][1]);
            else {
              if ("asap" !== t3) throw new Y.InvalidArgument("Invalid event config");
              var n4 = u2(e4, X, function() {
                for (var e5 = arguments.length, t4 = new Array(e5); e5--; ) t4[e5] = arguments[e5];
                n4.subscribers.forEach(function(e6) {
                  v(function() {
                    e6.apply(null, t4);
                  });
                });
              });
            }
          });
        }
      }
      function pt(e2, t2) {
        return o(t2).from({ prototype: e2 }), t2;
      }
      function yt(e2, t2) {
        return !(e2.filter || e2.algorithm || e2.or) && (t2 ? e2.justLimit : !e2.replayFilter);
      }
      function vt(e2, t2) {
        e2.filter = it(e2.filter, t2);
      }
      function mt(e2, t2, n2) {
        var r2 = e2.replayFilter;
        e2.replayFilter = r2 ? function() {
          return it(r2(), t2());
        } : t2, e2.justLimit = n2 && !r2;
      }
      function bt(e2, t2) {
        if (e2.isPrimKey) return t2.primaryKey;
        var n2 = t2.getIndexByKeyPath(e2.index);
        if (!n2) throw new Y.Schema("KeyPath " + e2.index + " on object store " + t2.name + " is not indexed");
        return n2;
      }
      function gt(e2, t2, n2) {
        var r2 = bt(e2, t2.schema);
        return t2.openCursor({ trans: n2, values: !e2.keysOnly, reverse: "prev" === e2.dir, unique: !!e2.unique, query: { index: r2, range: e2.range } });
      }
      function wt(e2, o2, t2, n2) {
        var a2 = e2.replayFilter ? it(e2.filter, e2.replayFilter()) : e2.filter;
        if (e2.or) {
          var u2 = {}, r2 = function(e3, t3, n3) {
            var r3, i2;
            a2 && !a2(t3, n3, function(e4) {
              return t3.stop(e4);
            }, function(e4) {
              return t3.fail(e4);
            }) || ("[object ArrayBuffer]" === (i2 = "" + (r3 = t3.primaryKey)) && (i2 = "" + new Uint8Array(r3)), m(u2, i2) || (u2[i2] = true, o2(e3, t3, n3)));
          };
          return Promise.all([e2.or._iterate(r2, t2), _t(gt(e2, n2, t2), e2.algorithm, r2, !e2.keysOnly && e2.valueMapper)]);
        }
        return _t(gt(e2, n2, t2), it(e2.algorithm, a2), o2, !e2.keysOnly && e2.valueMapper);
      }
      function _t(e2, r2, i2, o2) {
        var a2 = qe(o2 ? function(e3, t2, n2) {
          return i2(o2(e3), t2, n2);
        } : i2);
        return e2.then(function(n2) {
          if (n2) return n2.start(function() {
            var t2 = function() {
              return n2.continue();
            };
            r2 && !r2(n2, function(e3) {
              return t2 = e3;
            }, function(e3) {
              n2.stop(e3), t2 = G;
            }, function(e3) {
              n2.fail(e3), t2 = G;
            }) || a2(n2.value, n2, function(e3) {
              return t2 = e3;
            }), t2();
          });
        });
      }
      var xt = (kt.prototype.execute = function(e2) {
        var t2 = this["@@propmod"];
        if (void 0 !== t2.add) {
          var n2 = t2.add;
          if (k(n2)) return i(i([], k(e2) ? e2 : [], true), n2).sort();
          if ("number" == typeof n2) return (Number(e2) || 0) + n2;
          if ("bigint" == typeof n2) try {
            return BigInt(e2) + n2;
          } catch (e3) {
            return BigInt(0) + n2;
          }
          throw new TypeError("Invalid term ".concat(n2));
        }
        if (void 0 !== t2.remove) {
          var r2 = t2.remove;
          if (k(r2)) return k(e2) ? e2.filter(function(e3) {
            return !r2.includes(e3);
          }).sort() : [];
          if ("number" == typeof r2) return Number(e2) - r2;
          if ("bigint" == typeof r2) try {
            return BigInt(e2) - r2;
          } catch (e3) {
            return BigInt(0) - r2;
          }
          throw new TypeError("Invalid subtrahend ".concat(r2));
        }
        n2 = null === (n2 = t2.replacePrefix) || void 0 === n2 ? void 0 : n2[0];
        return n2 && "string" == typeof e2 && e2.startsWith(n2) ? t2.replacePrefix[1] + e2.substring(n2.length) : e2;
      }, kt);
      function kt(e2) {
        this["@@propmod"] = e2;
      }
      var Ot = (Pt.prototype._read = function(e2, t2) {
        var n2 = this._ctx;
        return n2.error ? n2.table._trans(null, Xe.bind(null, n2.error)) : n2.table._trans("readonly", e2).then(t2);
      }, Pt.prototype._write = function(e2) {
        var t2 = this._ctx;
        return t2.error ? t2.table._trans(null, Xe.bind(null, t2.error)) : t2.table._trans("readwrite", e2, "locked");
      }, Pt.prototype._addAlgorithm = function(e2) {
        var t2 = this._ctx;
        t2.algorithm = it(t2.algorithm, e2);
      }, Pt.prototype._iterate = function(e2, t2) {
        return wt(this._ctx, e2, t2, this._ctx.table.core);
      }, Pt.prototype.clone = function(e2) {
        var t2 = Object.create(this.constructor.prototype), n2 = Object.create(this._ctx);
        return e2 && a(n2, e2), t2._ctx = n2, t2;
      }, Pt.prototype.raw = function() {
        return this._ctx.valueMapper = null, this;
      }, Pt.prototype.each = function(t2) {
        var n2 = this._ctx;
        return this._read(function(e2) {
          return wt(n2, t2, e2, n2.table.core);
        });
      }, Pt.prototype.count = function(e2) {
        var i2 = this;
        return this._read(function(e3) {
          var t2 = i2._ctx, n2 = t2.table.core;
          if (yt(t2, true)) return n2.count({ trans: e3, query: { index: bt(t2, n2.schema), range: t2.range } }).then(function(e4) {
            return Math.min(e4, t2.limit);
          });
          var r2 = 0;
          return wt(t2, function() {
            return ++r2, false;
          }, e3, n2).then(function() {
            return r2;
          });
        }).then(e2);
      }, Pt.prototype.sortBy = function(e2, t2) {
        var n2 = e2.split(".").reverse(), r2 = n2[0], i2 = n2.length - 1;
        function o2(e3, t3) {
          return t3 ? o2(e3[n2[t3]], t3 - 1) : e3[r2];
        }
        var a2 = "next" === this._ctx.dir ? 1 : -1;
        function u2(e3, t3) {
          return st(o2(e3, i2), o2(t3, i2)) * a2;
        }
        return this.toArray(function(e3) {
          return e3.sort(u2);
        }).then(t2);
      }, Pt.prototype.toArray = function(e2) {
        var o2 = this;
        return this._read(function(e3) {
          var t2 = o2._ctx;
          if ("next" === t2.dir && yt(t2, true) && 0 < t2.limit) {
            var n2 = t2.valueMapper, r2 = bt(t2, t2.table.core.schema);
            return t2.table.core.query({ trans: e3, limit: t2.limit, values: true, query: { index: r2, range: t2.range } }).then(function(e4) {
              e4 = e4.result;
              return n2 ? e4.map(n2) : e4;
            });
          }
          var i2 = [];
          return wt(t2, function(e4) {
            return i2.push(e4);
          }, e3, t2.table.core).then(function() {
            return i2;
          });
        }, e2);
      }, Pt.prototype.offset = function(t2) {
        var e2 = this._ctx;
        return t2 <= 0 || (e2.offset += t2, yt(e2) ? mt(e2, function() {
          var n2 = t2;
          return function(e3, t3) {
            return 0 === n2 || (1 === n2 ? --n2 : t3(function() {
              e3.advance(n2), n2 = 0;
            }), false);
          };
        }) : mt(e2, function() {
          var e3 = t2;
          return function() {
            return --e3 < 0;
          };
        })), this;
      }, Pt.prototype.limit = function(e2) {
        return this._ctx.limit = Math.min(this._ctx.limit, e2), mt(this._ctx, function() {
          var r2 = e2;
          return function(e3, t2, n2) {
            return --r2 <= 0 && t2(n2), 0 <= r2;
          };
        }, true), this;
      }, Pt.prototype.until = function(r2, i2) {
        return vt(this._ctx, function(e2, t2, n2) {
          return !r2(e2.value) || (t2(n2), i2);
        }), this;
      }, Pt.prototype.first = function(e2) {
        return this.limit(1).toArray(function(e3) {
          return e3[0];
        }).then(e2);
      }, Pt.prototype.last = function(e2) {
        return this.reverse().first(e2);
      }, Pt.prototype.filter = function(t2) {
        var e2;
        return vt(this._ctx, function(e3) {
          return t2(e3.value);
        }), (e2 = this._ctx).isMatch = it(e2.isMatch, t2), this;
      }, Pt.prototype.and = function(e2) {
        return this.filter(e2);
      }, Pt.prototype.or = function(e2) {
        return new this.db.WhereClause(this._ctx.table, e2, this);
      }, Pt.prototype.reverse = function() {
        return this._ctx.dir = "prev" === this._ctx.dir ? "next" : "prev", this._ondirectionchange && this._ondirectionchange(this._ctx.dir), this;
      }, Pt.prototype.desc = function() {
        return this.reverse();
      }, Pt.prototype.eachKey = function(n2) {
        var e2 = this._ctx;
        return e2.keysOnly = !e2.isMatch, this.each(function(e3, t2) {
          n2(t2.key, t2);
        });
      }, Pt.prototype.eachUniqueKey = function(e2) {
        return this._ctx.unique = "unique", this.eachKey(e2);
      }, Pt.prototype.eachPrimaryKey = function(n2) {
        var e2 = this._ctx;
        return e2.keysOnly = !e2.isMatch, this.each(function(e3, t2) {
          n2(t2.primaryKey, t2);
        });
      }, Pt.prototype.keys = function(e2) {
        var t2 = this._ctx;
        t2.keysOnly = !t2.isMatch;
        var n2 = [];
        return this.each(function(e3, t3) {
          n2.push(t3.key);
        }).then(function() {
          return n2;
        }).then(e2);
      }, Pt.prototype.primaryKeys = function(e2) {
        var n2 = this._ctx;
        if ("next" === n2.dir && yt(n2, true) && 0 < n2.limit) return this._read(function(e3) {
          var t2 = bt(n2, n2.table.core.schema);
          return n2.table.core.query({ trans: e3, values: false, limit: n2.limit, query: { index: t2, range: n2.range } });
        }).then(function(e3) {
          return e3.result;
        }).then(e2);
        n2.keysOnly = !n2.isMatch;
        var r2 = [];
        return this.each(function(e3, t2) {
          r2.push(t2.primaryKey);
        }).then(function() {
          return r2;
        }).then(e2);
      }, Pt.prototype.uniqueKeys = function(e2) {
        return this._ctx.unique = "unique", this.keys(e2);
      }, Pt.prototype.firstKey = function(e2) {
        return this.limit(1).keys(function(e3) {
          return e3[0];
        }).then(e2);
      }, Pt.prototype.lastKey = function(e2) {
        return this.reverse().firstKey(e2);
      }, Pt.prototype.distinct = function() {
        var e2 = this._ctx, e2 = e2.index && e2.table.schema.idxByName[e2.index];
        if (!e2 || !e2.multi) return this;
        var n2 = {};
        return vt(this._ctx, function(e3) {
          var t2 = e3.primaryKey.toString(), e3 = m(n2, t2);
          return n2[t2] = true, !e3;
        }), this;
      }, Pt.prototype.modify = function(w2) {
        var n2 = this, r2 = this._ctx;
        return this._write(function(d2) {
          var a2, u2, p2;
          p2 = "function" == typeof w2 ? w2 : (a2 = x(w2), u2 = a2.length, function(e3) {
            for (var t3 = false, n3 = 0; n3 < u2; ++n3) {
              var r3 = a2[n3], i2 = w2[r3], o2 = O(e3, r3);
              i2 instanceof xt ? (P(e3, r3, i2.execute(o2)), t3 = true) : o2 !== i2 && (P(e3, r3, i2), t3 = true);
            }
            return t3;
          });
          var y2 = r2.table.core, e2 = y2.schema.primaryKey, v2 = e2.outbound, m2 = e2.extractKey, b2 = 200, e2 = n2.db._options.modifyChunkSize;
          e2 && (b2 = "object" == typeof e2 ? e2[y2.name] || e2["*"] || 200 : e2);
          function g2(e3, t3) {
            var n3 = t3.failures, t3 = t3.numFailures;
            c2 += e3 - t3;
            for (var r3 = 0, i2 = x(n3); r3 < i2.length; r3++) {
              var o2 = i2[r3];
              s2.push(n3[o2]);
            }
          }
          var s2 = [], c2 = 0, t2 = [];
          return n2.clone().primaryKeys().then(function(l2) {
            function f2(s3) {
              var c3 = Math.min(b2, l2.length - s3);
              return y2.getMany({ trans: d2, keys: l2.slice(s3, s3 + c3), cache: "immutable" }).then(function(e3) {
                for (var n3 = [], t3 = [], r3 = v2 ? [] : null, i2 = [], o2 = 0; o2 < c3; ++o2) {
                  var a3 = e3[o2], u3 = { value: S(a3), primKey: l2[s3 + o2] };
                  false !== p2.call(u3, u3.value, u3) && (null == u3.value ? i2.push(l2[s3 + o2]) : v2 || 0 === st(m2(a3), m2(u3.value)) ? (t3.push(u3.value), v2 && r3.push(l2[s3 + o2])) : (i2.push(l2[s3 + o2]), n3.push(u3.value)));
                }
                return Promise.resolve(0 < n3.length && y2.mutate({ trans: d2, type: "add", values: n3 }).then(function(e4) {
                  for (var t4 in e4.failures) i2.splice(parseInt(t4), 1);
                  g2(n3.length, e4);
                })).then(function() {
                  return (0 < t3.length || h2 && "object" == typeof w2) && y2.mutate({ trans: d2, type: "put", keys: r3, values: t3, criteria: h2, changeSpec: "function" != typeof w2 && w2, isAdditionalChunk: 0 < s3 }).then(function(e4) {
                    return g2(t3.length, e4);
                  });
                }).then(function() {
                  return (0 < i2.length || h2 && w2 === Kt) && y2.mutate({ trans: d2, type: "delete", keys: i2, criteria: h2, isAdditionalChunk: 0 < s3 }).then(function(e4) {
                    return g2(i2.length, e4);
                  });
                }).then(function() {
                  return l2.length > s3 + c3 && f2(s3 + b2);
                });
              });
            }
            var h2 = yt(r2) && r2.limit === 1 / 0 && ("function" != typeof w2 || w2 === Kt) && { index: r2.index, range: r2.range };
            return f2(0).then(function() {
              if (0 < s2.length) throw new U("Error modifying one or more objects", s2, c2, t2);
              return l2.length;
            });
          });
        });
      }, Pt.prototype.delete = function() {
        var i2 = this._ctx, n2 = i2.range;
        return yt(i2) && (i2.isPrimKey || 3 === n2.type) ? this._write(function(e2) {
          var t2 = i2.table.core.schema.primaryKey, r2 = n2;
          return i2.table.core.count({ trans: e2, query: { index: t2, range: r2 } }).then(function(n3) {
            return i2.table.core.mutate({ trans: e2, type: "deleteRange", range: r2 }).then(function(e3) {
              var t3 = e3.failures;
              e3.lastResult, e3.results;
              e3 = e3.numFailures;
              if (e3) throw new U("Could not delete some values", Object.keys(t3).map(function(e4) {
                return t3[e4];
              }), n3 - e3);
              return n3 - e3;
            });
          });
        }) : this.modify(Kt);
      }, Pt);
      function Pt() {
      }
      var Kt = function(e2, t2) {
        return t2.value = null;
      };
      function Et(e2, t2) {
        return e2 < t2 ? -1 : e2 === t2 ? 0 : 1;
      }
      function St(e2, t2) {
        return t2 < e2 ? -1 : e2 === t2 ? 0 : 1;
      }
      function jt(e2, t2, n2) {
        e2 = e2 instanceof Dt ? new e2.Collection(e2) : e2;
        return e2._ctx.error = new (n2 || TypeError)(t2), e2;
      }
      function At(e2) {
        return new e2.Collection(e2, function() {
          return qt("");
        }).limit(0);
      }
      function Ct(e2, s2, n2, r2) {
        var i2, c2, l2, f2, h2, d2, p2, y2 = n2.length;
        if (!n2.every(function(e3) {
          return "string" == typeof e3;
        })) return jt(e2, Ze);
        function t2(e3) {
          i2 = "next" === e3 ? function(e4) {
            return e4.toUpperCase();
          } : function(e4) {
            return e4.toLowerCase();
          }, c2 = "next" === e3 ? function(e4) {
            return e4.toLowerCase();
          } : function(e4) {
            return e4.toUpperCase();
          }, l2 = "next" === e3 ? Et : St;
          var t3 = n2.map(function(e4) {
            return { lower: c2(e4), upper: i2(e4) };
          }).sort(function(e4, t4) {
            return l2(e4.lower, t4.lower);
          });
          f2 = t3.map(function(e4) {
            return e4.upper;
          }), h2 = t3.map(function(e4) {
            return e4.lower;
          }), p2 = "next" === (d2 = e3) ? "" : r2;
        }
        t2("next");
        e2 = new e2.Collection(e2, function() {
          return Tt(f2[0], h2[y2 - 1] + r2);
        });
        e2._ondirectionchange = function(e3) {
          t2(e3);
        };
        var v2 = 0;
        return e2._addAlgorithm(function(e3, t3, n3) {
          var r3 = e3.key;
          if ("string" != typeof r3) return false;
          var i3 = c2(r3);
          if (s2(i3, h2, v2)) return true;
          for (var o2 = null, a2 = v2; a2 < y2; ++a2) {
            var u2 = (function(e4, t4, n4, r4, i4, o3) {
              for (var a3 = Math.min(e4.length, r4.length), u3 = -1, s3 = 0; s3 < a3; ++s3) {
                var c3 = t4[s3];
                if (c3 !== r4[s3]) return i4(e4[s3], n4[s3]) < 0 ? e4.substr(0, s3) + n4[s3] + n4.substr(s3 + 1) : i4(e4[s3], r4[s3]) < 0 ? e4.substr(0, s3) + r4[s3] + n4.substr(s3 + 1) : 0 <= u3 ? e4.substr(0, u3) + t4[u3] + n4.substr(u3 + 1) : null;
                i4(e4[s3], c3) < 0 && (u3 = s3);
              }
              return a3 < r4.length && "next" === o3 ? e4 + n4.substr(e4.length) : a3 < e4.length && "prev" === o3 ? e4.substr(0, n4.length) : u3 < 0 ? null : e4.substr(0, u3) + r4[u3] + n4.substr(u3 + 1);
            })(r3, i3, f2[a2], h2[a2], l2, d2);
            null === u2 && null === o2 ? v2 = a2 + 1 : (null === o2 || 0 < l2(o2, u2)) && (o2 = u2);
          }
          return t3(null !== o2 ? function() {
            e3.continue(o2 + p2);
          } : n3), false;
        }), e2;
      }
      function Tt(e2, t2, n2, r2) {
        return { type: 2, lower: e2, upper: t2, lowerOpen: n2, upperOpen: r2 };
      }
      function qt(e2) {
        return { type: 1, lower: e2, upper: e2 };
      }
      var Dt = (Object.defineProperty(It.prototype, "Collection", { get: function() {
        return this._ctx.table.db.Collection;
      }, enumerable: false, configurable: true }), It.prototype.between = function(e2, t2, n2, r2) {
        n2 = false !== n2, r2 = true === r2;
        try {
          return 0 < this._cmp(e2, t2) || 0 === this._cmp(e2, t2) && (n2 || r2) && (!n2 || !r2) ? At(this) : new this.Collection(this, function() {
            return Tt(e2, t2, !n2, !r2);
          });
        } catch (e3) {
          return jt(this, Je);
        }
      }, It.prototype.equals = function(e2) {
        return null == e2 ? jt(this, Je) : new this.Collection(this, function() {
          return qt(e2);
        });
      }, It.prototype.above = function(e2) {
        return null == e2 ? jt(this, Je) : new this.Collection(this, function() {
          return Tt(e2, void 0, true);
        });
      }, It.prototype.aboveOrEqual = function(e2) {
        return null == e2 ? jt(this, Je) : new this.Collection(this, function() {
          return Tt(e2, void 0, false);
        });
      }, It.prototype.below = function(e2) {
        return null == e2 ? jt(this, Je) : new this.Collection(this, function() {
          return Tt(void 0, e2, false, true);
        });
      }, It.prototype.belowOrEqual = function(e2) {
        return null == e2 ? jt(this, Je) : new this.Collection(this, function() {
          return Tt(void 0, e2);
        });
      }, It.prototype.startsWith = function(e2) {
        return "string" != typeof e2 ? jt(this, Ze) : this.between(e2, e2 + He, true, true);
      }, It.prototype.startsWithIgnoreCase = function(e2) {
        return "" === e2 ? this.startsWith(e2) : Ct(this, function(e3, t2) {
          return 0 === e3.indexOf(t2[0]);
        }, [e2], He);
      }, It.prototype.equalsIgnoreCase = function(e2) {
        return Ct(this, function(e3, t2) {
          return e3 === t2[0];
        }, [e2], "");
      }, It.prototype.anyOfIgnoreCase = function() {
        var e2 = I.apply(D, arguments);
        return 0 === e2.length ? At(this) : Ct(this, function(e3, t2) {
          return -1 !== t2.indexOf(e3);
        }, e2, "");
      }, It.prototype.startsWithAnyOfIgnoreCase = function() {
        var e2 = I.apply(D, arguments);
        return 0 === e2.length ? At(this) : Ct(this, function(t2, e3) {
          return e3.some(function(e4) {
            return 0 === t2.indexOf(e4);
          });
        }, e2, He);
      }, It.prototype.anyOf = function() {
        var t2 = this, i2 = I.apply(D, arguments), o2 = this._cmp;
        try {
          i2.sort(o2);
        } catch (e3) {
          return jt(this, Je);
        }
        if (0 === i2.length) return At(this);
        var e2 = new this.Collection(this, function() {
          return Tt(i2[0], i2[i2.length - 1]);
        });
        e2._ondirectionchange = function(e3) {
          o2 = "next" === e3 ? t2._ascending : t2._descending, i2.sort(o2);
        };
        var a2 = 0;
        return e2._addAlgorithm(function(e3, t3, n2) {
          for (var r2 = e3.key; 0 < o2(r2, i2[a2]); ) if (++a2 === i2.length) return t3(n2), false;
          return 0 === o2(r2, i2[a2]) || (t3(function() {
            e3.continue(i2[a2]);
          }), false);
        }), e2;
      }, It.prototype.notEqual = function(e2) {
        return this.inAnyRange([[-1 / 0, e2], [e2, this.db._maxKey]], { includeLowers: false, includeUppers: false });
      }, It.prototype.noneOf = function() {
        var e2 = I.apply(D, arguments);
        if (0 === e2.length) return new this.Collection(this);
        try {
          e2.sort(this._ascending);
        } catch (e3) {
          return jt(this, Je);
        }
        var t2 = e2.reduce(function(e3, t3) {
          return e3 ? e3.concat([[e3[e3.length - 1][1], t3]]) : [[-1 / 0, t3]];
        }, null);
        return t2.push([e2[e2.length - 1], this.db._maxKey]), this.inAnyRange(t2, { includeLowers: false, includeUppers: false });
      }, It.prototype.inAnyRange = function(e2, t2) {
        var o2 = this, a2 = this._cmp, u2 = this._ascending, n2 = this._descending, s2 = this._min, c2 = this._max;
        if (0 === e2.length) return At(this);
        if (!e2.every(function(e3) {
          return void 0 !== e3[0] && void 0 !== e3[1] && u2(e3[0], e3[1]) <= 0;
        })) return jt(this, "First argument to inAnyRange() must be an Array of two-value Arrays [lower,upper] where upper must not be lower than lower", Y.InvalidArgument);
        var r2 = !t2 || false !== t2.includeLowers, i2 = t2 && true === t2.includeUppers;
        var l2, f2 = u2;
        function h2(e3, t3) {
          return f2(e3[0], t3[0]);
        }
        try {
          (l2 = e2.reduce(function(e3, t3) {
            for (var n3 = 0, r3 = e3.length; n3 < r3; ++n3) {
              var i3 = e3[n3];
              if (a2(t3[0], i3[1]) < 0 && 0 < a2(t3[1], i3[0])) {
                i3[0] = s2(i3[0], t3[0]), i3[1] = c2(i3[1], t3[1]);
                break;
              }
            }
            return n3 === r3 && e3.push(t3), e3;
          }, [])).sort(h2);
        } catch (e3) {
          return jt(this, Je);
        }
        var d2 = 0, p2 = i2 ? function(e3) {
          return 0 < u2(e3, l2[d2][1]);
        } : function(e3) {
          return 0 <= u2(e3, l2[d2][1]);
        }, y2 = r2 ? function(e3) {
          return 0 < n2(e3, l2[d2][0]);
        } : function(e3) {
          return 0 <= n2(e3, l2[d2][0]);
        };
        var v2 = p2, e2 = new this.Collection(this, function() {
          return Tt(l2[0][0], l2[l2.length - 1][1], !r2, !i2);
        });
        return e2._ondirectionchange = function(e3) {
          f2 = "next" === e3 ? (v2 = p2, u2) : (v2 = y2, n2), l2.sort(h2);
        }, e2._addAlgorithm(function(e3, t3, n3) {
          for (var r3, i3 = e3.key; v2(i3); ) if (++d2 === l2.length) return t3(n3), false;
          return !p2(r3 = i3) && !y2(r3) || (0 === o2._cmp(i3, l2[d2][1]) || 0 === o2._cmp(i3, l2[d2][0]) || t3(function() {
            f2 === u2 ? e3.continue(l2[d2][0]) : e3.continue(l2[d2][1]);
          }), false);
        }), e2;
      }, It.prototype.startsWithAnyOf = function() {
        var e2 = I.apply(D, arguments);
        return e2.every(function(e3) {
          return "string" == typeof e3;
        }) ? 0 === e2.length ? At(this) : this.inAnyRange(e2.map(function(e3) {
          return [e3, e3 + He];
        })) : jt(this, "startsWithAnyOf() only works with strings");
      }, It);
      function It() {
      }
      function Bt(t2) {
        return qe(function(e2) {
          return Rt(e2), t2(e2.target.error), false;
        });
      }
      function Rt(e2) {
        e2.stopPropagation && e2.stopPropagation(), e2.preventDefault && e2.preventDefault();
      }
      var Ft = "storagemutated", Mt = "x-storagemutated-1", Nt = dt(null, Ft), Lt = (Ut.prototype._lock = function() {
        return y(!me.global), ++this._reculock, 1 !== this._reculock || me.global || (me.lockOwnerFor = this), this;
      }, Ut.prototype._unlock = function() {
        if (y(!me.global), 0 == --this._reculock) for (me.global || (me.lockOwnerFor = null); 0 < this._blockedFuncs.length && !this._locked(); ) {
          var e2 = this._blockedFuncs.shift();
          try {
            $e(e2[1], e2[0]);
          } catch (e3) {
          }
        }
        return this;
      }, Ut.prototype._locked = function() {
        return this._reculock && me.lockOwnerFor !== this;
      }, Ut.prototype.create = function(t2) {
        var n2 = this;
        if (!this.mode) return this;
        var e2 = this.db.idbdb, r2 = this.db._state.dbOpenError;
        if (y(!this.idbtrans), !t2 && !e2) switch (r2 && r2.name) {
          case "DatabaseClosedError":
            throw new Y.DatabaseClosed(r2);
          case "MissingAPIError":
            throw new Y.MissingAPI(r2.message, r2);
          default:
            throw new Y.OpenFailed(r2);
        }
        if (!this.active) throw new Y.TransactionInactive();
        return y(null === this._completion._state), (t2 = this.idbtrans = t2 || (this.db.core || e2).transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability })).onerror = qe(function(e3) {
          Rt(e3), n2._reject(t2.error);
        }), t2.onabort = qe(function(e3) {
          Rt(e3), n2.active && n2._reject(new Y.Abort(t2.error)), n2.active = false, n2.on("abort").fire(e3);
        }), t2.oncomplete = qe(function() {
          n2.active = false, n2._resolve(), "mutatedParts" in t2 && Nt.storagemutated.fire(t2.mutatedParts);
        }), this;
      }, Ut.prototype._promise = function(n2, r2, i2) {
        var o2 = this;
        if ("readwrite" === n2 && "readwrite" !== this.mode) return Xe(new Y.ReadOnly("Transaction is readonly"));
        if (!this.active) return Xe(new Y.TransactionInactive());
        if (this._locked()) return new _e(function(e3, t2) {
          o2._blockedFuncs.push([function() {
            o2._promise(n2, r2, i2).then(e3, t2);
          }, me]);
        });
        if (i2) return Ne(function() {
          var e3 = new _e(function(e4, t2) {
            o2._lock();
            var n3 = r2(e4, t2, o2);
            n3 && n3.then && n3.then(e4, t2);
          });
          return e3.finally(function() {
            return o2._unlock();
          }), e3._lib = true, e3;
        });
        var e2 = new _e(function(e3, t2) {
          var n3 = r2(e3, t2, o2);
          n3 && n3.then && n3.then(e3, t2);
        });
        return e2._lib = true, e2;
      }, Ut.prototype._root = function() {
        return this.parent ? this.parent._root() : this;
      }, Ut.prototype.waitFor = function(e2) {
        var t2, r2 = this._root(), i2 = _e.resolve(e2);
        r2._waitingFor ? r2._waitingFor = r2._waitingFor.then(function() {
          return i2;
        }) : (r2._waitingFor = i2, r2._waitingQueue = [], t2 = r2.idbtrans.objectStore(r2.storeNames[0]), (function e3() {
          for (++r2._spinCount; r2._waitingQueue.length; ) r2._waitingQueue.shift()();
          r2._waitingFor && (t2.get(-1 / 0).onsuccess = e3);
        })());
        var o2 = r2._waitingFor;
        return new _e(function(t3, n2) {
          i2.then(function(e3) {
            return r2._waitingQueue.push(qe(t3.bind(null, e3)));
          }, function(e3) {
            return r2._waitingQueue.push(qe(n2.bind(null, e3)));
          }).finally(function() {
            r2._waitingFor === o2 && (r2._waitingFor = null);
          });
        });
      }, Ut.prototype.abort = function() {
        this.active && (this.active = false, this.idbtrans && this.idbtrans.abort(), this._reject(new Y.Abort()));
      }, Ut.prototype.table = function(e2) {
        var t2 = this._memoizedTables || (this._memoizedTables = {});
        if (m(t2, e2)) return t2[e2];
        var n2 = this.schema[e2];
        if (!n2) throw new Y.NotFound("Table " + e2 + " not part of transaction");
        n2 = new this.db.Table(e2, n2, this);
        return n2.core = this.db.core.table(e2), t2[e2] = n2;
      }, Ut);
      function Ut() {
      }
      function Vt(e2, t2, n2, r2, i2, o2, a2) {
        return { name: e2, keyPath: t2, unique: n2, multi: r2, auto: i2, compound: o2, src: (n2 && !a2 ? "&" : "") + (r2 ? "*" : "") + (i2 ? "++" : "") + zt(t2) };
      }
      function zt(e2) {
        return "string" == typeof e2 ? e2 : e2 ? "[" + [].join.call(e2, "+") + "]" : "";
      }
      function Wt(e2, t2, n2) {
        return { name: e2, primKey: t2, indexes: n2, mappedClass: null, idxByName: (r2 = function(e3) {
          return [e3.name, e3];
        }, n2.reduce(function(e3, t3, n3) {
          n3 = r2(t3, n3);
          return n3 && (e3[n3[0]] = n3[1]), e3;
        }, {})) };
        var r2;
      }
      var Yt = function(e2) {
        try {
          return e2.only([[]]), Yt = function() {
            return [[]];
          }, [[]];
        } catch (e3) {
          return Yt = function() {
            return He;
          }, He;
        }
      };
      function $t(t2) {
        return null == t2 ? function() {
        } : "string" == typeof t2 ? 1 === (n2 = t2).split(".").length ? function(e2) {
          return e2[n2];
        } : function(e2) {
          return O(e2, n2);
        } : function(e2) {
          return O(e2, t2);
        };
        var n2;
      }
      function Qt(e2) {
        return [].slice.call(e2);
      }
      var Gt = 0;
      function Xt(e2) {
        return null == e2 ? ":id" : "string" == typeof e2 ? e2 : "[".concat(e2.join("+"), "]");
      }
      function Ht(e2, i2, t2) {
        function _2(e3) {
          if (3 === e3.type) return null;
          if (4 === e3.type) throw new Error("Cannot convert never type to IDBKeyRange");
          var t3 = e3.lower, n3 = e3.upper, r3 = e3.lowerOpen, e3 = e3.upperOpen;
          return void 0 === t3 ? void 0 === n3 ? null : i2.upperBound(n3, !!e3) : void 0 === n3 ? i2.lowerBound(t3, !!r3) : i2.bound(t3, n3, !!r3, !!e3);
        }
        function n2(e3) {
          var h2, w2 = e3.name;
          return { name: w2, schema: e3, mutate: function(e4) {
            var y2 = e4.trans, v2 = e4.type, m2 = e4.keys, b2 = e4.values, g2 = e4.range;
            return new Promise(function(t3, e5) {
              t3 = qe(t3);
              var n3 = y2.objectStore(w2), r3 = null == n3.keyPath, i3 = "put" === v2 || "add" === v2;
              if (!i3 && "delete" !== v2 && "deleteRange" !== v2) throw new Error("Invalid operation type: " + v2);
              var o3, a3 = (m2 || b2 || { length: 1 }).length;
              if (m2 && b2 && m2.length !== b2.length) throw new Error("Given keys array must have same length as given values array.");
              if (0 === a3) return t3({ numFailures: 0, failures: {}, results: [], lastResult: void 0 });
              function u3(e6) {
                ++l2, Rt(e6);
              }
              var s3 = [], c3 = [], l2 = 0;
              if ("deleteRange" === v2) {
                if (4 === g2.type) return t3({ numFailures: l2, failures: c3, results: [], lastResult: void 0 });
                3 === g2.type ? s3.push(o3 = n3.clear()) : s3.push(o3 = n3.delete(_2(g2)));
              } else {
                var r3 = i3 ? r3 ? [b2, m2] : [b2, null] : [m2, null], f2 = r3[0], h3 = r3[1];
                if (i3) for (var d2 = 0; d2 < a3; ++d2) s3.push(o3 = h3 && void 0 !== h3[d2] ? n3[v2](f2[d2], h3[d2]) : n3[v2](f2[d2])), o3.onerror = u3;
                else for (d2 = 0; d2 < a3; ++d2) s3.push(o3 = n3[v2](f2[d2])), o3.onerror = u3;
              }
              function p2(e6) {
                e6 = e6.target.result, s3.forEach(function(e7, t4) {
                  return null != e7.error && (c3[t4] = e7.error);
                }), t3({ numFailures: l2, failures: c3, results: "delete" === v2 ? m2 : s3.map(function(e7) {
                  return e7.result;
                }), lastResult: e6 });
              }
              o3.onerror = function(e6) {
                u3(e6), p2(e6);
              }, o3.onsuccess = p2;
            });
          }, getMany: function(e4) {
            var f2 = e4.trans, h3 = e4.keys;
            return new Promise(function(t3, e5) {
              t3 = qe(t3);
              for (var n3, r3 = f2.objectStore(w2), i3 = h3.length, o3 = new Array(i3), a3 = 0, u3 = 0, s3 = function(e6) {
                e6 = e6.target;
                o3[e6._pos] = e6.result, ++u3 === a3 && t3(o3);
              }, c3 = Bt(e5), l2 = 0; l2 < i3; ++l2) null != h3[l2] && ((n3 = r3.get(h3[l2]))._pos = l2, n3.onsuccess = s3, n3.onerror = c3, ++a3);
              0 === a3 && t3(o3);
            });
          }, get: function(e4) {
            var r3 = e4.trans, i3 = e4.key;
            return new Promise(function(t3, e5) {
              t3 = qe(t3);
              var n3 = r3.objectStore(w2).get(i3);
              n3.onsuccess = function(e6) {
                return t3(e6.target.result);
              }, n3.onerror = Bt(e5);
            });
          }, query: (h2 = s2, function(f2) {
            return new Promise(function(n3, e4) {
              n3 = qe(n3);
              var r3, i3, o3, t3 = f2.trans, a3 = f2.values, u3 = f2.limit, s3 = f2.query, c3 = u3 === 1 / 0 ? void 0 : u3, l2 = s3.index, s3 = s3.range, t3 = t3.objectStore(w2), l2 = l2.isPrimaryKey ? t3 : t3.index(l2.name), s3 = _2(s3);
              if (0 === u3) return n3({ result: [] });
              h2 ? ((c3 = a3 ? l2.getAll(s3, c3) : l2.getAllKeys(s3, c3)).onsuccess = function(e5) {
                return n3({ result: e5.target.result });
              }, c3.onerror = Bt(e4)) : (r3 = 0, i3 = !a3 && "openKeyCursor" in l2 ? l2.openKeyCursor(s3) : l2.openCursor(s3), o3 = [], i3.onsuccess = function(e5) {
                var t4 = i3.result;
                return t4 ? (o3.push(a3 ? t4.value : t4.primaryKey), ++r3 === u3 ? n3({ result: o3 }) : void t4.continue()) : n3({ result: o3 });
              }, i3.onerror = Bt(e4));
            });
          }), openCursor: function(e4) {
            var c3 = e4.trans, o3 = e4.values, a3 = e4.query, u3 = e4.reverse, l2 = e4.unique;
            return new Promise(function(t3, n3) {
              t3 = qe(t3);
              var e5 = a3.index, r3 = a3.range, i3 = c3.objectStore(w2), i3 = e5.isPrimaryKey ? i3 : i3.index(e5.name), e5 = u3 ? l2 ? "prevunique" : "prev" : l2 ? "nextunique" : "next", s3 = !o3 && "openKeyCursor" in i3 ? i3.openKeyCursor(_2(r3), e5) : i3.openCursor(_2(r3), e5);
              s3.onerror = Bt(n3), s3.onsuccess = qe(function(e6) {
                var r4, i4, o4, a4, u4 = s3.result;
                u4 ? (u4.___id = ++Gt, u4.done = false, r4 = u4.continue.bind(u4), i4 = (i4 = u4.continuePrimaryKey) && i4.bind(u4), o4 = u4.advance.bind(u4), a4 = function() {
                  throw new Error("Cursor not stopped");
                }, u4.trans = c3, u4.stop = u4.continue = u4.continuePrimaryKey = u4.advance = function() {
                  throw new Error("Cursor not started");
                }, u4.fail = qe(n3), u4.next = function() {
                  var e7 = this, t4 = 1;
                  return this.start(function() {
                    return t4-- ? e7.continue() : e7.stop();
                  }).then(function() {
                    return e7;
                  });
                }, u4.start = function(e7) {
                  function t4() {
                    if (s3.result) try {
                      e7();
                    } catch (e8) {
                      u4.fail(e8);
                    }
                    else u4.done = true, u4.start = function() {
                      throw new Error("Cursor behind last entry");
                    }, u4.stop();
                  }
                  var n4 = new Promise(function(t5, e8) {
                    t5 = qe(t5), s3.onerror = Bt(e8), u4.fail = e8, u4.stop = function(e9) {
                      u4.stop = u4.continue = u4.continuePrimaryKey = u4.advance = a4, t5(e9);
                    };
                  });
                  return s3.onsuccess = qe(function(e8) {
                    s3.onsuccess = t4, t4();
                  }), u4.continue = r4, u4.continuePrimaryKey = i4, u4.advance = o4, t4(), n4;
                }, t3(u4)) : t3(null);
              }, n3);
            });
          }, count: function(e4) {
            var t3 = e4.query, i3 = e4.trans, o3 = t3.index, a3 = t3.range;
            return new Promise(function(t4, e5) {
              var n3 = i3.objectStore(w2), r3 = o3.isPrimaryKey ? n3 : n3.index(o3.name), n3 = _2(a3), r3 = n3 ? r3.count(n3) : r3.count();
              r3.onsuccess = qe(function(e6) {
                return t4(e6.target.result);
              }), r3.onerror = Bt(e5);
            });
          } };
        }
        var r2, o2, a2, u2 = (o2 = t2, a2 = Qt((r2 = e2).objectStoreNames), { schema: { name: r2.name, tables: a2.map(function(e3) {
          return o2.objectStore(e3);
        }).map(function(t3) {
          var e3 = t3.keyPath, n3 = t3.autoIncrement, r3 = k(e3), i3 = {}, n3 = { name: t3.name, primaryKey: { name: null, isPrimaryKey: true, outbound: null == e3, compound: r3, keyPath: e3, autoIncrement: n3, unique: true, extractKey: $t(e3) }, indexes: Qt(t3.indexNames).map(function(e4) {
            return t3.index(e4);
          }).map(function(e4) {
            var t4 = e4.name, n4 = e4.unique, r4 = e4.multiEntry, e4 = e4.keyPath, r4 = { name: t4, compound: k(e4), keyPath: e4, unique: n4, multiEntry: r4, extractKey: $t(e4) };
            return i3[Xt(e4)] = r4;
          }), getIndexByKeyPath: function(e4) {
            return i3[Xt(e4)];
          } };
          return i3[":id"] = n3.primaryKey, null != e3 && (i3[Xt(e3)] = n3.primaryKey), n3;
        }) }, hasGetAll: 0 < a2.length && "getAll" in o2.objectStore(a2[0]) && !("undefined" != typeof navigator && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604) }), t2 = u2.schema, s2 = u2.hasGetAll, u2 = t2.tables.map(n2), c2 = {};
        return u2.forEach(function(e3) {
          return c2[e3.name] = e3;
        }), { stack: "dbcore", transaction: e2.transaction.bind(e2), table: function(e3) {
          if (!c2[e3]) throw new Error("Table '".concat(e3, "' not found"));
          return c2[e3];
        }, MIN_KEY: -1 / 0, MAX_KEY: Yt(i2), schema: t2 };
      }
      function Jt(e2, t2, n2, r2) {
        var i2 = n2.IDBKeyRange;
        return n2.indexedDB, { dbcore: (r2 = Ht(t2, i2, r2), e2.dbcore.reduce(function(e3, t3) {
          t3 = t3.create;
          return _(_({}, e3), t3(e3));
        }, r2)) };
      }
      function Zt(n2, e2) {
        var t2 = e2.db, e2 = Jt(n2._middlewares, t2, n2._deps, e2);
        n2.core = e2.dbcore, n2.tables.forEach(function(e3) {
          var t3 = e3.name;
          n2.core.schema.tables.some(function(e4) {
            return e4.name === t3;
          }) && (e3.core = n2.core.table(t3), n2[t3] instanceof n2.Table && (n2[t3].core = e3.core));
        });
      }
      function en(i2, e2, t2, o2) {
        t2.forEach(function(n2) {
          var r2 = o2[n2];
          e2.forEach(function(e3) {
            var t3 = (function e4(t4, n3) {
              return h(t4, n3) || (t4 = c(t4)) && e4(t4, n3);
            })(e3, n2);
            (!t3 || "value" in t3 && void 0 === t3.value) && (e3 === i2.Transaction.prototype || e3 instanceof i2.Transaction ? l(e3, n2, { get: function() {
              return this.table(n2);
            }, set: function(e4) {
              u(this, n2, { value: e4, writable: true, configurable: true, enumerable: true });
            } }) : e3[n2] = new i2.Table(n2, r2));
          });
        });
      }
      function tn(n2, e2) {
        e2.forEach(function(e3) {
          for (var t2 in e3) e3[t2] instanceof n2.Table && delete e3[t2];
        });
      }
      function nn(e2, t2) {
        return e2._cfg.version - t2._cfg.version;
      }
      function rn(n2, r2, i2, e2) {
        var o2 = n2._dbSchema;
        i2.objectStoreNames.contains("$meta") && !o2.$meta && (o2.$meta = Wt("$meta", hn("")[0], []), n2._storeNames.push("$meta"));
        var a2 = n2._createTransaction("readwrite", n2._storeNames, o2);
        a2.create(i2), a2._completion.catch(e2);
        var u2 = a2._reject.bind(a2), s2 = me.transless || me;
        Ne(function() {
          return me.trans = a2, me.transless = s2, 0 !== r2 ? (Zt(n2, i2), t2 = r2, ((e3 = a2).storeNames.includes("$meta") ? e3.table("$meta").get("version").then(function(e4) {
            return null != e4 ? e4 : t2;
          }) : _e.resolve(t2)).then(function(e4) {
            return c2 = e4, l2 = a2, f2 = i2, t3 = [], e4 = (s3 = n2)._versions, h2 = s3._dbSchema = ln(0, s3.idbdb, f2), 0 !== (e4 = e4.filter(function(e5) {
              return e5._cfg.version >= c2;
            })).length ? (e4.forEach(function(u3) {
              t3.push(function() {
                var t4 = h2, e5 = u3._cfg.dbschema;
                fn(s3, t4, f2), fn(s3, e5, f2), h2 = s3._dbSchema = e5;
                var n3 = an(t4, e5);
                n3.add.forEach(function(e6) {
                  un(f2, e6[0], e6[1].primKey, e6[1].indexes);
                }), n3.change.forEach(function(e6) {
                  if (e6.recreate) throw new Y.Upgrade("Not yet support for changing primary key");
                  var t5 = f2.objectStore(e6.name);
                  e6.add.forEach(function(e7) {
                    return cn(t5, e7);
                  }), e6.change.forEach(function(e7) {
                    t5.deleteIndex(e7.name), cn(t5, e7);
                  }), e6.del.forEach(function(e7) {
                    return t5.deleteIndex(e7);
                  });
                });
                var r3 = u3._cfg.contentUpgrade;
                if (r3 && u3._cfg.version > c2) {
                  Zt(s3, f2), l2._memoizedTables = {};
                  var i3 = g(e5);
                  n3.del.forEach(function(e6) {
                    i3[e6] = t4[e6];
                  }), tn(s3, [s3.Transaction.prototype]), en(s3, [s3.Transaction.prototype], x(i3), i3), l2.schema = i3;
                  var o3, a3 = B(r3);
                  a3 && Le();
                  n3 = _e.follow(function() {
                    var e6;
                    (o3 = r3(l2)) && a3 && (e6 = Ue.bind(null, null), o3.then(e6, e6));
                  });
                  return o3 && "function" == typeof o3.then ? _e.resolve(o3) : n3.then(function() {
                    return o3;
                  });
                }
              }), t3.push(function(e5) {
                var t4, n3, r3 = u3._cfg.dbschema;
                t4 = r3, n3 = e5, [].slice.call(n3.db.objectStoreNames).forEach(function(e6) {
                  return null == t4[e6] && n3.db.deleteObjectStore(e6);
                }), tn(s3, [s3.Transaction.prototype]), en(s3, [s3.Transaction.prototype], s3._storeNames, s3._dbSchema), l2.schema = s3._dbSchema;
              }), t3.push(function(e5) {
                s3.idbdb.objectStoreNames.contains("$meta") && (Math.ceil(s3.idbdb.version / 10) === u3._cfg.version ? (s3.idbdb.deleteObjectStore("$meta"), delete s3._dbSchema.$meta, s3._storeNames = s3._storeNames.filter(function(e6) {
                  return "$meta" !== e6;
                })) : e5.objectStore("$meta").put(u3._cfg.version, "version"));
              });
            }), (function e5() {
              return t3.length ? _e.resolve(t3.shift()(l2.idbtrans)).then(e5) : _e.resolve();
            })().then(function() {
              sn(h2, f2);
            })) : _e.resolve();
            var s3, c2, l2, f2, t3, h2;
          }).catch(u2)) : (x(o2).forEach(function(e4) {
            un(i2, e4, o2[e4].primKey, o2[e4].indexes);
          }), Zt(n2, i2), void _e.follow(function() {
            return n2.on.populate.fire(a2);
          }).catch(u2));
          var e3, t2;
        });
      }
      function on(e2, r2) {
        sn(e2._dbSchema, r2), r2.db.version % 10 != 0 || r2.objectStoreNames.contains("$meta") || r2.db.createObjectStore("$meta").add(Math.ceil(r2.db.version / 10 - 1), "version");
        var t2 = ln(0, e2.idbdb, r2);
        fn(e2, e2._dbSchema, r2);
        for (var n2 = 0, i2 = an(t2, e2._dbSchema).change; n2 < i2.length; n2++) {
          var o2 = (function(t3) {
            if (t3.change.length || t3.recreate) return console.warn("Unable to patch indexes of table ".concat(t3.name, " because it has changes on the type of index or primary key.")), { value: void 0 };
            var n3 = r2.objectStore(t3.name);
            t3.add.forEach(function(e3) {
              ie && console.debug("Dexie upgrade patch: Creating missing index ".concat(t3.name, ".").concat(e3.src)), cn(n3, e3);
            });
          })(i2[n2]);
          if ("object" == typeof o2) return o2.value;
        }
      }
      function an(e2, t2) {
        var n2, r2 = { del: [], add: [], change: [] };
        for (n2 in e2) t2[n2] || r2.del.push(n2);
        for (n2 in t2) {
          var i2 = e2[n2], o2 = t2[n2];
          if (i2) {
            var a2 = { name: n2, def: o2, recreate: false, del: [], add: [], change: [] };
            if ("" + (i2.primKey.keyPath || "") != "" + (o2.primKey.keyPath || "") || i2.primKey.auto !== o2.primKey.auto) a2.recreate = true, r2.change.push(a2);
            else {
              var u2 = i2.idxByName, s2 = o2.idxByName, c2 = void 0;
              for (c2 in u2) s2[c2] || a2.del.push(c2);
              for (c2 in s2) {
                var l2 = u2[c2], f2 = s2[c2];
                l2 ? l2.src !== f2.src && a2.change.push(f2) : a2.add.push(f2);
              }
              (0 < a2.del.length || 0 < a2.add.length || 0 < a2.change.length) && r2.change.push(a2);
            }
          } else r2.add.push([n2, o2]);
        }
        return r2;
      }
      function un(e2, t2, n2, r2) {
        var i2 = e2.db.createObjectStore(t2, n2.keyPath ? { keyPath: n2.keyPath, autoIncrement: n2.auto } : { autoIncrement: n2.auto });
        return r2.forEach(function(e3) {
          return cn(i2, e3);
        }), i2;
      }
      function sn(t2, n2) {
        x(t2).forEach(function(e2) {
          n2.db.objectStoreNames.contains(e2) || (ie && console.debug("Dexie: Creating missing table", e2), un(n2, e2, t2[e2].primKey, t2[e2].indexes));
        });
      }
      function cn(e2, t2) {
        e2.createIndex(t2.name, t2.keyPath, { unique: t2.unique, multiEntry: t2.multi });
      }
      function ln(e2, t2, u2) {
        var s2 = {};
        return b(t2.objectStoreNames, 0).forEach(function(e3) {
          for (var t3 = u2.objectStore(e3), n2 = Vt(zt(a2 = t3.keyPath), a2 || "", true, false, !!t3.autoIncrement, a2 && "string" != typeof a2, true), r2 = [], i2 = 0; i2 < t3.indexNames.length; ++i2) {
            var o2 = t3.index(t3.indexNames[i2]), a2 = o2.keyPath, o2 = Vt(o2.name, a2, !!o2.unique, !!o2.multiEntry, false, a2 && "string" != typeof a2, false);
            r2.push(o2);
          }
          s2[e3] = Wt(e3, n2, r2);
        }), s2;
      }
      function fn(e2, t2, n2) {
        for (var r2 = n2.db.objectStoreNames, i2 = 0; i2 < r2.length; ++i2) {
          var o2 = r2[i2], a2 = n2.objectStore(o2);
          e2._hasGetAll = "getAll" in a2;
          for (var u2 = 0; u2 < a2.indexNames.length; ++u2) {
            var s2 = a2.indexNames[u2], c2 = a2.index(s2).keyPath, l2 = "string" == typeof c2 ? c2 : "[" + b(c2).join("+") + "]";
            !t2[o2] || (c2 = t2[o2].idxByName[l2]) && (c2.name = s2, delete t2[o2].idxByName[l2], t2[o2].idxByName[s2] = c2);
          }
        }
        "undefined" != typeof navigator && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && f.WorkerGlobalScope && f instanceof f.WorkerGlobalScope && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604 && (e2._hasGetAll = false);
      }
      function hn(e2) {
        return e2.split(",").map(function(e3, t2) {
          var n2 = (e3 = e3.trim()).replace(/([&*]|\+\+)/g, ""), r2 = /^\[/.test(n2) ? n2.match(/^\[(.*)\]$/)[1].split("+") : n2;
          return Vt(n2, r2 || null, /\&/.test(e3), /\*/.test(e3), /\+\+/.test(e3), k(r2), 0 === t2);
        });
      }
      var dn = (pn.prototype._parseStoresSpec = function(r2, i2) {
        x(r2).forEach(function(e2) {
          if (null !== r2[e2]) {
            var t2 = hn(r2[e2]), n2 = t2.shift();
            if (n2.unique = true, n2.multi) throw new Y.Schema("Primary key cannot be multi-valued");
            t2.forEach(function(e3) {
              if (e3.auto) throw new Y.Schema("Only primary key can be marked as autoIncrement (++)");
              if (!e3.keyPath) throw new Y.Schema("Index must have a name and cannot be an empty string");
            }), i2[e2] = Wt(e2, n2, t2);
          }
        });
      }, pn.prototype.stores = function(e2) {
        var t2 = this.db;
        this._cfg.storesSource = this._cfg.storesSource ? a(this._cfg.storesSource, e2) : e2;
        var e2 = t2._versions, n2 = {}, r2 = {};
        return e2.forEach(function(e3) {
          a(n2, e3._cfg.storesSource), r2 = e3._cfg.dbschema = {}, e3._parseStoresSpec(n2, r2);
        }), t2._dbSchema = r2, tn(t2, [t2._allTables, t2, t2.Transaction.prototype]), en(t2, [t2._allTables, t2, t2.Transaction.prototype, this._cfg.tables], x(r2), r2), t2._storeNames = x(r2), this;
      }, pn.prototype.upgrade = function(e2) {
        return this._cfg.contentUpgrade = re(this._cfg.contentUpgrade || G, e2), this;
      }, pn);
      function pn() {
      }
      function yn(e2, t2) {
        var n2 = e2._dbNamesDB;
        return n2 || (n2 = e2._dbNamesDB = new er(tt, { addons: [], indexedDB: e2, IDBKeyRange: t2 })).version(1).stores({ dbnames: "name" }), n2.table("dbnames");
      }
      function vn(e2) {
        return e2 && "function" == typeof e2.databases;
      }
      function mn(e2) {
        return Ne(function() {
          return me.letThrough = true, e2();
        });
      }
      function bn(e2) {
        return !("from" in e2);
      }
      var gn = function(e2, t2) {
        if (!this) {
          var n2 = new gn();
          return e2 && "d" in e2 && a(n2, e2), n2;
        }
        a(this, arguments.length ? { d: 1, from: e2, to: 1 < arguments.length ? t2 : e2 } : { d: 0 });
      };
      function wn(e2, t2, n2) {
        var r2 = st(t2, n2);
        if (!isNaN(r2)) {
          if (0 < r2) throw RangeError();
          if (bn(e2)) return a(e2, { from: t2, to: n2, d: 1 });
          var i2 = e2.l, r2 = e2.r;
          if (st(n2, e2.from) < 0) return i2 ? wn(i2, t2, n2) : e2.l = { from: t2, to: n2, d: 1, l: null, r: null }, On(e2);
          if (0 < st(t2, e2.to)) return r2 ? wn(r2, t2, n2) : e2.r = { from: t2, to: n2, d: 1, l: null, r: null }, On(e2);
          st(t2, e2.from) < 0 && (e2.from = t2, e2.l = null, e2.d = r2 ? r2.d + 1 : 1), 0 < st(n2, e2.to) && (e2.to = n2, e2.r = null, e2.d = e2.l ? e2.l.d + 1 : 1);
          n2 = !e2.r;
          i2 && !e2.l && _n(e2, i2), r2 && n2 && _n(e2, r2);
        }
      }
      function _n(e2, t2) {
        bn(t2) || (function e3(t3, n2) {
          var r2 = n2.from, i2 = n2.to, o2 = n2.l, n2 = n2.r;
          wn(t3, r2, i2), o2 && e3(t3, o2), n2 && e3(t3, n2);
        })(e2, t2);
      }
      function xn(e2, t2) {
        var n2 = kn(t2), r2 = n2.next();
        if (r2.done) return false;
        for (var i2 = r2.value, o2 = kn(e2), a2 = o2.next(i2.from), u2 = a2.value; !r2.done && !a2.done; ) {
          if (st(u2.from, i2.to) <= 0 && 0 <= st(u2.to, i2.from)) return true;
          st(i2.from, u2.from) < 0 ? i2 = (r2 = n2.next(u2.from)).value : u2 = (a2 = o2.next(i2.from)).value;
        }
        return false;
      }
      function kn(e2) {
        var n2 = bn(e2) ? null : { s: 0, n: e2 };
        return { next: function(e3) {
          for (var t2 = 0 < arguments.length; n2; ) switch (n2.s) {
            case 0:
              if (n2.s = 1, t2) for (; n2.n.l && st(e3, n2.n.from) < 0; ) n2 = { up: n2, n: n2.n.l, s: 1 };
              else for (; n2.n.l; ) n2 = { up: n2, n: n2.n.l, s: 1 };
            case 1:
              if (n2.s = 2, !t2 || st(e3, n2.n.to) <= 0) return { value: n2.n, done: false };
            case 2:
              if (n2.n.r) {
                n2.s = 3, n2 = { up: n2, n: n2.n.r, s: 0 };
                continue;
              }
            case 3:
              n2 = n2.up;
          }
          return { done: true };
        } };
      }
      function On(e2) {
        var t2, n2, r2 = ((null === (t2 = e2.r) || void 0 === t2 ? void 0 : t2.d) || 0) - ((null === (n2 = e2.l) || void 0 === n2 ? void 0 : n2.d) || 0), i2 = 1 < r2 ? "r" : r2 < -1 ? "l" : "";
        i2 && (t2 = "r" == i2 ? "l" : "r", n2 = _({}, e2), r2 = e2[i2], e2.from = r2.from, e2.to = r2.to, e2[i2] = r2[i2], n2[i2] = r2[t2], (e2[t2] = n2).d = Pn(n2)), e2.d = Pn(e2);
      }
      function Pn(e2) {
        var t2 = e2.r, e2 = e2.l;
        return (t2 ? e2 ? Math.max(t2.d, e2.d) : t2.d : e2 ? e2.d : 0) + 1;
      }
      function Kn(t2, n2) {
        return x(n2).forEach(function(e2) {
          t2[e2] ? _n(t2[e2], n2[e2]) : t2[e2] = (function e3(t3) {
            var n3, r2, i2 = {};
            for (n3 in t3) m(t3, n3) && (r2 = t3[n3], i2[n3] = !r2 || "object" != typeof r2 || K.has(r2.constructor) ? r2 : e3(r2));
            return i2;
          })(n2[e2]);
        }), t2;
      }
      function En(t2, n2) {
        return t2.all || n2.all || Object.keys(t2).some(function(e2) {
          return n2[e2] && xn(n2[e2], t2[e2]);
        });
      }
      r(gn.prototype, ((F = { add: function(e2) {
        return _n(this, e2), this;
      }, addKey: function(e2) {
        return wn(this, e2, e2), this;
      }, addKeys: function(e2) {
        var t2 = this;
        return e2.forEach(function(e3) {
          return wn(t2, e3, e3);
        }), this;
      }, hasKey: function(e2) {
        var t2 = kn(this).next(e2).value;
        return t2 && st(t2.from, e2) <= 0 && 0 <= st(t2.to, e2);
      } })[C] = function() {
        return kn(this);
      }, F));
      var Sn = {}, jn = {}, An = false;
      function Cn(e2) {
        Kn(jn, e2), An || (An = true, setTimeout(function() {
          An = false, Tn(jn, !(jn = {}));
        }, 0));
      }
      function Tn(e2, t2) {
        void 0 === t2 && (t2 = false);
        var n2 = /* @__PURE__ */ new Set();
        if (e2.all) for (var r2 = 0, i2 = Object.values(Sn); r2 < i2.length; r2++) qn(a2 = i2[r2], e2, n2, t2);
        else for (var o2 in e2) {
          var a2, u2 = /^idb\:\/\/(.*)\/(.*)\//.exec(o2);
          u2 && (o2 = u2[1], u2 = u2[2], (a2 = Sn["idb://".concat(o2, "/").concat(u2)]) && qn(a2, e2, n2, t2));
        }
        n2.forEach(function(e3) {
          return e3();
        });
      }
      function qn(e2, t2, n2, r2) {
        for (var i2 = [], o2 = 0, a2 = Object.entries(e2.queries.query); o2 < a2.length; o2++) {
          for (var u2 = a2[o2], s2 = u2[0], c2 = [], l2 = 0, f2 = u2[1]; l2 < f2.length; l2++) {
            var h2 = f2[l2];
            En(t2, h2.obsSet) ? h2.subscribers.forEach(function(e3) {
              return n2.add(e3);
            }) : r2 && c2.push(h2);
          }
          r2 && i2.push([s2, c2]);
        }
        if (r2) for (var d2 = 0, p2 = i2; d2 < p2.length; d2++) {
          var y2 = p2[d2], s2 = y2[0], c2 = y2[1];
          e2.queries.query[s2] = c2;
        }
      }
      function Dn(f2) {
        var h2 = f2._state, r2 = f2._deps.indexedDB;
        if (h2.isBeingOpened || f2.idbdb) return h2.dbReadyPromise.then(function() {
          return h2.dbOpenError ? Xe(h2.dbOpenError) : f2;
        });
        h2.isBeingOpened = true, h2.dbOpenError = null, h2.openComplete = false;
        var t2 = h2.openCanceller, d2 = Math.round(10 * f2.verno), p2 = false;
        function e2() {
          if (h2.openCanceller !== t2) throw new Y.DatabaseClosed("db.open() was cancelled");
        }
        function y2() {
          return new _e(function(s2, n3) {
            if (e2(), !r2) throw new Y.MissingAPI();
            var c2 = f2.name, l2 = h2.autoSchema || !d2 ? r2.open(c2) : r2.open(c2, d2);
            if (!l2) throw new Y.MissingAPI();
            l2.onerror = Bt(n3), l2.onblocked = qe(f2._fireOnBlocked), l2.onupgradeneeded = qe(function(e3) {
              var t3;
              v2 = l2.transaction, h2.autoSchema && !f2._options.allowEmptyDB ? (l2.onerror = Rt, v2.abort(), l2.result.close(), (t3 = r2.deleteDatabase(c2)).onsuccess = t3.onerror = qe(function() {
                n3(new Y.NoSuchDatabase("Database ".concat(c2, " doesnt exist")));
              })) : (v2.onerror = Bt(n3), e3 = e3.oldVersion > Math.pow(2, 62) ? 0 : e3.oldVersion, m2 = e3 < 1, f2.idbdb = l2.result, p2 && on(f2, v2), rn(f2, e3 / 10, v2, n3));
            }, n3), l2.onsuccess = qe(function() {
              v2 = null;
              var e3, t3, n4, r3, i3, o2 = f2.idbdb = l2.result, a2 = b(o2.objectStoreNames);
              if (0 < a2.length) try {
                var u2 = o2.transaction(1 === (r3 = a2).length ? r3[0] : r3, "readonly");
                if (h2.autoSchema) t3 = o2, n4 = u2, (e3 = f2).verno = t3.version / 10, n4 = e3._dbSchema = ln(0, t3, n4), e3._storeNames = b(t3.objectStoreNames, 0), en(e3, [e3._allTables], x(n4), n4);
                else if (fn(f2, f2._dbSchema, u2), ((i3 = an(ln(0, (i3 = f2).idbdb, u2), i3._dbSchema)).add.length || i3.change.some(function(e4) {
                  return e4.add.length || e4.change.length;
                })) && !p2) return console.warn("Dexie SchemaDiff: Schema was extended without increasing the number passed to db.version(). Dexie will add missing parts and increment native version number to workaround this."), o2.close(), d2 = o2.version + 1, p2 = true, s2(y2());
                Zt(f2, u2);
              } catch (e4) {
              }
              et.push(f2), o2.onversionchange = qe(function(e4) {
                h2.vcFired = true, f2.on("versionchange").fire(e4);
              }), o2.onclose = qe(function(e4) {
                f2.on("close").fire(e4);
              }), m2 && (i3 = f2._deps, u2 = c2, o2 = i3.indexedDB, i3 = i3.IDBKeyRange, vn(o2) || u2 === tt || yn(o2, i3).put({ name: u2 }).catch(G)), s2();
            }, n3);
          }).catch(function(e3) {
            switch (null == e3 ? void 0 : e3.name) {
              case "UnknownError":
                if (0 < h2.PR1398_maxLoop) return h2.PR1398_maxLoop--, console.warn("Dexie: Workaround for Chrome UnknownError on open()"), y2();
                break;
              case "VersionError":
                if (0 < d2) return d2 = 0, y2();
            }
            return _e.reject(e3);
          });
        }
        var n2, i2 = h2.dbReadyResolve, v2 = null, m2 = false;
        return _e.race([t2, ("undefined" == typeof navigator ? _e.resolve() : !navigator.userAgentData && /Safari\//.test(navigator.userAgent) && !/Chrom(e|ium)\//.test(navigator.userAgent) && indexedDB.databases ? new Promise(function(e3) {
          function t3() {
            return indexedDB.databases().finally(e3);
          }
          n2 = setInterval(t3, 100), t3();
        }).finally(function() {
          return clearInterval(n2);
        }) : Promise.resolve()).then(y2)]).then(function() {
          return e2(), h2.onReadyBeingFired = [], _e.resolve(mn(function() {
            return f2.on.ready.fire(f2.vip);
          })).then(function e3() {
            if (0 < h2.onReadyBeingFired.length) {
              var t3 = h2.onReadyBeingFired.reduce(re, G);
              return h2.onReadyBeingFired = [], _e.resolve(mn(function() {
                return t3(f2.vip);
              })).then(e3);
            }
          });
        }).finally(function() {
          h2.openCanceller === t2 && (h2.onReadyBeingFired = null, h2.isBeingOpened = false);
        }).catch(function(e3) {
          h2.dbOpenError = e3;
          try {
            v2 && v2.abort();
          } catch (e4) {
          }
          return t2 === h2.openCanceller && f2._close(), Xe(e3);
        }).finally(function() {
          h2.openComplete = true, i2();
        }).then(function() {
          var n3;
          return m2 && (n3 = {}, f2.tables.forEach(function(t3) {
            t3.schema.indexes.forEach(function(e3) {
              e3.name && (n3["idb://".concat(f2.name, "/").concat(t3.name, "/").concat(e3.name)] = new gn(-1 / 0, [[[]]]));
            }), n3["idb://".concat(f2.name, "/").concat(t3.name, "/")] = n3["idb://".concat(f2.name, "/").concat(t3.name, "/:dels")] = new gn(-1 / 0, [[[]]]);
          }), Nt(Ft).fire(n3), Tn(n3, true)), f2;
        });
      }
      function In(t2) {
        function e2(e3) {
          return t2.next(e3);
        }
        var r2 = n2(e2), i2 = n2(function(e3) {
          return t2.throw(e3);
        });
        function n2(n3) {
          return function(e3) {
            var t3 = n3(e3), e3 = t3.value;
            return t3.done ? e3 : e3 && "function" == typeof e3.then ? e3.then(r2, i2) : k(e3) ? Promise.all(e3).then(r2, i2) : r2(e3);
          };
        }
        return n2(e2)();
      }
      function Bn(e2, t2, n2) {
        for (var r2 = k(e2) ? e2.slice() : [e2], i2 = 0; i2 < n2; ++i2) r2.push(t2);
        return r2;
      }
      var Rn = { stack: "dbcore", name: "VirtualIndexMiddleware", level: 1, create: function(f2) {
        return _(_({}, f2), { table: function(e2) {
          var a2 = f2.table(e2), t2 = a2.schema, u2 = {}, s2 = [];
          function c2(e3, t3, n3) {
            var r3 = Xt(e3), i3 = u2[r3] = u2[r3] || [], o2 = null == e3 ? 0 : "string" == typeof e3 ? 1 : e3.length, a3 = 0 < t3, a3 = _(_({}, n3), { name: a3 ? "".concat(r3, "(virtual-from:").concat(n3.name, ")") : n3.name, lowLevelIndex: n3, isVirtual: a3, keyTail: t3, keyLength: o2, extractKey: $t(e3), unique: !a3 && n3.unique });
            return i3.push(a3), a3.isPrimaryKey || s2.push(a3), 1 < o2 && c2(2 === o2 ? e3[0] : e3.slice(0, o2 - 1), t3 + 1, n3), i3.sort(function(e4, t4) {
              return e4.keyTail - t4.keyTail;
            }), a3;
          }
          e2 = c2(t2.primaryKey.keyPath, 0, t2.primaryKey);
          u2[":id"] = [e2];
          for (var n2 = 0, r2 = t2.indexes; n2 < r2.length; n2++) {
            var i2 = r2[n2];
            c2(i2.keyPath, 0, i2);
          }
          function l2(e3) {
            var t3, n3 = e3.query.index;
            return n3.isVirtual ? _(_({}, e3), { query: { index: n3.lowLevelIndex, range: (t3 = e3.query.range, n3 = n3.keyTail, { type: 1 === t3.type ? 2 : t3.type, lower: Bn(t3.lower, t3.lowerOpen ? f2.MAX_KEY : f2.MIN_KEY, n3), lowerOpen: true, upper: Bn(t3.upper, t3.upperOpen ? f2.MIN_KEY : f2.MAX_KEY, n3), upperOpen: true }) } }) : e3;
          }
          return _(_({}, a2), { schema: _(_({}, t2), { primaryKey: e2, indexes: s2, getIndexByKeyPath: function(e3) {
            return (e3 = u2[Xt(e3)]) && e3[0];
          } }), count: function(e3) {
            return a2.count(l2(e3));
          }, query: function(e3) {
            return a2.query(l2(e3));
          }, openCursor: function(t3) {
            var e3 = t3.query.index, r3 = e3.keyTail, n3 = e3.isVirtual, i3 = e3.keyLength;
            return n3 ? a2.openCursor(l2(t3)).then(function(e4) {
              return e4 && o2(e4);
            }) : a2.openCursor(t3);
            function o2(n4) {
              return Object.create(n4, { continue: { value: function(e4) {
                null != e4 ? n4.continue(Bn(e4, t3.reverse ? f2.MAX_KEY : f2.MIN_KEY, r3)) : t3.unique ? n4.continue(n4.key.slice(0, i3).concat(t3.reverse ? f2.MIN_KEY : f2.MAX_KEY, r3)) : n4.continue();
              } }, continuePrimaryKey: { value: function(e4, t4) {
                n4.continuePrimaryKey(Bn(e4, f2.MAX_KEY, r3), t4);
              } }, primaryKey: { get: function() {
                return n4.primaryKey;
              } }, key: { get: function() {
                var e4 = n4.key;
                return 1 === i3 ? e4[0] : e4.slice(0, i3);
              } }, value: { get: function() {
                return n4.value;
              } } });
            }
          } });
        } });
      } };
      function Fn(i2, o2, a2, u2) {
        return a2 = a2 || {}, u2 = u2 || "", x(i2).forEach(function(e2) {
          var t2, n2, r2;
          m(o2, e2) ? (t2 = i2[e2], n2 = o2[e2], "object" == typeof t2 && "object" == typeof n2 && t2 && n2 ? (r2 = A(t2)) !== A(n2) ? a2[u2 + e2] = o2[e2] : "Object" === r2 ? Fn(t2, n2, a2, u2 + e2 + ".") : t2 !== n2 && (a2[u2 + e2] = o2[e2]) : t2 !== n2 && (a2[u2 + e2] = o2[e2])) : a2[u2 + e2] = void 0;
        }), x(o2).forEach(function(e2) {
          m(i2, e2) || (a2[u2 + e2] = o2[e2]);
        }), a2;
      }
      function Mn(e2, t2) {
        return "delete" === t2.type ? t2.keys : t2.keys || t2.values.map(e2.extractKey);
      }
      var Nn = { stack: "dbcore", name: "HooksMiddleware", level: 2, create: function(e2) {
        return _(_({}, e2), { table: function(r2) {
          var y2 = e2.table(r2), v2 = y2.schema.primaryKey;
          return _(_({}, y2), { mutate: function(e3) {
            var t2 = me.trans, n2 = t2.table(r2).hook, h2 = n2.deleting, d2 = n2.creating, p2 = n2.updating;
            switch (e3.type) {
              case "add":
                if (d2.fire === G) break;
                return t2._promise("readwrite", function() {
                  return a2(e3);
                }, true);
              case "put":
                if (d2.fire === G && p2.fire === G) break;
                return t2._promise("readwrite", function() {
                  return a2(e3);
                }, true);
              case "delete":
                if (h2.fire === G) break;
                return t2._promise("readwrite", function() {
                  return a2(e3);
                }, true);
              case "deleteRange":
                if (h2.fire === G) break;
                return t2._promise("readwrite", function() {
                  return (function n3(r3, i2, o2) {
                    return y2.query({ trans: r3, values: false, query: { index: v2, range: i2 }, limit: o2 }).then(function(e4) {
                      var t3 = e4.result;
                      return a2({ type: "delete", keys: t3, trans: r3 }).then(function(e5) {
                        return 0 < e5.numFailures ? Promise.reject(e5.failures[0]) : t3.length < o2 ? { failures: [], numFailures: 0, lastResult: void 0 } : n3(r3, _(_({}, i2), { lower: t3[t3.length - 1], lowerOpen: true }), o2);
                      });
                    });
                  })(e3.trans, e3.range, 1e4);
                }, true);
            }
            return y2.mutate(e3);
            function a2(c2) {
              var e4, t3, n3, l2 = me.trans, f2 = c2.keys || Mn(v2, c2);
              if (!f2) throw new Error("Keys missing");
              return "delete" !== (c2 = "add" === c2.type || "put" === c2.type ? _(_({}, c2), { keys: f2 }) : _({}, c2)).type && (c2.values = i([], c2.values)), c2.keys && (c2.keys = i([], c2.keys)), e4 = y2, n3 = f2, ("add" === (t3 = c2).type ? Promise.resolve([]) : e4.getMany({ trans: t3.trans, keys: n3, cache: "immutable" })).then(function(u2) {
                var s2 = f2.map(function(e5, t4) {
                  var n4, r3, i2, o2 = u2[t4], a3 = { onerror: null, onsuccess: null };
                  return "delete" === c2.type ? h2.fire.call(a3, e5, o2, l2) : "add" === c2.type || void 0 === o2 ? (n4 = d2.fire.call(a3, e5, c2.values[t4], l2), null == e5 && null != n4 && (c2.keys[t4] = e5 = n4, v2.outbound || P(c2.values[t4], v2.keyPath, e5))) : (n4 = Fn(o2, c2.values[t4]), (r3 = p2.fire.call(a3, n4, e5, o2, l2)) && (i2 = c2.values[t4], Object.keys(r3).forEach(function(e6) {
                    m(i2, e6) ? i2[e6] = r3[e6] : P(i2, e6, r3[e6]);
                  }))), a3;
                });
                return y2.mutate(c2).then(function(e5) {
                  for (var t4 = e5.failures, n4 = e5.results, r3 = e5.numFailures, e5 = e5.lastResult, i2 = 0; i2 < f2.length; ++i2) {
                    var o2 = (n4 || f2)[i2], a3 = s2[i2];
                    null == o2 ? a3.onerror && a3.onerror(t4[i2]) : a3.onsuccess && a3.onsuccess("put" === c2.type && u2[i2] ? c2.values[i2] : o2);
                  }
                  return { failures: t4, results: n4, numFailures: r3, lastResult: e5 };
                }).catch(function(t4) {
                  return s2.forEach(function(e5) {
                    return e5.onerror && e5.onerror(t4);
                  }), Promise.reject(t4);
                });
              });
            }
          } });
        } });
      } };
      function Ln(e2, t2, n2) {
        try {
          if (!t2) return null;
          if (t2.keys.length < e2.length) return null;
          for (var r2 = [], i2 = 0, o2 = 0; i2 < t2.keys.length && o2 < e2.length; ++i2) 0 === st(t2.keys[i2], e2[o2]) && (r2.push(n2 ? S(t2.values[i2]) : t2.values[i2]), ++o2);
          return r2.length === e2.length ? r2 : null;
        } catch (e3) {
          return null;
        }
      }
      var Un = { stack: "dbcore", level: -1, create: function(t2) {
        return { table: function(e2) {
          var n2 = t2.table(e2);
          return _(_({}, n2), { getMany: function(t3) {
            if (!t3.cache) return n2.getMany(t3);
            var e3 = Ln(t3.keys, t3.trans._cache, "clone" === t3.cache);
            return e3 ? _e.resolve(e3) : n2.getMany(t3).then(function(e4) {
              return t3.trans._cache = { keys: t3.keys, values: "clone" === t3.cache ? S(e4) : e4 }, e4;
            });
          }, mutate: function(e3) {
            return "add" !== e3.type && (e3.trans._cache = null), n2.mutate(e3);
          } });
        } };
      } };
      function Vn(e2, t2) {
        return "readonly" === e2.trans.mode && !!e2.subscr && !e2.trans.explicit && "disabled" !== e2.trans.db._options.cache && !t2.schema.primaryKey.outbound;
      }
      function zn(e2, t2) {
        switch (e2) {
          case "query":
            return t2.values && !t2.unique;
          case "get":
          case "getMany":
          case "count":
          case "openCursor":
            return false;
        }
      }
      var Wn = { stack: "dbcore", level: 0, name: "Observability", create: function(b2) {
        var g2 = b2.schema.name, w2 = new gn(b2.MIN_KEY, b2.MAX_KEY);
        return _(_({}, b2), { transaction: function(e2, t2, n2) {
          if (me.subscr && "readonly" !== t2) throw new Y.ReadOnly("Readwrite transaction in liveQuery context. Querier source: ".concat(me.querier));
          return b2.transaction(e2, t2, n2);
        }, table: function(d2) {
          var p2 = b2.table(d2), y2 = p2.schema, v2 = y2.primaryKey, e2 = y2.indexes, c2 = v2.extractKey, l2 = v2.outbound, m2 = v2.autoIncrement && e2.filter(function(e3) {
            return e3.compound && e3.keyPath.includes(v2.keyPath);
          }), t2 = _(_({}, p2), { mutate: function(a2) {
            function u2(e4) {
              return e4 = "idb://".concat(g2, "/").concat(d2, "/").concat(e4), n2[e4] || (n2[e4] = new gn());
            }
            var e3, o2, s2, t3 = a2.trans, n2 = a2.mutatedParts || (a2.mutatedParts = {}), r2 = u2(""), i2 = u2(":dels"), c3 = a2.type, l3 = "deleteRange" === a2.type ? [a2.range] : "delete" === a2.type ? [a2.keys] : a2.values.length < 50 ? [Mn(v2, a2).filter(function(e4) {
              return e4;
            }), a2.values] : [], f3 = l3[0], h2 = l3[1], l3 = a2.trans._cache;
            return k(f3) ? (r2.addKeys(f3), (l3 = "delete" === c3 || f3.length === h2.length ? Ln(f3, l3) : null) || i2.addKeys(f3), (l3 || h2) && (e3 = u2, o2 = l3, s2 = h2, y2.indexes.forEach(function(t4) {
              var n3 = e3(t4.name || "");
              function r3(e4) {
                return null != e4 ? t4.extractKey(e4) : null;
              }
              function i3(e4) {
                return t4.multiEntry && k(e4) ? e4.forEach(function(e5) {
                  return n3.addKey(e5);
                }) : n3.addKey(e4);
              }
              (o2 || s2).forEach(function(e4, t5) {
                var n4 = o2 && r3(o2[t5]), t5 = s2 && r3(s2[t5]);
                0 !== st(n4, t5) && (null != n4 && i3(n4), null != t5 && i3(t5));
              });
            }))) : f3 ? (h2 = { from: null !== (h2 = f3.lower) && void 0 !== h2 ? h2 : b2.MIN_KEY, to: null !== (h2 = f3.upper) && void 0 !== h2 ? h2 : b2.MAX_KEY }, i2.add(h2), r2.add(h2)) : (r2.add(w2), i2.add(w2), y2.indexes.forEach(function(e4) {
              return u2(e4.name).add(w2);
            })), p2.mutate(a2).then(function(o3) {
              return !f3 || "add" !== a2.type && "put" !== a2.type || (r2.addKeys(o3.results), m2 && m2.forEach(function(t4) {
                for (var e4 = a2.values.map(function(e5) {
                  return t4.extractKey(e5);
                }), n3 = t4.keyPath.findIndex(function(e5) {
                  return e5 === v2.keyPath;
                }), r3 = 0, i3 = o3.results.length; r3 < i3; ++r3) e4[r3][n3] = o3.results[r3];
                u2(t4.name).addKeys(e4);
              })), t3.mutatedParts = Kn(t3.mutatedParts || {}, n2), o3;
            });
          } }), e2 = function(e3) {
            var t3 = e3.query, e3 = t3.index, t3 = t3.range;
            return [e3, new gn(null !== (e3 = t3.lower) && void 0 !== e3 ? e3 : b2.MIN_KEY, null !== (t3 = t3.upper) && void 0 !== t3 ? t3 : b2.MAX_KEY)];
          }, f2 = { get: function(e3) {
            return [v2, new gn(e3.key)];
          }, getMany: function(e3) {
            return [v2, new gn().addKeys(e3.keys)];
          }, count: e2, query: e2, openCursor: e2 };
          return x(f2).forEach(function(s2) {
            t2[s2] = function(i2) {
              var e3 = me.subscr, t3 = !!e3, n2 = Vn(me, p2) && zn(s2, i2) ? i2.obsSet = {} : e3;
              if (t3) {
                var r2 = function(e4) {
                  e4 = "idb://".concat(g2, "/").concat(d2, "/").concat(e4);
                  return n2[e4] || (n2[e4] = new gn());
                }, o2 = r2(""), a2 = r2(":dels"), e3 = f2[s2](i2), t3 = e3[0], e3 = e3[1];
                if (("query" === s2 && t3.isPrimaryKey && !i2.values ? a2 : r2(t3.name || "")).add(e3), !t3.isPrimaryKey) {
                  if ("count" !== s2) {
                    var u2 = "query" === s2 && l2 && i2.values && p2.query(_(_({}, i2), { values: false }));
                    return p2[s2].apply(this, arguments).then(function(t4) {
                      if ("query" === s2) {
                        if (l2 && i2.values) return u2.then(function(e5) {
                          e5 = e5.result;
                          return o2.addKeys(e5), t4;
                        });
                        var e4 = i2.values ? t4.result.map(c2) : t4.result;
                        (i2.values ? o2 : a2).addKeys(e4);
                      } else if ("openCursor" === s2) {
                        var n3 = t4, r3 = i2.values;
                        return n3 && Object.create(n3, { key: { get: function() {
                          return a2.addKey(n3.primaryKey), n3.key;
                        } }, primaryKey: { get: function() {
                          var e5 = n3.primaryKey;
                          return a2.addKey(e5), e5;
                        } }, value: { get: function() {
                          return r3 && o2.addKey(n3.primaryKey), n3.value;
                        } } });
                      }
                      return t4;
                    });
                  }
                  a2.add(w2);
                }
              }
              return p2[s2].apply(this, arguments);
            };
          }), t2;
        } });
      } };
      function Yn(e2, t2, n2) {
        if (0 === n2.numFailures) return t2;
        if ("deleteRange" === t2.type) return null;
        var r2 = t2.keys ? t2.keys.length : "values" in t2 && t2.values ? t2.values.length : 1;
        if (n2.numFailures === r2) return null;
        t2 = _({}, t2);
        return k(t2.keys) && (t2.keys = t2.keys.filter(function(e3, t3) {
          return !(t3 in n2.failures);
        })), "values" in t2 && k(t2.values) && (t2.values = t2.values.filter(function(e3, t3) {
          return !(t3 in n2.failures);
        })), t2;
      }
      function $n(e2, t2) {
        return n2 = e2, (void 0 === (r2 = t2).lower || (r2.lowerOpen ? 0 < st(n2, r2.lower) : 0 <= st(n2, r2.lower))) && (e2 = e2, void 0 === (t2 = t2).upper || (t2.upperOpen ? st(e2, t2.upper) < 0 : st(e2, t2.upper) <= 0));
        var n2, r2;
      }
      function Qn(e2, d2, t2, n2, r2, i2) {
        if (!t2 || 0 === t2.length) return e2;
        var o2 = d2.query.index, p2 = o2.multiEntry, y2 = d2.query.range, v2 = n2.schema.primaryKey.extractKey, m2 = o2.extractKey, a2 = (o2.lowLevelIndex || o2).extractKey, t2 = t2.reduce(function(e3, t3) {
          var n3 = e3, r3 = [];
          if ("add" === t3.type || "put" === t3.type) for (var i3 = new gn(), o3 = t3.values.length - 1; 0 <= o3; --o3) {
            var a3, u2 = t3.values[o3], s2 = v2(u2);
            i3.hasKey(s2) || (a3 = m2(u2), (p2 && k(a3) ? a3.some(function(e4) {
              return $n(e4, y2);
            }) : $n(a3, y2)) && (i3.addKey(s2), r3.push(u2)));
          }
          switch (t3.type) {
            case "add":
              var c2 = new gn().addKeys(d2.values ? e3.map(function(e4) {
                return v2(e4);
              }) : e3), n3 = e3.concat(d2.values ? r3.filter(function(e4) {
                e4 = v2(e4);
                return !c2.hasKey(e4) && (c2.addKey(e4), true);
              }) : r3.map(function(e4) {
                return v2(e4);
              }).filter(function(e4) {
                return !c2.hasKey(e4) && (c2.addKey(e4), true);
              }));
              break;
            case "put":
              var l2 = new gn().addKeys(t3.values.map(function(e4) {
                return v2(e4);
              }));
              n3 = e3.filter(function(e4) {
                return !l2.hasKey(d2.values ? v2(e4) : e4);
              }).concat(d2.values ? r3 : r3.map(function(e4) {
                return v2(e4);
              }));
              break;
            case "delete":
              var f2 = new gn().addKeys(t3.keys);
              n3 = e3.filter(function(e4) {
                return !f2.hasKey(d2.values ? v2(e4) : e4);
              });
              break;
            case "deleteRange":
              var h2 = t3.range;
              n3 = e3.filter(function(e4) {
                return !$n(v2(e4), h2);
              });
          }
          return n3;
        }, e2);
        return t2 === e2 ? e2 : (t2.sort(function(e3, t3) {
          return st(a2(e3), a2(t3)) || st(v2(e3), v2(t3));
        }), d2.limit && d2.limit < 1 / 0 && (t2.length > d2.limit ? t2.length = d2.limit : e2.length === d2.limit && t2.length < d2.limit && (r2.dirty = true)), i2 ? Object.freeze(t2) : t2);
      }
      function Gn(e2, t2) {
        return 0 === st(e2.lower, t2.lower) && 0 === st(e2.upper, t2.upper) && !!e2.lowerOpen == !!t2.lowerOpen && !!e2.upperOpen == !!t2.upperOpen;
      }
      function Xn(e2, t2) {
        return (function(e3, t3, n2, r2) {
          if (void 0 === e3) return void 0 !== t3 ? -1 : 0;
          if (void 0 === t3) return 1;
          if (0 === (t3 = st(e3, t3))) {
            if (n2 && r2) return 0;
            if (n2) return 1;
            if (r2) return -1;
          }
          return t3;
        })(e2.lower, t2.lower, e2.lowerOpen, t2.lowerOpen) <= 0 && 0 <= (function(e3, t3, n2, r2) {
          if (void 0 === e3) return void 0 !== t3 ? 1 : 0;
          if (void 0 === t3) return -1;
          if (0 === (t3 = st(e3, t3))) {
            if (n2 && r2) return 0;
            if (n2) return -1;
            if (r2) return 1;
          }
          return t3;
        })(e2.upper, t2.upper, e2.upperOpen, t2.upperOpen);
      }
      function Hn(n2, r2, i2, e2) {
        n2.subscribers.add(i2), e2.addEventListener("abort", function() {
          var e3, t2;
          n2.subscribers.delete(i2), 0 === n2.subscribers.size && (e3 = n2, t2 = r2, setTimeout(function() {
            0 === e3.subscribers.size && q(t2, e3);
          }, 3e3));
        });
      }
      var Jn = { stack: "dbcore", level: 0, name: "Cache", create: function(k2) {
        var O2 = k2.schema.name;
        return _(_({}, k2), { transaction: function(g2, w2, e2) {
          var _2, t2, x2 = k2.transaction(g2, w2, e2);
          return "readwrite" === w2 && (t2 = (_2 = new AbortController()).signal, e2 = function(b2) {
            return function() {
              if (_2.abort(), "readwrite" === w2) {
                for (var t3 = /* @__PURE__ */ new Set(), e3 = 0, n2 = g2; e3 < n2.length; e3++) {
                  var r2 = n2[e3], i2 = Sn["idb://".concat(O2, "/").concat(r2)];
                  if (i2) {
                    var o2 = k2.table(r2), a2 = i2.optimisticOps.filter(function(e4) {
                      return e4.trans === x2;
                    });
                    if (x2._explicit && b2 && x2.mutatedParts) for (var u2 = 0, s2 = Object.values(i2.queries.query); u2 < s2.length; u2++) for (var c2 = 0, l2 = (d2 = s2[u2]).slice(); c2 < l2.length; c2++) En((p2 = l2[c2]).obsSet, x2.mutatedParts) && (q(d2, p2), p2.subscribers.forEach(function(e4) {
                      return t3.add(e4);
                    }));
                    else if (0 < a2.length) {
                      i2.optimisticOps = i2.optimisticOps.filter(function(e4) {
                        return e4.trans !== x2;
                      });
                      for (var f2 = 0, h2 = Object.values(i2.queries.query); f2 < h2.length; f2++) for (var d2, p2, y2, v2 = 0, m2 = (d2 = h2[f2]).slice(); v2 < m2.length; v2++) null != (p2 = m2[v2]).res && x2.mutatedParts && (b2 && !p2.dirty ? (y2 = Object.isFrozen(p2.res), y2 = Qn(p2.res, p2.req, a2, o2, p2, y2), p2.dirty ? (q(d2, p2), p2.subscribers.forEach(function(e4) {
                        return t3.add(e4);
                      })) : y2 !== p2.res && (p2.res = y2, p2.promise = _e.resolve({ result: y2 }))) : (p2.dirty && q(d2, p2), p2.subscribers.forEach(function(e4) {
                        return t3.add(e4);
                      })));
                    }
                  }
                }
                t3.forEach(function(e4) {
                  return e4();
                });
              }
            };
          }, x2.addEventListener("abort", e2(false), { signal: t2 }), x2.addEventListener("error", e2(false), { signal: t2 }), x2.addEventListener("complete", e2(true), { signal: t2 })), x2;
        }, table: function(c2) {
          var l2 = k2.table(c2), i2 = l2.schema.primaryKey;
          return _(_({}, l2), { mutate: function(t2) {
            var e2 = me.trans;
            if (i2.outbound || "disabled" === e2.db._options.cache || e2.explicit || "readwrite" !== e2.idbtrans.mode) return l2.mutate(t2);
            var n2 = Sn["idb://".concat(O2, "/").concat(c2)];
            if (!n2) return l2.mutate(t2);
            e2 = l2.mutate(t2);
            return "add" !== t2.type && "put" !== t2.type || !(50 <= t2.values.length || Mn(i2, t2).some(function(e3) {
              return null == e3;
            })) ? (n2.optimisticOps.push(t2), t2.mutatedParts && Cn(t2.mutatedParts), e2.then(function(e3) {
              0 < e3.numFailures && (q(n2.optimisticOps, t2), (e3 = Yn(0, t2, e3)) && n2.optimisticOps.push(e3), t2.mutatedParts && Cn(t2.mutatedParts));
            }), e2.catch(function() {
              q(n2.optimisticOps, t2), t2.mutatedParts && Cn(t2.mutatedParts);
            })) : e2.then(function(r2) {
              var e3 = Yn(0, _(_({}, t2), { values: t2.values.map(function(e4, t3) {
                var n3;
                if (r2.failures[t3]) return e4;
                e4 = null !== (n3 = i2.keyPath) && void 0 !== n3 && n3.includes(".") ? S(e4) : _({}, e4);
                return P(e4, i2.keyPath, r2.results[t3]), e4;
              }) }), r2);
              n2.optimisticOps.push(e3), queueMicrotask(function() {
                return t2.mutatedParts && Cn(t2.mutatedParts);
              });
            }), e2;
          }, query: function(t2) {
            if (!Vn(me, l2) || !zn("query", t2)) return l2.query(t2);
            var i3 = "immutable" === (null === (o2 = me.trans) || void 0 === o2 ? void 0 : o2.db._options.cache), e2 = me, n2 = e2.requery, r2 = e2.signal, o2 = (function(e3, t3, n3, r3) {
              var i4 = Sn["idb://".concat(e3, "/").concat(t3)];
              if (!i4) return [];
              if (!(t3 = i4.queries[n3])) return [null, false, i4, null];
              var o3 = t3[(r3.query ? r3.query.index.name : null) || ""];
              if (!o3) return [null, false, i4, null];
              switch (n3) {
                case "query":
                  var a3 = o3.find(function(e4) {
                    return e4.req.limit === r3.limit && e4.req.values === r3.values && Gn(e4.req.query.range, r3.query.range);
                  });
                  return a3 ? [a3, true, i4, o3] : [o3.find(function(e4) {
                    return ("limit" in e4.req ? e4.req.limit : 1 / 0) >= r3.limit && (!r3.values || e4.req.values) && Xn(e4.req.query.range, r3.query.range);
                  }), false, i4, o3];
                case "count":
                  a3 = o3.find(function(e4) {
                    return Gn(e4.req.query.range, r3.query.range);
                  });
                  return [a3, !!a3, i4, o3];
              }
            })(O2, c2, "query", t2), a2 = o2[0], e2 = o2[1], u2 = o2[2], s2 = o2[3];
            return a2 && e2 ? a2.obsSet = t2.obsSet : (e2 = l2.query(t2).then(function(e3) {
              var t3 = e3.result;
              if (a2 && (a2.res = t3), i3) {
                for (var n3 = 0, r3 = t3.length; n3 < r3; ++n3) Object.freeze(t3[n3]);
                Object.freeze(t3);
              } else e3.result = S(t3);
              return e3;
            }).catch(function(e3) {
              return s2 && a2 && q(s2, a2), Promise.reject(e3);
            }), a2 = { obsSet: t2.obsSet, promise: e2, subscribers: /* @__PURE__ */ new Set(), type: "query", req: t2, dirty: false }, s2 ? s2.push(a2) : (s2 = [a2], (u2 = u2 || (Sn["idb://".concat(O2, "/").concat(c2)] = { queries: { query: {}, count: {} }, objs: /* @__PURE__ */ new Map(), optimisticOps: [], unsignaledParts: {} })).queries.query[t2.query.index.name || ""] = s2)), Hn(a2, s2, n2, r2), a2.promise.then(function(e3) {
              return { result: Qn(e3.result, t2, null == u2 ? void 0 : u2.optimisticOps, l2, a2, i3) };
            });
          } });
        } });
      } };
      function Zn(e2, r2) {
        return new Proxy(e2, { get: function(e3, t2, n2) {
          return "db" === t2 ? r2 : Reflect.get(e3, t2, n2);
        } });
      }
      var er = (tr.prototype.version = function(t2) {
        if (isNaN(t2) || t2 < 0.1) throw new Y.Type("Given version is not a positive number");
        if (t2 = Math.round(10 * t2) / 10, this.idbdb || this._state.isBeingOpened) throw new Y.Schema("Cannot add version when database is open");
        this.verno = Math.max(this.verno, t2);
        var e2 = this._versions, n2 = e2.filter(function(e3) {
          return e3._cfg.version === t2;
        })[0];
        return n2 || (n2 = new this.Version(t2), e2.push(n2), e2.sort(nn), n2.stores({}), this._state.autoSchema = false, n2);
      }, tr.prototype._whenReady = function(e2) {
        var n2 = this;
        return this.idbdb && (this._state.openComplete || me.letThrough || this._vip) ? e2() : new _e(function(e3, t2) {
          if (n2._state.openComplete) return t2(new Y.DatabaseClosed(n2._state.dbOpenError));
          if (!n2._state.isBeingOpened) {
            if (!n2._state.autoOpen) return void t2(new Y.DatabaseClosed());
            n2.open().catch(G);
          }
          n2._state.dbReadyPromise.then(e3, t2);
        }).then(e2);
      }, tr.prototype.use = function(e2) {
        var t2 = e2.stack, n2 = e2.create, r2 = e2.level, i2 = e2.name;
        i2 && this.unuse({ stack: t2, name: i2 });
        e2 = this._middlewares[t2] || (this._middlewares[t2] = []);
        return e2.push({ stack: t2, create: n2, level: null == r2 ? 10 : r2, name: i2 }), e2.sort(function(e3, t3) {
          return e3.level - t3.level;
        }), this;
      }, tr.prototype.unuse = function(e2) {
        var t2 = e2.stack, n2 = e2.name, r2 = e2.create;
        return t2 && this._middlewares[t2] && (this._middlewares[t2] = this._middlewares[t2].filter(function(e3) {
          return r2 ? e3.create !== r2 : !!n2 && e3.name !== n2;
        })), this;
      }, tr.prototype.open = function() {
        var e2 = this;
        return $e(ve, function() {
          return Dn(e2);
        });
      }, tr.prototype._close = function() {
        var n2 = this._state, e2 = et.indexOf(this);
        if (0 <= e2 && et.splice(e2, 1), this.idbdb) {
          try {
            this.idbdb.close();
          } catch (e3) {
          }
          this.idbdb = null;
        }
        n2.isBeingOpened || (n2.dbReadyPromise = new _e(function(e3) {
          n2.dbReadyResolve = e3;
        }), n2.openCanceller = new _e(function(e3, t2) {
          n2.cancelOpen = t2;
        }));
      }, tr.prototype.close = function(e2) {
        var t2 = (void 0 === e2 ? { disableAutoOpen: true } : e2).disableAutoOpen, e2 = this._state;
        t2 ? (e2.isBeingOpened && e2.cancelOpen(new Y.DatabaseClosed()), this._close(), e2.autoOpen = false, e2.dbOpenError = new Y.DatabaseClosed()) : (this._close(), e2.autoOpen = this._options.autoOpen || e2.isBeingOpened, e2.openComplete = false, e2.dbOpenError = null);
      }, tr.prototype.delete = function(n2) {
        var i2 = this;
        void 0 === n2 && (n2 = { disableAutoOpen: true });
        var o2 = 0 < arguments.length && "object" != typeof arguments[0], a2 = this._state;
        return new _e(function(r2, t2) {
          function e2() {
            i2.close(n2);
            var e3 = i2._deps.indexedDB.deleteDatabase(i2.name);
            e3.onsuccess = qe(function() {
              var e4, t3, n3;
              e4 = i2._deps, t3 = i2.name, n3 = e4.indexedDB, e4 = e4.IDBKeyRange, vn(n3) || t3 === tt || yn(n3, e4).delete(t3).catch(G), r2();
            }), e3.onerror = Bt(t2), e3.onblocked = i2._fireOnBlocked;
          }
          if (o2) throw new Y.InvalidArgument("Invalid closeOptions argument to db.delete()");
          a2.isBeingOpened ? a2.dbReadyPromise.then(e2) : e2();
        });
      }, tr.prototype.backendDB = function() {
        return this.idbdb;
      }, tr.prototype.isOpen = function() {
        return null !== this.idbdb;
      }, tr.prototype.hasBeenClosed = function() {
        var e2 = this._state.dbOpenError;
        return e2 && "DatabaseClosed" === e2.name;
      }, tr.prototype.hasFailed = function() {
        return null !== this._state.dbOpenError;
      }, tr.prototype.dynamicallyOpened = function() {
        return this._state.autoSchema;
      }, Object.defineProperty(tr.prototype, "tables", { get: function() {
        var t2 = this;
        return x(this._allTables).map(function(e2) {
          return t2._allTables[e2];
        });
      }, enumerable: false, configurable: true }), tr.prototype.transaction = function() {
        var e2 = (function(e3, t2, n2) {
          var r2 = arguments.length;
          if (r2 < 2) throw new Y.InvalidArgument("Too few arguments");
          for (var i2 = new Array(r2 - 1); --r2; ) i2[r2 - 1] = arguments[r2];
          return n2 = i2.pop(), [e3, w(i2), n2];
        }).apply(this, arguments);
        return this._transaction.apply(this, e2);
      }, tr.prototype._transaction = function(e2, t2, n2) {
        var r2 = this, i2 = me.trans;
        i2 && i2.db === this && -1 === e2.indexOf("!") || (i2 = null);
        var o2, a2, u2 = -1 !== e2.indexOf("?");
        e2 = e2.replace("!", "").replace("?", "");
        try {
          if (a2 = t2.map(function(e3) {
            e3 = e3 instanceof r2.Table ? e3.name : e3;
            if ("string" != typeof e3) throw new TypeError("Invalid table argument to Dexie.transaction(). Only Table or String are allowed");
            return e3;
          }), "r" == e2 || e2 === nt) o2 = nt;
          else {
            if ("rw" != e2 && e2 != rt) throw new Y.InvalidArgument("Invalid transaction mode: " + e2);
            o2 = rt;
          }
          if (i2) {
            if (i2.mode === nt && o2 === rt) {
              if (!u2) throw new Y.SubTransaction("Cannot enter a sub-transaction with READWRITE mode when parent transaction is READONLY");
              i2 = null;
            }
            i2 && a2.forEach(function(e3) {
              if (i2 && -1 === i2.storeNames.indexOf(e3)) {
                if (!u2) throw new Y.SubTransaction("Table " + e3 + " not included in parent transaction.");
                i2 = null;
              }
            }), u2 && i2 && !i2.active && (i2 = null);
          }
        } catch (n3) {
          return i2 ? i2._promise(null, function(e3, t3) {
            t3(n3);
          }) : Xe(n3);
        }
        var s2 = (function i3(o3, a3, u3, s3, c2) {
          return _e.resolve().then(function() {
            var e3 = me.transless || me, t3 = o3._createTransaction(a3, u3, o3._dbSchema, s3);
            if (t3.explicit = true, e3 = { trans: t3, transless: e3 }, s3) t3.idbtrans = s3.idbtrans;
            else try {
              t3.create(), t3.idbtrans._explicit = true, o3._state.PR1398_maxLoop = 3;
            } catch (e4) {
              return e4.name === z.InvalidState && o3.isOpen() && 0 < --o3._state.PR1398_maxLoop ? (console.warn("Dexie: Need to reopen db"), o3.close({ disableAutoOpen: false }), o3.open().then(function() {
                return i3(o3, a3, u3, null, c2);
              })) : Xe(e4);
            }
            var n3, r3 = B(c2);
            return r3 && Le(), e3 = _e.follow(function() {
              var e4;
              (n3 = c2.call(t3, t3)) && (r3 ? (e4 = Ue.bind(null, null), n3.then(e4, e4)) : "function" == typeof n3.next && "function" == typeof n3.throw && (n3 = In(n3)));
            }, e3), (n3 && "function" == typeof n3.then ? _e.resolve(n3).then(function(e4) {
              return t3.active ? e4 : Xe(new Y.PrematureCommit("Transaction committed too early. See http://bit.ly/2kdckMn"));
            }) : e3.then(function() {
              return n3;
            })).then(function(e4) {
              return s3 && t3._resolve(), t3._completion.then(function() {
                return e4;
              });
            }).catch(function(e4) {
              return t3._reject(e4), Xe(e4);
            });
          });
        }).bind(null, this, o2, a2, i2, n2);
        return i2 ? i2._promise(o2, s2, "lock") : me.trans ? $e(me.transless, function() {
          return r2._whenReady(s2);
        }) : this._whenReady(s2);
      }, tr.prototype.table = function(e2) {
        if (!m(this._allTables, e2)) throw new Y.InvalidTable("Table ".concat(e2, " does not exist"));
        return this._allTables[e2];
      }, tr);
      function tr(e2, t2) {
        var o2 = this;
        this._middlewares = {}, this.verno = 0;
        var n2 = tr.dependencies;
        this._options = t2 = _({ addons: tr.addons, autoOpen: true, indexedDB: n2.indexedDB, IDBKeyRange: n2.IDBKeyRange, cache: "cloned" }, t2), this._deps = { indexedDB: t2.indexedDB, IDBKeyRange: t2.IDBKeyRange };
        n2 = t2.addons;
        this._dbSchema = {}, this._versions = [], this._storeNames = [], this._allTables = {}, this.idbdb = null, this._novip = this;
        var a2, r2, u2, i2, s2, c2 = { dbOpenError: null, isBeingOpened: false, onReadyBeingFired: null, openComplete: false, dbReadyResolve: G, dbReadyPromise: null, cancelOpen: G, openCanceller: null, autoSchema: true, PR1398_maxLoop: 3, autoOpen: t2.autoOpen };
        c2.dbReadyPromise = new _e(function(e3) {
          c2.dbReadyResolve = e3;
        }), c2.openCanceller = new _e(function(e3, t3) {
          c2.cancelOpen = t3;
        }), this._state = c2, this.name = e2, this.on = dt(this, "populate", "blocked", "versionchange", "close", { ready: [re, G] }), this.on.ready.subscribe = p(this.on.ready.subscribe, function(i3) {
          return function(n3, r3) {
            tr.vip(function() {
              var t3, e3 = o2._state;
              e3.openComplete ? (e3.dbOpenError || _e.resolve().then(n3), r3 && i3(n3)) : e3.onReadyBeingFired ? (e3.onReadyBeingFired.push(n3), r3 && i3(n3)) : (i3(n3), t3 = o2, r3 || i3(function e4() {
                t3.on.ready.unsubscribe(n3), t3.on.ready.unsubscribe(e4);
              }));
            });
          };
        }), this.Collection = (a2 = this, pt(Ot.prototype, function(e3, t3) {
          this.db = a2;
          var n3 = ot, r3 = null;
          if (t3) try {
            n3 = t3();
          } catch (e4) {
            r3 = e4;
          }
          var i3 = e3._ctx, t3 = i3.table, e3 = t3.hook.reading.fire;
          this._ctx = { table: t3, index: i3.index, isPrimKey: !i3.index || t3.schema.primKey.keyPath && i3.index === t3.schema.primKey.name, range: n3, keysOnly: false, dir: "next", unique: "", algorithm: null, filter: null, replayFilter: null, justLimit: true, isMatch: null, offset: 0, limit: 1 / 0, error: r3, or: i3.or, valueMapper: e3 !== X ? e3 : null };
        })), this.Table = (r2 = this, pt(ft.prototype, function(e3, t3, n3) {
          this.db = r2, this._tx = n3, this.name = e3, this.schema = t3, this.hook = r2._allTables[e3] ? r2._allTables[e3].hook : dt(null, { creating: [Z, G], reading: [H, X], updating: [te, G], deleting: [ee, G] });
        })), this.Transaction = (u2 = this, pt(Lt.prototype, function(e3, t3, n3, r3, i3) {
          var o3 = this;
          this.db = u2, this.mode = e3, this.storeNames = t3, this.schema = n3, this.chromeTransactionDurability = r3, this.idbtrans = null, this.on = dt(this, "complete", "error", "abort"), this.parent = i3 || null, this.active = true, this._reculock = 0, this._blockedFuncs = [], this._resolve = null, this._reject = null, this._waitingFor = null, this._waitingQueue = null, this._spinCount = 0, this._completion = new _e(function(e4, t4) {
            o3._resolve = e4, o3._reject = t4;
          }), this._completion.then(function() {
            o3.active = false, o3.on.complete.fire();
          }, function(e4) {
            var t4 = o3.active;
            return o3.active = false, o3.on.error.fire(e4), o3.parent ? o3.parent._reject(e4) : t4 && o3.idbtrans && o3.idbtrans.abort(), Xe(e4);
          });
        })), this.Version = (i2 = this, pt(dn.prototype, function(e3) {
          this.db = i2, this._cfg = { version: e3, storesSource: null, dbschema: {}, tables: {}, contentUpgrade: null };
        })), this.WhereClause = (s2 = this, pt(Dt.prototype, function(e3, t3, n3) {
          if (this.db = s2, this._ctx = { table: e3, index: ":id" === t3 ? null : t3, or: n3 }, this._cmp = this._ascending = st, this._descending = function(e4, t4) {
            return st(t4, e4);
          }, this._max = function(e4, t4) {
            return 0 < st(e4, t4) ? e4 : t4;
          }, this._min = function(e4, t4) {
            return st(e4, t4) < 0 ? e4 : t4;
          }, this._IDBKeyRange = s2._deps.IDBKeyRange, !this._IDBKeyRange) throw new Y.MissingAPI();
        })), this.on("versionchange", function(e3) {
          0 < e3.newVersion ? console.warn("Another connection wants to upgrade database '".concat(o2.name, "'. Closing db now to resume the upgrade.")) : console.warn("Another connection wants to delete database '".concat(o2.name, "'. Closing db now to resume the delete request.")), o2.close({ disableAutoOpen: false });
        }), this.on("blocked", function(e3) {
          !e3.newVersion || e3.newVersion < e3.oldVersion ? console.warn("Dexie.delete('".concat(o2.name, "') was blocked")) : console.warn("Upgrade '".concat(o2.name, "' blocked by other connection holding version ").concat(e3.oldVersion / 10));
        }), this._maxKey = Yt(t2.IDBKeyRange), this._createTransaction = function(e3, t3, n3, r3) {
          return new o2.Transaction(e3, t3, n3, o2._options.chromeTransactionDurability, r3);
        }, this._fireOnBlocked = function(t3) {
          o2.on("blocked").fire(t3), et.filter(function(e3) {
            return e3.name === o2.name && e3 !== o2 && !e3._state.vcFired;
          }).map(function(e3) {
            return e3.on("versionchange").fire(t3);
          });
        }, this.use(Un), this.use(Jn), this.use(Wn), this.use(Rn), this.use(Nn);
        var l2 = new Proxy(this, { get: function(e3, t3, n3) {
          if ("_vip" === t3) return true;
          if ("table" === t3) return function(e4) {
            return Zn(o2.table(e4), l2);
          };
          var r3 = Reflect.get(e3, t3, n3);
          return r3 instanceof ft ? Zn(r3, l2) : "tables" === t3 ? r3.map(function(e4) {
            return Zn(e4, l2);
          }) : "_createTransaction" === t3 ? function() {
            return Zn(r3.apply(this, arguments), l2);
          } : r3;
        } });
        this.vip = l2, n2.forEach(function(e3) {
          return e3(o2);
        });
      }
      var nr, F = "undefined" != typeof Symbol && "observable" in Symbol ? Symbol.observable : "@@observable", rr = (ir.prototype.subscribe = function(e2, t2, n2) {
        return this._subscribe(e2 && "function" != typeof e2 ? e2 : { next: e2, error: t2, complete: n2 });
      }, ir.prototype[F] = function() {
        return this;
      }, ir);
      function ir(e2) {
        this._subscribe = e2;
      }
      try {
        nr = { indexedDB: f.indexedDB || f.mozIndexedDB || f.webkitIndexedDB || f.msIndexedDB, IDBKeyRange: f.IDBKeyRange || f.webkitIDBKeyRange };
      } catch (e2) {
        nr = { indexedDB: null, IDBKeyRange: null };
      }
      function or(h2) {
        var d2, p2 = false, e2 = new rr(function(r2) {
          var i2 = B(h2);
          var o2, a2 = false, u2 = {}, s2 = {}, e3 = { get closed() {
            return a2;
          }, unsubscribe: function() {
            a2 || (a2 = true, o2 && o2.abort(), c2 && Nt.storagemutated.unsubscribe(f2));
          } };
          r2.start && r2.start(e3);
          var c2 = false, l2 = function() {
            return Ge(t2);
          };
          var f2 = function(e4) {
            Kn(u2, e4), En(s2, u2) && l2();
          }, t2 = function() {
            var t3, n2, e4;
            !a2 && nr.indexedDB && (u2 = {}, t3 = {}, o2 && o2.abort(), o2 = new AbortController(), e4 = (function(e5) {
              var t4 = je();
              try {
                i2 && Le();
                var n3 = Ne(h2, e5);
                return n3 = i2 ? n3.finally(Ue) : n3;
              } finally {
                t4 && Ae();
              }
            })(n2 = { subscr: t3, signal: o2.signal, requery: l2, querier: h2, trans: null }), Promise.resolve(e4).then(function(e5) {
              p2 = true, d2 = e5, a2 || n2.signal.aborted || (u2 = {}, (function(e6) {
                for (var t4 in e6) if (m(e6, t4)) return;
                return 1;
              })(s2 = t3) || c2 || (Nt(Ft, f2), c2 = true), Ge(function() {
                return !a2 && r2.next && r2.next(e5);
              }));
            }, function(e5) {
              p2 = false, ["DatabaseClosedError", "AbortError"].includes(null == e5 ? void 0 : e5.name) || a2 || Ge(function() {
                a2 || r2.error && r2.error(e5);
              });
            }));
          };
          return setTimeout(l2, 0), e3;
        });
        return e2.hasValue = function() {
          return p2;
        }, e2.getValue = function() {
          return d2;
        }, e2;
      }
      var ar = er;
      function ur(e2) {
        var t2 = cr;
        try {
          cr = true, Nt.storagemutated.fire(e2), Tn(e2, true);
        } finally {
          cr = t2;
        }
      }
      r(ar, _(_({}, Q), { delete: function(e2) {
        return new ar(e2, { addons: [] }).delete();
      }, exists: function(e2) {
        return new ar(e2, { addons: [] }).open().then(function(e3) {
          return e3.close(), true;
        }).catch("NoSuchDatabaseError", function() {
          return false;
        });
      }, getDatabaseNames: function(e2) {
        try {
          return t2 = ar.dependencies, n2 = t2.indexedDB, t2 = t2.IDBKeyRange, (vn(n2) ? Promise.resolve(n2.databases()).then(function(e3) {
            return e3.map(function(e4) {
              return e4.name;
            }).filter(function(e4) {
              return e4 !== tt;
            });
          }) : yn(n2, t2).toCollection().primaryKeys()).then(e2);
        } catch (e3) {
          return Xe(new Y.MissingAPI());
        }
        var t2, n2;
      }, defineClass: function() {
        return function(e2) {
          a(this, e2);
        };
      }, ignoreTransaction: function(e2) {
        return me.trans ? $e(me.transless, e2) : e2();
      }, vip: mn, async: function(t2) {
        return function() {
          try {
            var e2 = In(t2.apply(this, arguments));
            return e2 && "function" == typeof e2.then ? e2 : _e.resolve(e2);
          } catch (e3) {
            return Xe(e3);
          }
        };
      }, spawn: function(e2, t2, n2) {
        try {
          var r2 = In(e2.apply(n2, t2 || []));
          return r2 && "function" == typeof r2.then ? r2 : _e.resolve(r2);
        } catch (e3) {
          return Xe(e3);
        }
      }, currentTransaction: { get: function() {
        return me.trans || null;
      } }, waitFor: function(e2, t2) {
        t2 = _e.resolve("function" == typeof e2 ? ar.ignoreTransaction(e2) : e2).timeout(t2 || 6e4);
        return me.trans ? me.trans.waitFor(t2) : t2;
      }, Promise: _e, debug: { get: function() {
        return ie;
      }, set: function(e2) {
        oe(e2);
      } }, derive: o, extend: a, props: r, override: p, Events: dt, on: Nt, liveQuery: or, extendObservabilitySet: Kn, getByKeyPath: O, setByKeyPath: P, delByKeyPath: function(t2, e2) {
        "string" == typeof e2 ? P(t2, e2, void 0) : "length" in e2 && [].map.call(e2, function(e3) {
          P(t2, e3, void 0);
        });
      }, shallowClone: g, deepClone: S, getObjectDiff: Fn, cmp: st, asap: v, minKey: -1 / 0, addons: [], connections: et, errnames: z, dependencies: nr, cache: Sn, semVer: "4.0.11", version: "4.0.11".split(".").map(function(e2) {
        return parseInt(e2);
      }).reduce(function(e2, t2, n2) {
        return e2 + t2 / Math.pow(10, 2 * n2);
      }) })), ar.maxKey = Yt(ar.dependencies.IDBKeyRange), "undefined" != typeof dispatchEvent && "undefined" != typeof addEventListener && (Nt(Ft, function(e2) {
        cr || (e2 = new CustomEvent(Mt, { detail: e2 }), cr = true, dispatchEvent(e2), cr = false);
      }), addEventListener(Mt, function(e2) {
        e2 = e2.detail;
        cr || ur(e2);
      }));
      var sr, cr = false, lr = function() {
      };
      return "undefined" != typeof BroadcastChannel && ((lr = function() {
        (sr = new BroadcastChannel(Mt)).onmessage = function(e2) {
          return e2.data && ur(e2.data);
        };
      })(), "function" == typeof sr.unref && sr.unref(), Nt(Ft, function(e2) {
        cr || sr.postMessage(e2);
      })), "undefined" != typeof addEventListener && (addEventListener("pagehide", function(e2) {
        if (!er.disableBfCache && e2.persisted) {
          ie && console.debug("Dexie: handling persisted pagehide"), null != sr && sr.close();
          for (var t2 = 0, n2 = et; t2 < n2.length; t2++) n2[t2].close({ disableAutoOpen: false });
        }
      }), addEventListener("pageshow", function(e2) {
        !er.disableBfCache && e2.persisted && (ie && console.debug("Dexie: handling persisted pageshow"), lr(), ur({ all: new gn(-1 / 0, [[]]) }));
      })), _e.rejectionMapper = function(e2, t2) {
        return !e2 || e2 instanceof N || e2 instanceof TypeError || e2 instanceof SyntaxError || !e2.name || !$[e2.name] ? e2 : (t2 = new $[e2.name](t2 || e2.message, e2), "stack" in e2 && l(t2, "stack", { get: function() {
          return this.inner.stack;
        } }), t2);
      }, oe(ie), _(er, Object.freeze({ __proto__: null, Dexie: er, liveQuery: or, Entity: ut, cmp: st, PropModification: xt, replacePrefix: function(e2, t2) {
        return new xt({ replacePrefix: [e2, t2] });
      }, add: function(e2) {
        return new xt({ add: e2 });
      }, remove: function(e2) {
        return new xt({ remove: e2 });
      }, default: er, RangeSet: gn, mergeRanges: _n, rangesOverlap: xn }), { default: er }), er;
    });
  })(dexie_min$1);
  return dexie_min$1.exports;
}
var dexie_minExports = requireDexie_min();
var _Dexie = getDefaultExportFromCjs(dexie_minExports);
var DexieSymbol = Symbol.for("Dexie");
var Dexie = globalThis[DexieSymbol] || (globalThis[DexieSymbol] = _Dexie);
if (_Dexie.semVer !== Dexie.semVer) {
  throw new Error(`Two different versions of Dexie loaded in the same app: ${_Dexie.semVer} and ${Dexie.semVer}`);
}
var {
  liveQuery,
  mergeRanges,
  rangesOverlap,
  RangeSet,
  cmp,
  Entity,
  PropModification,
  replacePrefix,
  add,
  remove
} = Dexie;
function typeOf(value) {
  if (value === null) {
    return "null";
  }
  if (value !== Object(value)) {
    return typeof value;
  }
  return {}.toString.call(value).slice(8, -1).toLowerCase();
}
function isEmpty(input) {
  if (typeOf(input) !== "string") {
    return true;
  }
  return !input.length;
}
function isCharInRange(char = "", start, end) {
  if (isEmpty(char))
    return false;
  const code = char.charCodeAt(0);
  return start <= code && code <= end;
}
var TO_KANA_METHODS = {
  HIRAGANA: "toHiragana",
  KATAKANA: "toKatakana"
};
var ROMANIZATIONS = {
  HEPBURN: "hepburn"
};
var DEFAULT_OPTIONS = {
  useObsoleteKana: false,
  passRomaji: false,
  convertLongVowelMark: true,
  upcaseKatakana: false,
  IMEMode: false,
  romanization: ROMANIZATIONS.HEPBURN
};
var LATIN_UPPERCASE_START = 65;
var LATIN_UPPERCASE_END = 90;
var HIRAGANA_START = 12353;
var HIRAGANA_END = 12438;
var KATAKANA_START = 12449;
var PROLONGED_SOUND_MARK = 12540;
var KANA_SLASH_DOT = 12539;
var MODERN_ENGLISH = [0, 127];
var HEPBURN_MACRON_RANGES = [
  [256, 257],
  [274, 275],
  [298, 299],
  [332, 333],
  [362, 363]
  // Ū ū
];
var ROMAJI_RANGES = [MODERN_ENGLISH, ...HEPBURN_MACRON_RANGES];
var safeIsNaN = Number.isNaN || function ponyfill(value) {
  return typeof value === "number" && value !== value;
};
function isEqual(first, second) {
  if (first === second) {
    return true;
  }
  if (safeIsNaN(first) && safeIsNaN(second)) {
    return true;
  }
  return false;
}
function areInputsEqual(newInputs, lastInputs) {
  if (newInputs.length !== lastInputs.length) {
    return false;
  }
  for (var i = 0; i < newInputs.length; i++) {
    if (!isEqual(newInputs[i], lastInputs[i])) {
      return false;
    }
  }
  return true;
}
function memoizeOne(resultFn, isEqual2) {
  if (isEqual2 === void 0) {
    isEqual2 = areInputsEqual;
  }
  var cache = null;
  function memoized() {
    var newArgs = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      newArgs[_i] = arguments[_i];
    }
    if (cache && cache.lastThis === this && isEqual2(newArgs, cache.lastArgs)) {
      return cache.lastResult;
    }
    var lastResult = resultFn.apply(this, newArgs);
    cache = {
      lastResult,
      lastArgs: newArgs,
      lastThis: this
    };
    return lastResult;
  }
  memoized.clear = function clear() {
    cache = null;
  };
  return memoized;
}
var has = Object.prototype.hasOwnProperty;
function find(iter, tar, key) {
  for (key of iter.keys()) {
    if (dequal(key, tar))
      return key;
  }
}
function dequal(foo, bar) {
  var ctor, len, tmp;
  if (foo === bar)
    return true;
  if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
    if (ctor === Date)
      return foo.getTime() === bar.getTime();
    if (ctor === RegExp)
      return foo.toString() === bar.toString();
    if (ctor === Array) {
      if ((len = foo.length) === bar.length) {
        while (len-- && dequal(foo[len], bar[len]))
          ;
      }
      return len === -1;
    }
    if (ctor === Set) {
      if (foo.size !== bar.size) {
        return false;
      }
      for (len of foo) {
        tmp = len;
        if (tmp && typeof tmp === "object") {
          tmp = find(bar, tmp);
          if (!tmp)
            return false;
        }
        if (!bar.has(tmp))
          return false;
      }
      return true;
    }
    if (ctor === Map) {
      if (foo.size !== bar.size) {
        return false;
      }
      for (len of foo) {
        tmp = len[0];
        if (tmp && typeof tmp === "object") {
          tmp = find(bar, tmp);
          if (!tmp)
            return false;
        }
        if (!dequal(len[1], bar.get(tmp))) {
          return false;
        }
      }
      return true;
    }
    if (ctor === ArrayBuffer) {
      foo = new Uint8Array(foo);
      bar = new Uint8Array(bar);
    } else if (ctor === DataView) {
      if ((len = foo.byteLength) === bar.byteLength) {
        while (len-- && foo.getInt8(len) === bar.getInt8(len))
          ;
      }
      return len === -1;
    }
    if (ArrayBuffer.isView(foo)) {
      if ((len = foo.byteLength) === bar.byteLength) {
        while (len-- && foo[len] === bar[len])
          ;
      }
      return len === -1;
    }
    if (!ctor || typeof foo === "object") {
      len = 0;
      for (ctor in foo) {
        if (has.call(foo, ctor) && ++len && !has.call(bar, ctor))
          return false;
        if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor]))
          return false;
      }
      return Object.keys(bar).length === len;
    }
  }
  return foo !== foo && bar !== bar;
}
var mergeWithDefaultOptions = (opts = {}) => Object.assign({}, DEFAULT_OPTIONS, opts);
function applyMapping(string, mapping, convertEnding) {
  const root = mapping;
  function nextSubtree(tree, nextChar) {
    const subtree = tree[nextChar];
    if (subtree === void 0) {
      return void 0;
    }
    return Object.assign({ "": tree[""] + nextChar }, tree[nextChar]);
  }
  function newChunk(remaining, currentCursor) {
    const firstChar = remaining.charAt(0);
    return parse(Object.assign({ "": firstChar }, root[firstChar]), remaining.slice(1), currentCursor, currentCursor + 1);
  }
  function parse(tree, remaining, lastCursor, currentCursor) {
    if (!remaining) {
      if (convertEnding || Object.keys(tree).length === 1) {
        return tree[""] ? [[lastCursor, currentCursor, tree[""]]] : [];
      }
      return [[lastCursor, currentCursor, null]];
    }
    if (Object.keys(tree).length === 1) {
      return [[lastCursor, currentCursor, tree[""]]].concat(newChunk(remaining, currentCursor));
    }
    const subtree = nextSubtree(tree, remaining.charAt(0));
    if (subtree === void 0) {
      return [[lastCursor, currentCursor, tree[""]]].concat(newChunk(remaining, currentCursor));
    }
    return parse(subtree, remaining.slice(1), lastCursor, currentCursor + 1);
  }
  return newChunk(string, 0);
}
function transform(tree) {
  return Object.entries(tree).reduce((map, [char, subtree]) => {
    const endOfBranch = typeOf(subtree) === "string";
    map[char] = endOfBranch ? { "": subtree } : transform(subtree);
    return map;
  }, {});
}
function getSubTreeOf(tree, string) {
  return string.split("").reduce((correctSubTree, char) => {
    if (correctSubTree[char] === void 0) {
      correctSubTree[char] = {};
    }
    return correctSubTree[char];
  }, tree);
}
function createCustomMapping(customMap = {}) {
  const customTree = {};
  if (typeOf(customMap) === "object") {
    Object.entries(customMap).forEach(([roma, kana]) => {
      let subTree = customTree;
      roma.split("").forEach((char) => {
        if (subTree[char] === void 0) {
          subTree[char] = {};
        }
        subTree = subTree[char];
      });
      subTree[""] = kana;
    });
  }
  return function makeMap(map) {
    const mapCopy = JSON.parse(JSON.stringify(map));
    function transformMap(mapSubtree, customSubtree) {
      if (mapSubtree === void 0 || typeOf(mapSubtree) === "string") {
        return customSubtree;
      }
      return Object.entries(customSubtree).reduce((newSubtree, [char, subtree]) => {
        newSubtree[char] = transformMap(mapSubtree[char], subtree);
        return newSubtree;
      }, mapSubtree);
    }
    return transformMap(mapCopy, customTree);
  };
}
function mergeCustomMapping(map, customMapping) {
  if (!customMapping) {
    return map;
  }
  return typeOf(customMapping) === "function" ? customMapping(map) : createCustomMapping(customMapping)(map);
}
var BASIC_KUNREI = {
  a: "あ",
  i: "い",
  u: "う",
  e: "え",
  o: "お",
  k: { a: "か", i: "き", u: "く", e: "け", o: "こ" },
  s: { a: "さ", i: "し", u: "す", e: "せ", o: "そ" },
  t: { a: "た", i: "ち", u: "つ", e: "て", o: "と" },
  n: { a: "な", i: "に", u: "ぬ", e: "ね", o: "の" },
  h: { a: "は", i: "ひ", u: "ふ", e: "へ", o: "ほ" },
  m: { a: "ま", i: "み", u: "む", e: "め", o: "も" },
  y: { a: "や", u: "ゆ", o: "よ" },
  r: { a: "ら", i: "り", u: "る", e: "れ", o: "ろ" },
  w: { a: "わ", i: "ゐ", e: "ゑ", o: "を" },
  g: { a: "が", i: "ぎ", u: "ぐ", e: "げ", o: "ご" },
  z: { a: "ざ", i: "じ", u: "ず", e: "ぜ", o: "ぞ" },
  d: { a: "だ", i: "ぢ", u: "づ", e: "で", o: "ど" },
  b: { a: "ば", i: "び", u: "ぶ", e: "べ", o: "ぼ" },
  p: { a: "ぱ", i: "ぴ", u: "ぷ", e: "ぺ", o: "ぽ" },
  v: { a: "ゔぁ", i: "ゔぃ", u: "ゔ", e: "ゔぇ", o: "ゔぉ" }
};
var SPECIAL_SYMBOLS$1 = {
  ".": "。",
  ",": "、",
  ":": "：",
  "/": "・",
  "!": "！",
  "?": "？",
  "~": "〜",
  "-": "ー",
  "‘": "「",
  "’": "」",
  "“": "『",
  "”": "』",
  "[": "［",
  "]": "］",
  "(": "（",
  ")": "）",
  "{": "｛",
  "}": "｝"
};
var CONSONANTS = {
  k: "き",
  s: "し",
  t: "ち",
  n: "に",
  h: "ひ",
  m: "み",
  r: "り",
  g: "ぎ",
  z: "じ",
  d: "ぢ",
  b: "び",
  p: "ぴ",
  v: "ゔ",
  q: "く",
  f: "ふ"
};
var SMALL_Y$1 = { ya: "ゃ", yi: "ぃ", yu: "ゅ", ye: "ぇ", yo: "ょ" };
var SMALL_VOWELS = { a: "ぁ", i: "ぃ", u: "ぅ", e: "ぇ", o: "ぉ" };
var ALIASES = {
  sh: "sy",
  ch: "ty",
  cy: "ty",
  chy: "ty",
  shy: "sy",
  j: "zy",
  jy: "zy",
  // exceptions to above rules
  shi: "si",
  chi: "ti",
  tsu: "tu",
  ji: "zi",
  fu: "hu"
};
var SMALL_LETTERS = Object.assign({
  tu: "っ",
  wa: "ゎ",
  ka: "ヵ",
  ke: "ヶ"
}, SMALL_VOWELS, SMALL_Y$1);
var SPECIAL_CASES = {
  yi: "い",
  wu: "う",
  ye: "いぇ",
  wi: "うぃ",
  we: "うぇ",
  kwa: "くぁ",
  whu: "う",
  // because it's not thya for てゃ but tha
  // and tha is not てぁ, but てゃ
  tha: "てゃ",
  thu: "てゅ",
  tho: "てょ",
  dha: "でゃ",
  dhu: "でゅ",
  dho: "でょ"
};
var AIUEO_CONSTRUCTIONS = {
  wh: "う",
  kw: "く",
  qw: "く",
  q: "く",
  gw: "ぐ",
  sw: "す",
  ts: "つ",
  th: "て",
  tw: "と",
  dh: "で",
  dw: "ど",
  fw: "ふ",
  f: "ふ"
};
function createRomajiToKanaMap$1() {
  const kanaTree = transform(BASIC_KUNREI);
  const subtreeOf = (string) => getSubTreeOf(kanaTree, string);
  Object.entries(CONSONANTS).forEach(([consonant, yKana]) => {
    Object.entries(SMALL_Y$1).forEach(([roma, kana]) => {
      subtreeOf(consonant + roma)[""] = yKana + kana;
    });
  });
  Object.entries(SPECIAL_SYMBOLS$1).forEach(([symbol, jsymbol]) => {
    subtreeOf(symbol)[""] = jsymbol;
  });
  Object.entries(AIUEO_CONSTRUCTIONS).forEach(([consonant, aiueoKana]) => {
    Object.entries(SMALL_VOWELS).forEach(([vowel, kana]) => {
      const subtree = subtreeOf(consonant + vowel);
      subtree[""] = aiueoKana + kana;
    });
  });
  ["n", "n'", "xn"].forEach((nChar) => {
    subtreeOf(nChar)[""] = "ん";
  });
  kanaTree.c = JSON.parse(JSON.stringify(kanaTree.k));
  Object.entries(ALIASES).forEach(([string, alternative]) => {
    const allExceptLast = string.slice(0, string.length - 1);
    const last = string.charAt(string.length - 1);
    const parentTree = subtreeOf(allExceptLast);
    parentTree[last] = JSON.parse(JSON.stringify(subtreeOf(alternative)));
  });
  function getAlternatives(string) {
    return [...Object.entries(ALIASES), ...[["c", "k"]]].reduce((list, [alt, roma]) => string.startsWith(roma) ? list.concat(string.replace(roma, alt)) : list, []);
  }
  Object.entries(SMALL_LETTERS).forEach(([kunreiRoma, kana]) => {
    const last = (char) => char.charAt(char.length - 1);
    const allExceptLast = (chars) => chars.slice(0, chars.length - 1);
    const xRoma = `x${kunreiRoma}`;
    const xSubtree = subtreeOf(xRoma);
    xSubtree[""] = kana;
    const parentTree = subtreeOf(`l${allExceptLast(kunreiRoma)}`);
    parentTree[last(kunreiRoma)] = xSubtree;
    getAlternatives(kunreiRoma).forEach((altRoma) => {
      ["l", "x"].forEach((prefix) => {
        const altParentTree = subtreeOf(prefix + allExceptLast(altRoma));
        altParentTree[last(altRoma)] = subtreeOf(prefix + kunreiRoma);
      });
    });
  });
  Object.entries(SPECIAL_CASES).forEach(([string, kana]) => {
    subtreeOf(string)[""] = kana;
  });
  function addTsu(tree) {
    return Object.entries(tree).reduce((tsuTree, [key, value]) => {
      if (!key) {
        tsuTree[key] = `っ${value}`;
      } else {
        tsuTree[key] = addTsu(value);
      }
      return tsuTree;
    }, {});
  }
  [...Object.keys(CONSONANTS), "c", "y", "w", "j"].forEach((consonant) => {
    const subtree = kanaTree[consonant];
    subtree[consonant] = addTsu(subtree);
  });
  delete kanaTree.n.n;
  return Object.freeze(JSON.parse(JSON.stringify(kanaTree)));
}
var romajiToKanaMap = null;
function getRomajiToKanaTree() {
  if (romajiToKanaMap == null) {
    romajiToKanaMap = createRomajiToKanaMap$1();
  }
  return romajiToKanaMap;
}
var USE_OBSOLETE_KANA_MAP = createCustomMapping({
  wi: "ゐ",
  we: "ゑ"
});
function IME_MODE_MAP(map) {
  const mapCopy = JSON.parse(JSON.stringify(map));
  mapCopy.n.n = { "": "ん" };
  mapCopy.n[" "] = { "": "ん" };
  return mapCopy;
}
function isCharUpperCase(char = "") {
  if (isEmpty(char))
    return false;
  return isCharInRange(char, LATIN_UPPERCASE_START, LATIN_UPPERCASE_END);
}
function isCharLongDash(char = "") {
  if (isEmpty(char))
    return false;
  return char.charCodeAt(0) === PROLONGED_SOUND_MARK;
}
function isCharSlashDot(char = "") {
  if (isEmpty(char))
    return false;
  return char.charCodeAt(0) === KANA_SLASH_DOT;
}
function isCharHiragana(char = "") {
  if (isEmpty(char))
    return false;
  if (isCharLongDash(char))
    return true;
  return isCharInRange(char, HIRAGANA_START, HIRAGANA_END);
}
function hiraganaToKatakana(input = "") {
  const kata = [];
  input.split("").forEach((char) => {
    if (isCharLongDash(char) || isCharSlashDot(char)) {
      kata.push(char);
    } else if (isCharHiragana(char)) {
      const code = char.charCodeAt(0) + (KATAKANA_START - HIRAGANA_START);
      const kataChar = String.fromCharCode(code);
      kata.push(kataChar);
    } else {
      kata.push(char);
    }
  });
  return kata.join("");
}
var createRomajiToKanaMap = memoizeOne((IMEMode, useObsoleteKana, customKanaMapping) => {
  let map = getRomajiToKanaTree();
  map = IMEMode ? IME_MODE_MAP(map) : map;
  map = useObsoleteKana ? USE_OBSOLETE_KANA_MAP(map) : map;
  if (customKanaMapping) {
    map = mergeCustomMapping(map, customKanaMapping);
  }
  return map;
}, dequal);
function toKana(input = "", options = {}, map) {
  let config;
  if (!map) {
    config = mergeWithDefaultOptions(options);
    map = createRomajiToKanaMap(config.IMEMode, config.useObsoleteKana, config.customKanaMapping);
  } else {
    config = options;
  }
  return splitIntoConvertedKana(input, config, map).map((kanaToken) => {
    const [start, end, kana] = kanaToken;
    if (kana === null) {
      return input.slice(start);
    }
    const enforceHiragana = config.IMEMode === TO_KANA_METHODS.HIRAGANA;
    const enforceKatakana = config.IMEMode === TO_KANA_METHODS.KATAKANA || [...input.slice(start, end)].every(isCharUpperCase);
    return enforceHiragana || !enforceKatakana ? kana : hiraganaToKatakana(kana);
  }).join("");
}
function splitIntoConvertedKana(input = "", options = {}, map) {
  const { IMEMode, useObsoleteKana, customKanaMapping } = options;
  if (!map) {
    map = createRomajiToKanaMap(IMEMode, useObsoleteKana, customKanaMapping);
  }
  return applyMapping(input.toLowerCase(), map, !IMEMode);
}
function isCharRomaji(char = "") {
  if (isEmpty(char))
    return false;
  return ROMAJI_RANGES.some(([start, end]) => isCharInRange(char, start, end));
}
function isRomaji(input = "", allowed) {
  const augmented = typeOf(allowed) === "regexp";
  return isEmpty(input) ? false : [...input].every((char) => {
    const isRoma = isCharRomaji(char);
    return !augmented ? isRoma : isRoma || allowed.test(char);
  });
}
var kanaToHepburnMap = null;
var BASIC_ROMAJI = {
  あ: "a",
  い: "i",
  う: "u",
  え: "e",
  お: "o",
  か: "ka",
  き: "ki",
  く: "ku",
  け: "ke",
  こ: "ko",
  さ: "sa",
  し: "shi",
  す: "su",
  せ: "se",
  そ: "so",
  た: "ta",
  ち: "chi",
  つ: "tsu",
  て: "te",
  と: "to",
  な: "na",
  に: "ni",
  ぬ: "nu",
  ね: "ne",
  の: "no",
  は: "ha",
  ひ: "hi",
  ふ: "fu",
  へ: "he",
  ほ: "ho",
  ま: "ma",
  み: "mi",
  む: "mu",
  め: "me",
  も: "mo",
  ら: "ra",
  り: "ri",
  る: "ru",
  れ: "re",
  ろ: "ro",
  や: "ya",
  ゆ: "yu",
  よ: "yo",
  わ: "wa",
  ゐ: "wi",
  ゑ: "we",
  を: "wo",
  ん: "n",
  が: "ga",
  ぎ: "gi",
  ぐ: "gu",
  げ: "ge",
  ご: "go",
  ざ: "za",
  じ: "ji",
  ず: "zu",
  ぜ: "ze",
  ぞ: "zo",
  だ: "da",
  ぢ: "ji",
  づ: "zu",
  で: "de",
  ど: "do",
  ば: "ba",
  び: "bi",
  ぶ: "bu",
  べ: "be",
  ぼ: "bo",
  ぱ: "pa",
  ぴ: "pi",
  ぷ: "pu",
  ぺ: "pe",
  ぽ: "po",
  ゔぁ: "va",
  ゔぃ: "vi",
  ゔ: "vu",
  ゔぇ: "ve",
  ゔぉ: "vo"
};
var SPECIAL_SYMBOLS = {
  "。": ".",
  "、": ",",
  "：": ":",
  "・": "/",
  "！": "!",
  "？": "?",
  "〜": "~",
  "ー": "-",
  "「": "‘",
  "」": "’",
  "『": "“",
  "』": "”",
  "［": "[",
  "］": "]",
  "（": "(",
  "）": ")",
  "｛": "{",
  "｝": "}",
  "　": " "
};
var AMBIGUOUS_VOWELS = ["あ", "い", "う", "え", "お", "や", "ゆ", "よ"];
var SMALL_Y = { ゃ: "ya", ゅ: "yu", ょ: "yo" };
var SMALL_Y_EXTRA = { ぃ: "yi", ぇ: "ye" };
var SMALL_AIUEO = {
  ぁ: "a",
  ぃ: "i",
  ぅ: "u",
  ぇ: "e",
  ぉ: "o"
};
var YOON_KANA = [
  "き",
  "に",
  "ひ",
  "み",
  "り",
  "ぎ",
  "び",
  "ぴ",
  "ゔ",
  "く",
  "ふ"
];
var YOON_EXCEPTIONS = {
  し: "sh",
  ち: "ch",
  じ: "j",
  ぢ: "j"
};
var SMALL_KANA = {
  っ: "",
  ゃ: "ya",
  ゅ: "yu",
  ょ: "yo",
  ぁ: "a",
  ぃ: "i",
  ぅ: "u",
  ぇ: "e",
  ぉ: "o"
};
var SOKUON_WHITELIST = {
  b: "b",
  c: "t",
  d: "d",
  f: "f",
  g: "g",
  h: "h",
  j: "j",
  k: "k",
  m: "m",
  p: "p",
  q: "q",
  r: "r",
  s: "s",
  t: "t",
  v: "v",
  w: "w",
  x: "x",
  z: "z"
};
function getKanaToHepburnTree() {
  if (kanaToHepburnMap == null) {
    kanaToHepburnMap = createKanaToHepburnMap();
  }
  return kanaToHepburnMap;
}
function getKanaToRomajiTree(romanization) {
  switch (romanization) {
    case ROMANIZATIONS.HEPBURN:
      return getKanaToHepburnTree();
    default:
      return {};
  }
}
function createKanaToHepburnMap() {
  const romajiTree = transform(BASIC_ROMAJI);
  const subtreeOf = (string) => getSubTreeOf(romajiTree, string);
  const setTrans = (string, transliteration) => {
    subtreeOf(string)[""] = transliteration;
  };
  Object.entries(SPECIAL_SYMBOLS).forEach(([jsymbol, symbol]) => {
    subtreeOf(jsymbol)[""] = symbol;
  });
  [...Object.entries(SMALL_Y), ...Object.entries(SMALL_AIUEO)].forEach(([roma, kana]) => {
    setTrans(roma, kana);
  });
  YOON_KANA.forEach((kana) => {
    const firstRomajiChar = subtreeOf(kana)[""][0];
    Object.entries(SMALL_Y).forEach(([yKana, yRoma]) => {
      setTrans(kana + yKana, firstRomajiChar + yRoma);
    });
    Object.entries(SMALL_Y_EXTRA).forEach(([yKana, yRoma]) => {
      setTrans(kana + yKana, firstRomajiChar + yRoma);
    });
  });
  Object.entries(YOON_EXCEPTIONS).forEach(([kana, roma]) => {
    Object.entries(SMALL_Y).forEach(([yKana, yRoma]) => {
      setTrans(kana + yKana, roma + yRoma[1]);
    });
    setTrans(`${kana}ぃ`, `${roma}yi`);
    setTrans(`${kana}ぇ`, `${roma}e`);
  });
  romajiTree["っ"] = resolveTsu(romajiTree);
  Object.entries(SMALL_KANA).forEach(([kana, roma]) => {
    setTrans(kana, roma);
  });
  AMBIGUOUS_VOWELS.forEach((kana) => {
    setTrans(`ん${kana}`, `n'${subtreeOf(kana)[""]}`);
  });
  return Object.freeze(JSON.parse(JSON.stringify(romajiTree)));
}
function resolveTsu(tree) {
  return Object.entries(tree).reduce((tsuTree, [key, value]) => {
    if (!key) {
      const consonant = value.charAt(0);
      tsuTree[key] = Object.keys(SOKUON_WHITELIST).includes(consonant) ? SOKUON_WHITELIST[consonant] + value : value;
    } else {
      tsuTree[key] = resolveTsu(value);
    }
    return tsuTree;
  }, {});
}
memoizeOne((romanization, customRomajiMapping) => {
  let map = getKanaToRomajiTree(romanization);
  if (customRomajiMapping) {
    map = mergeCustomMapping(map, customRomajiMapping);
  }
  return map;
}, dequal);
var HooksManager = class {
  constructor(db, events, hooks) {
    if (!db) throw new Error("Database instance is required for HooksManager");
    if (!db.materials || !db.assignments || !db.reviews) throw new Error("Database must have materials, assignments, and reviews tables for HooksManager");
    if (!events) throw new Error("Event emitter instance is required for HooksManager");
    if (!events.emit || typeof events.emit !== "function") throw new Error("Event emitter must have an emit method");
    this.db = db;
    this.events = events;
    this.hooks = {
      reading: (hooks == null ? void 0 : hooks.reading) || true,
      updating: (hooks == null ? void 0 : hooks.updating) || true,
      deleting: (hooks == null ? void 0 : hooks.deleting) || true
    };
  }
  /**
   * Registers the hooks for reading, updating, and deleting operations.
   */
  register() {
    if (this.hooks.reading) this._readingHooks();
    if (this.hooks.updating) this._updatingHooks();
    if (this.hooks.deleting) this._deletingHooks();
  }
  /**
   * Registers hooks for reading operations on materials, assignments, and reviews.
   * 
   * @private
   * @fires WanikaniAPIManager#get:material
   * @fires WanikaniAPIManager#get:assignment
   * @fires WanikaniAPIManager#get:review
   */
  _readingHooks() {
    this.db.materials.hook("reading", (object) => {
      this.events.emit("get:material", object);
      return object;
    });
    this.db.assignments.hook("reading", (object) => {
      this.events.emit("get:assignment", object);
      return object;
    });
    this.db.reviews.hook("reading", (object) => {
      this.events.emit("get:review", object);
      return object;
    });
  }
  /**
   * Registers hooks for updating operations on materials, assignments, and reviews.
   * @private
   * @fires WanikaniAPIManager#update:material
   * @fires WanikaniAPIManager#update:assignment
   * @fires WanikaniAPIManager#update:review
   */
  _updatingHooks() {
    this.db.materials.hook("creating", (_, object) => {
      this.events.emit("update:material", object, null);
    });
    this.db.materials.hook("updating", (modifications, _, object) => {
      this.events.emit("update:material", object, modifications);
    });
    this.db.assignments.hook("creating", (_, object) => {
      this.events.emit("update:assignment", object, null);
    });
    this.db.assignments.hook("updating", (modifications, _, object) => {
      this.events.emit("update:assignment", object, modifications);
    });
    this.db.reviews.hook("creating", (_, object) => {
      this.events.emit("update:review", object, null);
    });
    this.db.reviews.hook("updating", (modifications, _, object) => {
      this.events.emit("update:review", object, modifications);
    });
  }
  /**
   * Registers hooks for deleting operations on materials, assignments, and reviews.
   * @private
   * @fires WanikaniAPIManager#delete:material
   * @fires WanikaniAPIManager#delete:assignment
   * @fires WanikaniAPIManager#delete:review
   */
  _deletingHooks() {
    this.db.materials.hook("deleting", (_, object) => {
      this.events.emit("delete:material", object);
    });
    this.db.assignments.hook("deleting", (_, object) => {
      this.events.emit("delete:assignment", object);
    });
    this.db.reviews.hook("deleting", (_, object) => {
      this.events.emit("delete:review", object);
    });
  }
};
var StorageManager = class _StorageManager {
  constructor(events, config) {
    this.events = events;
    this.config = config || {};
    this.subjects = new Dexie("WaniKaniAPIManager");
    this.subjects.version(1).stores({
      materials: "id, level, type, characters, *meaningsList, *readingsList, *charactersList",
      assignments: "id, subject_id, srs_stage, subject_type",
      reviews: "id, subject_id, subject_type",
      cacheById: "[id+source], timestamp",
      cacheByTypeAndLevel: "[type+level+source], timestamp",
      requests: "++id, timestamp, endpoint, responseSize"
    });
    this.STORAGE_KEY_PREFIX = "wanikani_api_manager_";
    if (this.events) {
      this.hooksManager = new HooksManager(this.subjects, this.events);
      this.hooksManager.register();
    }
    this.CORS_PROXY_URL = (config == null ? void 0 : config.proxy) || void 0 || null;
  }
  /**
   * Set a fetch timestamp for a specific endpoint in localStorage.
   * 
   * @param {string} endpoint - The endpoint for which to set the timestamp.
   * @param {Date} timestamp - The timestamp to set.
   * @returns {void}
   */
  setFetchTimestamp(endpoint, timestamp) {
    timestamp = timestamp.toUTCString();
    localStorage.setItem(this.STORAGE_KEY_PREFIX + endpoint + "_timestamp", timestamp);
  }
  /**
   * Get the fetch timestamp for a specific endpoint from localStorage.
   * 
   * @param {string} endpoint - The endpoint for which to get the timestamp.
   * @returns {string|null} The timestamp in UTC string format or null if not set.
   */
  getFetchTimestamp(endpoint) {
    const timestamp = localStorage.getItem(this.STORAGE_KEY_PREFIX + endpoint + "_timestamp");
    return timestamp ? new Date(timestamp).toUTCString() : null;
  }
  async setCacheForIDs(ids, timestamp, source) {
    timestamp = timestamp.toUTCString();
    const assignments = await this.subjects.assignments.where("subject_id").anyOf(ids).toArray();
    const records = ids.map((id) => {
      const assignment = assignments.find((a) => a.subject_id === id);
      if (!assignment) return { id, source, timestamp: null };
      const cacheInterval = assignment && assignment.available_at && new Date(assignment.available_at) > new Date(timestamp) ? new Date(assignment.available_at).toUTCString() : timestamp;
      return { id, source, timestamp: cacheInterval };
    });
    try {
      await this.subjects.cacheById.bulkPut(records);
    } catch (error) {
      console.error("[StorageManager] Error setting fetch timestamp IDs:", error);
    }
  }
  async checkCacheForIDs(ids, source) {
    ids = Array.isArray(ids) ? ids : [ids];
    try {
      const timestamps = await this.subjects.cacheById.where("[id+source]").anyOf(ids.map((id) => [id, source])).toArray();
      if (!timestamps.length) return { itemsToFetch: ids, itemsToBypassCacheInterval: [], lastUpdated: null };
      let itemsToFetch = [], itemsToBypassCacheInterval = [];
      let earliestTimestamp = null;
      const currentDate = /* @__PURE__ */ new Date();
      ids.forEach((id) => {
        const record = timestamps.find((r) => r.id === id && r.source === source);
        if (!record) itemsToBypassCacheInterval.push(id);
        if (record && !record.timestamp) return;
        if (record == null ? void 0 : record.timestamp) {
          if (new Date(record.timestamp) < currentDate) {
            itemsToFetch.push(id);
            if (!earliestTimestamp || new Date(record.timestamp) < new Date(earliestTimestamp)) {
              earliestTimestamp = record.timestamp;
            }
          }
        }
      });
      return {
        itemsToFetch,
        itemsToBypassCacheInterval,
        lastUpdated: earliestTimestamp ? new Date(earliestTimestamp).toUTCString() : null
      };
    } catch (error) {
      console.error("[StorageManager] Error getting fetch timestamp IDs:", error);
      return { itemsToFetch: [], itemsToBypassCacheInterval: [], lastUpdated: null };
    }
  }
  async setCacheForTypeAndLevel(type, levels, timestamp, source) {
    type = type || "all";
    timestamp = timestamp.toUTCString();
    let materials;
    if (type === "all") materials = await this.getMaterialsByLevel(levels);
    else materials = await this.getMaterialsByTypeAndLevel(type, levels);
    if (!materials || !materials.length) return;
    const ids = materials.map((m) => m.id);
    const assignments = await this.getAssignmentsBySubjectId(ids);
    if (!assignments || !assignments.length) return;
    let records = levels.map((level) => {
      return { type, level, source, timestamp: null };
    });
    materials.forEach((material) => {
      const level = material.level;
      const target = records.find((r) => r.type === type && r.level === level && r.source === source);
      const assignment = assignments.find((a) => a.subject_id === material.id);
      if (!assignment) {
        target.timestamp = null;
        return;
      }
      const currentDate = /* @__PURE__ */ new Date();
      const availableAt = assignment.available_at ? new Date(assignment.available_at) : null;
      if (availableAt && availableAt > currentDate) {
        if (!timestamp) {
          target.timestamp = availableAt.toUTCString();
        } else {
          target.timestamp = new Date(Math.min(availableAt, new Date(timestamp))).toUTCString();
        }
      }
    });
    try {
      await this.subjects.cacheByTypeAndLevel.bulkPut(records);
    } catch (error) {
      console.error("[StorageManager] Error setting fetch timestamp for type and level:", error);
    }
  }
  async checkCacheForTypeAndLevel(type, levels, source) {
    type = type || "all";
    levels = Array.isArray(levels) ? levels : [levels];
    try {
      const timestamps = await this.subjects.cacheByTypeAndLevel.where("[type+level+source]").anyOf(levels.map((level) => [type, level, source])).toArray();
      if (!timestamps.length) return { itemsToFetch: levels, itemsToBypassCacheInterval: [], lastUpdated: null };
      let itemsToFetch = [], itemsToBypassCacheInterval = [];
      let earliestTimestamp = null;
      const currentDate = /* @__PURE__ */ new Date();
      levels.forEach((level) => {
        const record = timestamps.find((r) => r.type === type && r.level === level && r.source === source);
        if (!record) itemsToBypassCacheInterval.push(level);
        if (record && !record.timestamp) return;
        if (record == null ? void 0 : record.timestamp) {
          if (new Date(record.timestamp) < currentDate) {
            itemsToFetch.push(level);
            if (!earliestTimestamp || new Date(record.timestamp) < new Date(earliestTimestamp)) {
              earliestTimestamp = record.timestamp;
            }
          }
        }
      });
      return {
        itemsToFetch,
        itemsToBypassCacheInterval,
        lastUpdated: earliestTimestamp ? new Date(earliestTimestamp).toUTCString() : null
      };
    } catch (error) {
      console.error("[StorageManager] Error getting fetch timestamp for type and level:", error);
      return { itemsToFetch: [], itemsToBypassCacheInterval: [], lastUpdated: null };
    }
  }
  setCacheForSRSStages(srsStages) {
    srsStages = Array.isArray(srsStages) ? srsStages : [srsStages];
    const timestamp = (/* @__PURE__ */ new Date()).toUTCString();
    srsStages.forEach((srsStage) => {
      localStorage.setItem(this.STORAGE_KEY_PREFIX + `srs_stage_${srsStage}`, timestamp);
    });
  }
  checkCacheForSRSStages(srsStages) {
    srsStages = Array.isArray(srsStages) ? srsStages : [srsStages];
    const timestamps = srsStages.map((srsStage) => localStorage.getItem(this.STORAGE_KEY_PREFIX + `srs_stage_${srsStage}`));
    if (timestamps.every((ts) => !ts)) {
      return { itemsToFetch: srsStages, itemsToBypassCacheInterval: [], lastUpdated: null };
    }
    let itemsToFetch = [], itemsToBypassCacheInterval = [];
    let earliestTimestamp = null;
    const currentDate = /* @__PURE__ */ new Date();
    srsStages.forEach((srsStage, index) => {
      const timestamp = timestamps[index];
      if (!timestamp) itemsToBypassCacheInterval.push(srsStage);
      if (timestamp) {
        if (new Date(timestamp) < currentDate) {
          itemsToFetch.push(srsStage);
          if (!earliestTimestamp || new Date(timestamp) < new Date(earliestTimestamp)) {
            earliestTimestamp = timestamp;
          }
        }
      }
    });
    return {
      itemsToFetch,
      itemsToBypassCacheInterval,
      lastUpdated: earliestTimestamp ? new Date(earliestTimestamp).toUTCString() : null
    };
  }
  /**
   * Set user information in localStorage.
   * 
   * @param {Object} data - User information to store.
   * @returns {void}
   */
  async setUserInfo(data) {
    data = data.data || data;
    localStorage.setItem(this.STORAGE_KEY_PREFIX + "user_info", JSON.stringify(data));
    this.events.emit("update:user", data);
    const avatar = await this.fetchUserAvatar(data.profile_url);
    this.events.emit("update:avatar", avatar);
  }
  /**
   * Get user information from localStorage.
   * 
   * @returns {Object|null} The user information or null if not set.
   */
  getUserInfo() {
    let data = localStorage.getItem(this.STORAGE_KEY_PREFIX + "user_info");
    if (!data) return null;
    data = JSON.parse(data);
    data.avatar = this.getUserAvatar();
    return data;
  }
  /**
   * Delete user info and avatar data from localStorage.
   */
  deleteUserInfo() {
    localStorage.removeItem(this.STORAGE_KEY_PREFIX + "user_info");
    localStorage.removeItem(this.STORAGE_KEY_PREFIX + "user_avatar");
  }
  /**
  	 * Get user avatar from localStorage.
  
  	 * @returns {string|null} The user avatar URL or null if not set.
  	 */
  getUserAvatar() {
    return localStorage.getItem(this.STORAGE_KEY_PREFIX + "user_avatar");
  }
  /**
   * Fetch user avatar from the profile URL.<br>
   * If you are running this on a browser, you need to handle CORS issues, for example, through a backend proxy.<br>
   * If in development, please provide the proxy url via the `VITE_CORS_PROXY_URL` environment variable
   * or pass it in the {@link WanikaniAPIManager} constructor.<br>
   * You can run `npm run cors-server` in the root of the repository of this project to start a proxy server locally.
   * This uses the cors-anywhere package.
   *
   * @param {string} profileUrl - The profile URL to fetch the avatar from.
   * @returns {Promise<string|null>} The user avatar data or null if not found.
   */
  async fetchUserAvatar(profileUrl) {
    const url = this.CORS_PROXY_URL ? this.CORS_PROXY_URL + "/" + profileUrl : profileUrl;
    const result = await fetch(url);
    const content = await result.text();
    const parser = new DOMParser();
    const doc = parser.parseFromString(content, "text/html");
    const avatarElem = doc.querySelector(".user-avatar");
    const avatarSrc = avatarElem.getAttribute("data-load-gravatar-url-value") || avatarElem.src;
    if (avatarElem && avatarSrc) {
      const imageData = await this._fetchImageData(avatarSrc);
      console.log(imageData);
      localStorage.setItem(this.STORAGE_KEY_PREFIX + "user_avatar", imageData);
      return imageData;
    }
    return null;
  }
  async _fetchImageData(url) {
    try {
      const response = await fetch(url);
      const blob = await response.blob();
      return new Promise((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = function(event) {
          const imageData = event.target.result;
          resolve(imageData);
        };
        reader.onerror = function(error) {
          reject(error);
        };
        reader.readAsDataURL(blob);
      });
    } catch (error) {
      throw error;
    }
  }
  /**
   * Save materials data to IndexedDB.
   * This method receives the direct result from the WaniKani API,
   * and normalizes it before saving.
   * 
   * @param {Array} data - Array of materials data.
   * @returns {Promise<void>} Resolves when the data is saved.
   * @throws {Error} If the data format is incorrect.
   */
  async saveMaterials(data) {
    data = _StorageManager._normalizeMaterialsData(data);
    try {
      await this.subjects.materials.bulkPut(data);
      this.events.emit("update:materials", data);
    } catch (error) {
      console.error("[StorageManager] Error saving materials:", error);
    }
  }
  /**
   * Save assignments data to IndexedDB.
   * This method receives the direct result from the WaniKani API,
   * and normalizes it before saving.
   * 
   * @param {Array} data - Array of assignments data.
   * @returns {Promise<void>} Resolves when the data is saved.
   * @throws {Error} If the data format is incorrect.
   */
  async saveAssignments(data) {
    data = _StorageManager._normalizeAssignmentsData(data);
    try {
      await this.subjects.assignments.bulkPut(data);
      this.events.emit("update:assignments", data);
    } catch (error) {
      console.error("[StorageManager] Error saving assignments:", error);
    }
  }
  setSubjectsForStudy(data, type) {
    data = _StorageManager._normalizeAssignmentsData(data);
    const count = data.length;
    const ids = data.map((item) => item.subject_id);
    localStorage.setItem(this.STORAGE_KEY_PREFIX + `subjects_for_${type}_count`, count);
    localStorage.setItem(this.STORAGE_KEY_PREFIX + `subjects_for_${type}_ids`, JSON.stringify(ids));
    return data;
  }
  setFutureAssignmentsSummary(summary) {
    localStorage.setItem(this.STORAGE_KEY_PREFIX + "future_assignments_summary", JSON.stringify(summary));
  }
  getFutureAssignmentsSummary() {
    const summary = localStorage.getItem(this.STORAGE_KEY_PREFIX + "future_assignments_summary");
    return summary ? JSON.parse(summary) : null;
  }
  /**
   * Save reviews data to IndexedDB.
   * This method receives the direct result from the WaniKani API,
   * and normalizes it before saving.
   * 
   * @param {Array} data - Array of reviews data.
   * @returns {Promise<void>} Resolves when the data is saved.
   * @throws {Error} If the data format is incorrect.
   */
  async saveReviews(data) {
    data = _StorageManager._normalizeReviewsData(data);
    try {
      await this.subjects.reviews.bulkPut(data);
      this.events.emit("update:reviews", data);
    } catch (error) {
      console.error("[StorageManager] Error saving reviews:", error);
    }
  }
  /**
   * Get all materials from IndexedDB.
   * Optionally filter by type.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<Array>} Array of materials or an empty array if none found.
   */
  async getMaterials(type) {
    try {
      let materials;
      if (type) materials = await this.subjects.materials.where("type").equals(type).toArray();
      else materials = await this.subjects.materials.toArray();
      this.events.emit("get:materials", { entries: materials, type });
      return materials;
    } catch (error) {
      console.error("[StorageManager] Error fetching materials:", error);
      return [];
    }
  }
  /**
   * Get materials by ID or array of IDs from IndexedDB.
   * 
   * @param {number|Array} ids - The ID or array of IDs of the subjects to fetch.
   * @returns {Promise<Array>} The array of subject objects or an empty array if not found.
   */
  async getMaterialsById(ids) {
    return this._getMaterialsByAnyOf("id", ids);
  }
  /**
   * Get materials by level from IndexedDB.
   * 
   * @param {number} levels - Level or array of levels to filter materials.
   * @returns {Promise<Array>} Array of materials at the specified level or an empty array if none found.
   */
  async getMaterialsByLevel(levels) {
    return this._getMaterialsByAnyOf("level", levels);
  }
  /**
   * Get materials by type and level from IndexedDB.
   * 
   * @param {string} type - Material type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {number} levels - Level or array of levels to filter materials.
   * @returns {Promise<Array>} Array of materials of the specified type and level or an empty array if none found.
   */
  async getMaterialsByTypeAndLevel(type, levels) {
    levels = Array.isArray(levels) ? levels : [levels];
    try {
      const materials = await this.subjects.materials.where("type").equals(type).and((subject) => levels.includes(subject.level)).toArray();
      this.events.emit("get:materials", { entries: materials, type, levels });
      return materials;
    } catch (error) {
      console.error("[StorageManager] Error fetching materials by type and level:", error);
      return [];
    }
  }
  /**
   * Get materials by characters from IndexedDB.<br>
   * If the word is composed of a single character, it returns itself and other vocabulary that contains that character.<br>
   * If the word is composed of multiple characters, it returns itself and its components.
   *
   * @param {string} characters - The characters to filter materials.
   * @return {Promise<Array>} Array of materials matching the characters or an empty array if none found.
   * @example
   * const materials = await storageManager.getMaterialsByCharacters('日');
   * // Returns '日', '日本', '日曜日', etc.
   * const materials = await storageManager.getMaterialsByCharacters('日本');
   * // Returns '日本', '日' and '本',
   **/
  async getMaterialsByCharacters(characters) {
    if (characters.length == 1)
      return this._getMaterialsByAnyOf("charactersList", Array.from(characters));
    const mainMaterial = await this.subjects.materials.where("characters").equalsIgnoreCase(characters).first();
    if (mainMaterial) {
      const componentIds = mainMaterial.component_subject_ids || [];
      const components = await this.getMaterialsById(componentIds);
      return [mainMaterial, ...components];
    }
    return [];
  }
  /**
   * Get materials by meaning from IndexedDB.<br>
   * This method searches for materials that contain part of the given meaning.<br>
   * All meanings are stored as shingles of size 3. The word to search is sliced to the first 3 characters
   * to match any of the shingles.<br>
   * Because this creates some false positives, for example "mood" for "moon", it then filters those results.
   * 
   * @param {string} meaning - The meaning to filter materials.
   * @return {Promise<Array>} Array of materials matching the meaning or an empty array if none found.
   * @example
   * const materials = await storageManager.getMaterialsByMeaning('moon');
   * // Returns materials that have meanings like 'moon', 'honeymoon', etc.
   **/
  async getMaterialsByMeaning(meaning) {
    const result = await this._getMaterialsByAnyOf("meaningsList", meaning.substring(0, 3).toLowerCase());
    return result.filter((material) => material.meanings.some((entry) => entry.meaning.toLowerCase().includes(meaning.toLowerCase())));
  }
  /**
   * Get materials by reading from IndexedDB.<br>
   * This method searches for materials that contain the given reading.<br>
   * Accepts both kana and romaji readings.<br>
   * All readings are stored as shingles of size 2. The word to search is sliced to the first 2 characters
   * to match any of the shingles.<br>
   * Because this creates some false positives, for example "かって" for "かっこ", it then filters those results.
   * 
   * @param {string} reading - The reading to filter materials.
   * @return {Promise<Array>} Array of materials matching the reading or an empty array if none found.
   * @example
   * const materials = await storageManager.getMaterialsByReading('かっこ');
   * // Returns materials that have readings like 'かっこ', 'かっこう', etc.
   */
  async getMaterialsByReading(reading) {
    reading = isRomaji(reading) ? toKana(reading) : reading;
    const result = await this._getMaterialsByAnyOf("readingsList", reading.substring(0, 2).toLowerCase());
    return result.filter((material) => material.readings.some((entry) => entry.reading.toLowerCase().includes(reading.toLowerCase())));
  }
  async _getMaterialsByAnyOf(field, values) {
    values = Array.isArray(values) ? values : [values];
    try {
      const materials = await this.subjects.materials.where(field).anyOf(values).toArray();
      this.events.emit("get:materials", { entries: materials, [field]: values });
      return materials;
    } catch (error) {
      console.error(`[StorageManager] Error fetching materials by ${field}:`, error);
      return [];
    }
  }
  /**
   * Get all assignments from IndexedDB.
   * Optionally filter by type.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<Array>} Array of assignments or an empty array if none found.
   */
  async getAssignments(type) {
    try {
      let assignments;
      if (type) assignments = await this.subjects.assignments.where("subject_type").equals(type).toArray();
      else assignments = await this.subjects.assignments.toArray();
      this.events.emit("get:assignments", { entries: assignments, type });
      return assignments;
    } catch (error) {
      console.error("[StorageManager] Error fetching assignments:", error);
      return [];
    }
  }
  /**
   * Get an assignment by the ID or array of IDs of subjects from IndexedDB.
   * 
   * @param {number} subjectIds - The ID or array of IDs of the subjects to fetch assignments for.
   * @returns {Promise<Object|null>} The assignment object or null if not found.
   */
  async getAssignmentsBySubjectId(subjectIds) {
    subjectIds = Array.isArray(subjectIds) ? subjectIds : [subjectIds];
    try {
      const assignments = await this.subjects.assignments.where("subject_id").anyOf(subjectIds).toArray();
      this.events.emit("get:assignments", { entries: assignments, ids: subjectIds });
      return assignments;
    } catch (error) {
      console.error("[StorageManager] Error fetching assignment by subject ID:", error);
      return null;
    }
  }
  /**
   * Get assignments by SRS stage from IndexedDB.
   * 
   * @param {number} srsStages - SRS stage or array of SRS stages to filter assignments.
   * @returns {Promise<Array>} Array of assignments at the specified SRS stage or an empty array if none found.
   */
  async getAssignmentsBySRSStage(srsStages) {
    try {
      const assignments = await this.subjects.assignments.where("srs_stage").anyOf(srsStages).toArray();
      this.events.emit("get:assignments", { entries: assignments, srsStages });
      return assignments;
    } catch (error) {
      console.error("[StorageManager] Error fetching assignments by SRS stage:", error);
      return [];
    }
  }
  /**
   * Get all reviews from IndexedDB.
   * Optionally filter by type.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<Array>} Array of reviews or an empty array if none found.
   */
  async getReviews(type) {
    try {
      let reviews;
      if (type) reviews = await this.subjects.reviews.where("subject_type").equals(type).toArray();
      else reviews = await this.subjects.reviews.toArray();
      this.events.emit("get:reviews", { entries: reviews, type });
      return reviews;
    } catch (error) {
      console.error("[StorageManager] Error fetching reviews:", error);
      return [];
    }
  }
  /**
   * Get reviews by the ID or array of IDs of subjects from IndexedDB.
   * 
   * @param {number} subjectIds - The ID or array of IDs of the subjects to fetch reviews for.
   * @returns {Promise<Object|null>} The review object or null if not found.
   */
  async getReviewsBySubjectId(subjectIds) {
    subjectIds = Array.isArray(subjectIds) ? subjectIds : [subjectIds];
    try {
      const reviews = await this.subjects.reviews.where("subject_id").anyOf(subjectIds).toArray();
      this.events.emit("get:reviews", { entries: reviews, ids: subjectIds });
      return reviews;
    } catch (error) {
      console.error("[StorageManager] Error fetching reviews by subject ID:", error);
      return null;
    }
  }
  /**
   * Add a new request entry to the requests table.
   * 
   * @param {string} url - The API request URL.
   * @param {number} responseSize - The size of the response in bytes.
   * @returns {Promise<void>} Resolves when the entry is added.
   */
  async setRequestEntry(url, responseSize) {
    const endpoint = new URL(url).pathname.slice(4);
    const timestamp = (/* @__PURE__ */ new Date()).toUTCString();
    const entry = { url, endpoint, responseSize: parseInt(responseSize), timestamp };
    try {
      await this.subjects.requests.add(entry);
      return entry;
    } catch (error) {
      console.error("[StorageManager] Error adding request entry:", error);
      return error;
    }
  }
  /**
   * Get all request entries from the requests table.
   * 
   * @param {number} [timeOffset=60] - Time offset in seconds to filter requests (default is 60 seconds).
   * @returns {Promise<Array>} Resolves with an array of request entries.
   * @throws {Error} If there is an error fetching the entries.
   * @example
   * // Get all request entries from the last 120 seconds
   * const entries = await storageManager.getRequestEntries(120);
   */
  async getRequestEntries(timeOffset = 60) {
    const currentTime = /* @__PURE__ */ new Date();
    const timeThreshold = new Date(currentTime.getTime() - timeOffset * 1e3).toUTCString();
    try {
      return await this.subjects.requests.where("timestamp").aboveOrEqual(timeThreshold).toArray();
    } catch (error) {
      console.error("[StorageManager] Error fetching request entries:", error);
      return [];
    }
  }
  /**
   * Delete clear both localStorage and IndexedDB.
   * The IndexedDB is automatically opened again by Dexie.
   * 
   * @returns {Promise<void>} Resolves when the storage is cleared.
   * @throws {Error} If there is an error clearing the storage.
   * @example
   * // Clear all stored data
   * await storageManager.delete();
   */
  async delete() {
    Object.keys(localStorage).forEach((key) => {
      if (key.startsWith(this.STORAGE_KEY_PREFIX)) {
        localStorage.removeItem(key);
      }
    });
    const requests = await this.getRequestEntries(60);
    await this.subjects.delete({ disableAutoOpen: false });
    if (requests.length > 0) {
      await this.subjects.requests.bulkPut(requests);
    }
  }
  async loadSubjectsCache() {
    try {
      const materials = await this.subjects.materials.toArray();
      const assignments = await this.subjects.assignments.toArray();
      const reviews = await this.subjects.reviews.toArray();
      this.events.emit("cache:subjects", { materials, assignments, reviews });
      return { materials, assignments, reviews };
    } catch (error) {
      console.error("[StorageManager] Error loading subjects cache:", error);
      return { materials: [], assignments: [], reviews: [] };
    }
  }
  async loadUserFromCache() {
    let userInfo = localStorage.getItem(this.STORAGE_KEY_PREFIX + "user_info");
    const userAvatar = localStorage.getItem(this.STORAGE_KEY_PREFIX + "user_avatar");
    if (userInfo) {
      userInfo = JSON.parse(userInfo);
      userInfo.avatar = userAvatar;
      this.events.emit("cache:user", { userInfo });
      return userInfo;
    }
    return null;
  }
  // ====== PRIVATE METHODS ======
  /**
   * Normalize materials data from the WaniKani API.
   * Converts the raw API response into a format suitable for storage.
   * 
   * @private
   * @param {Array} data - Raw materials data from the API.
   * @returns {Array} Normalized materials data.
   * @throws {Error} If the data format is incorrect.
   */
  static _normalizeMaterialsData(data) {
    if (!Array.isArray(data)) {
      throw new Error("Materials data must be an array");
    }
    return data.reduce((acc, materials) => {
      return acc.concat(materials.data.reduce((arr, material) => {
        return arr.concat({
          ...material.data,
          id: material.id,
          data_updated_at: material.data_updated_at,
          type: material.object,
          meaningsList: material.data.meanings ? _StorageManager._generateShingles(material.data.meanings.map((m) => m.meaning)) : [],
          readingsList: material.data.readings ? _StorageManager._generateShingles(material.data.readings.map((r) => r.reading), 2) : [],
          charactersList: material.data.characters ? _StorageManager._generateShingles([material.data.characters]) : []
        });
      }, []));
    }, []);
  }
  static _generateShingles(words, n = 3) {
    if (!Array.isArray(words)) {
      throw new Error("Words must be an array");
    }
    const shingles = /* @__PURE__ */ new Set();
    for (let word of words) {
      word = word.trim().toLowerCase();
      if (word.length < n) {
        shingles.add(word);
        continue;
      }
      for (let i = 0; i <= word.length - n; i++) {
        shingles.add(word.substring(i, i + n));
      }
    }
    return Array.from(shingles);
  }
  /**
   * Normalize assignments data from the WaniKani API.
   * Converts the raw API response into a format suitable for storage.
   * 
   * @private
   * @param {Array} data - Raw assignments data from the API.
   * @returns {Array} Normalized assignments data.
   * @throws {Error} If the data format is incorrect.
   */
  static _normalizeAssignmentsData(data) {
    if (!Array.isArray(data)) {
      throw new Error("Assignments data must be an array");
    }
    return data.reduce((acc, assignments) => {
      return acc.concat(assignments.data.reduce((arr, assignment) => {
        return arr.concat({
          ...assignment.data,
          id: assignment.id,
          data_updated_at: assignment.data_updated_at
        });
      }, []));
    }, []);
  }
  /**
   * Normalize reviews data from the WaniKani API.
   * Converts the raw API response into a format suitable for storage.
   * 
   * @private
   * @param {Array} data - Raw reviews data from the API.
   * @returns {Array} Normalized reviews data.
   * @throws {Error} If the data format is incorrect.
   */
  static _normalizeReviewsData(data) {
    if (!Array.isArray(data)) {
      throw new Error("Reviews data must be an array");
    }
    return data.reduce((acc, reviews) => {
      return acc.concat(reviews.data.reduce((arr, review) => {
        return arr.concat({
          ...review.data,
          id: review.id,
          data_updated_at: review.data_updated_at
        });
      }, []));
    }, []);
  }
};
var eventemitter3 = { exports: {} };
var hasRequiredEventemitter3;
function requireEventemitter3() {
  if (hasRequiredEventemitter3) return eventemitter3.exports;
  hasRequiredEventemitter3 = 1;
  (function(module) {
    var has2 = Object.prototype.hasOwnProperty, prefix = "~";
    function Events() {
    }
    if (Object.create) {
      Events.prototype = /* @__PURE__ */ Object.create(null);
      if (!new Events().__proto__) prefix = false;
    }
    function EE(fn, context, once) {
      this.fn = fn;
      this.context = context;
      this.once = once || false;
    }
    function addListener(emitter, event, fn, context, once) {
      if (typeof fn !== "function") {
        throw new TypeError("The listener must be a function");
      }
      var listener = new EE(fn, context || emitter, once), evt = prefix ? prefix + event : event;
      if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
      else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
      else emitter._events[evt] = [emitter._events[evt], listener];
      return emitter;
    }
    function clearEvent(emitter, evt) {
      if (--emitter._eventsCount === 0) emitter._events = new Events();
      else delete emitter._events[evt];
    }
    function EventEmitter2() {
      this._events = new Events();
      this._eventsCount = 0;
    }
    EventEmitter2.prototype.eventNames = function eventNames() {
      var names = [], events, name;
      if (this._eventsCount === 0) return names;
      for (name in events = this._events) {
        if (has2.call(events, name)) names.push(prefix ? name.slice(1) : name);
      }
      if (Object.getOwnPropertySymbols) {
        return names.concat(Object.getOwnPropertySymbols(events));
      }
      return names;
    };
    EventEmitter2.prototype.listeners = function listeners(event) {
      var evt = prefix ? prefix + event : event, handlers = this._events[evt];
      if (!handlers) return [];
      if (handlers.fn) return [handlers.fn];
      for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) {
        ee[i] = handlers[i].fn;
      }
      return ee;
    };
    EventEmitter2.prototype.listenerCount = function listenerCount(event) {
      var evt = prefix ? prefix + event : event, listeners = this._events[evt];
      if (!listeners) return 0;
      if (listeners.fn) return 1;
      return listeners.length;
    };
    EventEmitter2.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return false;
      var listeners = this._events[evt], len = arguments.length, args, i;
      if (listeners.fn) {
        if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
        switch (len) {
          case 1:
            return listeners.fn.call(listeners.context), true;
          case 2:
            return listeners.fn.call(listeners.context, a1), true;
          case 3:
            return listeners.fn.call(listeners.context, a1, a2), true;
          case 4:
            return listeners.fn.call(listeners.context, a1, a2, a3), true;
          case 5:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
          case 6:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
        }
        for (i = 1, args = new Array(len - 1); i < len; i++) {
          args[i - 1] = arguments[i];
        }
        listeners.fn.apply(listeners.context, args);
      } else {
        var length = listeners.length, j;
        for (i = 0; i < length; i++) {
          if (listeners[i].once) this.removeListener(event, listeners[i].fn, void 0, true);
          switch (len) {
            case 1:
              listeners[i].fn.call(listeners[i].context);
              break;
            case 2:
              listeners[i].fn.call(listeners[i].context, a1);
              break;
            case 3:
              listeners[i].fn.call(listeners[i].context, a1, a2);
              break;
            case 4:
              listeners[i].fn.call(listeners[i].context, a1, a2, a3);
              break;
            default:
              if (!args) for (j = 1, args = new Array(len - 1); j < len; j++) {
                args[j - 1] = arguments[j];
              }
              listeners[i].fn.apply(listeners[i].context, args);
          }
        }
      }
      return true;
    };
    EventEmitter2.prototype.on = function on(event, fn, context) {
      return addListener(this, event, fn, context, false);
    };
    EventEmitter2.prototype.once = function once(event, fn, context) {
      return addListener(this, event, fn, context, true);
    };
    EventEmitter2.prototype.removeListener = function removeListener(event, fn, context, once) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return this;
      if (!fn) {
        clearEvent(this, evt);
        return this;
      }
      var listeners = this._events[evt];
      if (listeners.fn) {
        if (listeners.fn === fn && (!once || listeners.once) && (!context || listeners.context === context)) {
          clearEvent(this, evt);
        }
      } else {
        for (var i = 0, events = [], length = listeners.length; i < length; i++) {
          if (listeners[i].fn !== fn || once && !listeners[i].once || context && listeners[i].context !== context) {
            events.push(listeners[i]);
          }
        }
        if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
        else clearEvent(this, evt);
      }
      return this;
    };
    EventEmitter2.prototype.removeAllListeners = function removeAllListeners(event) {
      var evt;
      if (event) {
        evt = prefix ? prefix + event : event;
        if (this._events[evt]) clearEvent(this, evt);
      } else {
        this._events = new Events();
        this._eventsCount = 0;
      }
      return this;
    };
    EventEmitter2.prototype.off = EventEmitter2.prototype.removeListener;
    EventEmitter2.prototype.addListener = EventEmitter2.prototype.on;
    EventEmitter2.prefixed = prefix;
    EventEmitter2.EventEmitter = EventEmitter2;
    {
      module.exports = EventEmitter2;
    }
  })(eventemitter3);
  return eventemitter3.exports;
}
var eventemitter3Exports = requireEventemitter3();
var EventEmitter = getDefaultExportFromCjs(eventemitter3Exports);
var WanikaniAPIManager = class _WanikaniAPIManager {
  constructor(config) {
    var _a, _b, _c, _d;
    console.log("[WaniKaniAPI] Initializing WanikaniAPIManager with config:", config);
    this.apiKey = config.apiKey;
    this.baseUrl = config.baseUrl || "https://api.wanikani.com/v2/";
    this.events = new EventEmitter();
    this.storage = new StorageManager(this.events, {
      proxy: config == null ? void 0 : config.proxy
    });
    this.cacheIntervals = {
      user: ((_a = config.cacheIntervals) == null ? void 0 : _a.user) || 1 * 60 * 60 * 1e3,
      // 1 hour
      materials: ((_b = config.cacheIntervals) == null ? void 0 : _b.materials) || 24 * 60 * 60 * 1e3,
      // 24 hours
      assignments: ((_c = config.cacheIntervals) == null ? void 0 : _c.assignments) || 60 * 1e3,
      // 1 minute
      reviews: ((_d = config.cacheIntervals) == null ? void 0 : _d.reviews) || 60 * 1e3
      // 1 minute
    };
    this.RATE_LIMIT = 60;
  }
  /**
   * Fetch user info from the WaniKani API.
   * This method already handles caching.
   * 
   * @returns {Promise<Object>} User info object or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:user
   * @example
   * const userInfo = await wkManager.getUserInfo();
   */
  async getUserInfo(onUpdate) {
    return this._readFromStorage(
      () => this.updateUserInfo(),
      () => this.storage.getUserInfo(),
      onUpdate
    );
  }
  /**
   * Update user info from the WaniKani API.
   * 
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails.
   */
  async updateUserInfo() {
    const url = this.baseUrl + "user";
    const response = await this._fetchPage(url, null, false);
    if (response.error) {
      console.error("[WaniKaniAPI] Error fetching user info:", response.error);
      return;
    }
    this.storage.setUserInfo(response);
    this.storage.setFetchTimestamp("user", /* @__PURE__ */ new Date());
  }
  /**
   * Get all radicals, or filter by level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles data caching, so there is no need to call `updateSubjects` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} [level] - Optional level to filter radicals.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of radical subjects or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allRadicals = await wkManager.getRadicals(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5Radicals = await wkManager.getRadicals(5);
   */
  async getRadicals(level, onUpdate) {
    if (level) return this.getSubjectsByTypeAndLevel("radical", level, onUpdate);
    return this.getSubjects("radical", onUpdate);
  }
  /**
   * Get all kanji, or filter by level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles data caching, so there is no need to call `updateSubjects` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} [level] - Optional level to filter kanji.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of kanji subjects or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allKanji = await wkManager.getKanji(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5Kanji = await wkManager.getKanji(5);
   */
  async getKanji(level, onUpdate) {
    if (level) return this.getSubjectsByTypeAndLevel("kanji", level, onUpdate);
    return this.getSubjects("kanji", onUpdate);
  }
  /**
   * Get all vocabulary, or filter by level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles data caching, so there is no need to call `updateSubjects` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} [level] - Optional level to filter vocabulary.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of vocabulary subjects or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allVocabulary = await wkManager.getVocabulary(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5Vocabulary = await wkManager.getVocabulary(5);
   */
  async getVocabulary(level, onUpdate) {
    if (level) return this.getSubjectsByTypeAndLevel("vocabulary", level, onUpdate);
    return this.getSubjects("vocabulary", onUpdate);
  }
  /**
   * Get all kana vocabulary, or filter by level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles data caching, so there is no need to call `updateSubjects` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} [level] - Optional level to filter kana vocabulary.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of kana vocabulary subjects or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allKanaVocabulary = await wkManager.getKanaVocabulary(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5KanaVocabulary = await wkManager.getKanaVocabulary(5);
   */
  async getKanaVocabulary(level, onUpdate) {
    if (level) return this.getSubjectsByTypeAndLevel("kana_vocabulary", level, onUpdate);
    return this.getSubjects("kana_vocabulary", onUpdate);
  }
  /**
   * Get all subjects of a specific type or all subjects if no type is specified.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching, so there is no need to call `updateSubjects` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allSubjects = await wkManager.getSubjects(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5Subjects = await wkManager.getSubjects(5);
   */
  async getSubjects(type, onUpdate) {
    if (!_WanikaniAPIManager._validType(type)) return [];
    return this._readFromStorage(
      () => this.updateSubjects(type),
      () => this._readSubjects(type),
      onUpdate
    );
  }
  /**
   * Get subjects by ID or an array of IDs.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching, so there is no need to call `updateSubjectsById` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number|Array} ids - Subject ID or an array of IDs to filter subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects or error object.
   * @throws {Error} If the API request fails or if the ID is invalid.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const subjects = await wkManager.getSubjectsById([420, 300, 21, 1001], (updated, cached) => {...});
   * // blocking, waits for API response
   * const subject = await wkManager.getSubjectsById(12);
   */
  async getSubjectsById(ids, onUpdate) {
    ids = Array.isArray(ids) ? ids : [ids];
    return this._readFromStorage(
      () => this.updateSubjectsById(ids),
      () => this._readSubjectsById(ids),
      onUpdate
    );
  }
  /**
   * Get subjects by level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching, so there is no need to call `updateSubjectsByLevel` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} levels - Level or array of levels to filter subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects or error object.
   * @throws {Error} If the API request fails or if the level is invalid.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const allSubjects = await wkManager.getSubjectsByLevel(null, (updated, cached) => {...});
   * // blocking, waits for API response
   * const level5Subjects = await wkManager.getSubjectsByLevel(5);
   */
  async getSubjectsByLevel(levels, onUpdate) {
    levels = Array.isArray(levels) ? levels : [levels];
    if (!levels.length || levels.some((level) => isNaN(level))) {
      console.error("[WaniKaniAPI] Invalid levels:", levels);
      return [];
    }
    return this._readFromStorage(
      () => this.updateSubjectsByLevel(levels),
      async () => this._readSubjectsByMaterials(await this.storage.getMaterialsByLevel(levels)),
      onUpdate
    );
  }
  /**
   * Get subjects by type and level.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching, so there is no need to call `updateSubjectsByTypeAndLevel` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {string} type - Subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {number} levels - Level or array of levels to filter subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects or error object.
   * @throws {Error} If the API request fails or if the type or level is invalid.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const level5Kanji = await wkManager.getSubjectsByTypeAndLevel('kanji', 5, (updated, cached) => {...});
   * // blocking, waits for API response
   * const radicalsUpToLevel3 = await wkManager.getSubjectsByTypeAndLevel('radical', [1, 2, 3]);
  */
  async getSubjectsByTypeAndLevel(type, levels, onUpdate) {
    if (!type || typeof type !== "string" || !_WanikaniAPIManager._validType(type))
      return this.getSubjectsByLevel(levels, onUpdate);
    levels = Array.isArray(levels) ? levels : [levels];
    if (!levels || levels.some((level) => isNaN(level))) {
      console.error("[WaniKaniAPI] Invalid levels:", levels);
      return [];
    }
    return this._readFromStorage(
      () => this.updateSubjectsByTypeAndLevel(type, levels),
      async () => this._readSubjectsByMaterials(await this.storage.getMaterialsByTypeAndLevel(type, levels)),
      onUpdate,
      "subjects"
    );
  }
  /**
   * Get subjects at a specific SRS stage.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} srsStage - SRS stage to filter subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects at the specified SRS stage or error object.
   * @throws {Error} If the API request fails or if the SRS stage is invalid.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const guruISubjects = await wkManager.getSubjectsBySRSStage(5, (updated, cached) => {...});
   * // blocking, waits for API response
   * const appISubjects = await wkManager.getSubjectsBySRSStage(1);
   **/
  async getSubjectsBySRSStage(srsStage, onUpdate) {
    const assignments = await this.getAssignmentsBySRSStage(srsStage, onUpdate);
    const subjectIds = assignments.map((a) => a.subject_id);
    const subjects = await this.getSubjectsById(subjectIds, onUpdate);
    return subjects;
  }
  /**
   * Queries cache for subjects containing given characters.<br>
   * This method requires at least the materials to already be cached.
   * Subsequent calls to the Wanikani API are made to check for updates.
   * 
   * @param {string} characters - Characters to search for in subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects containing the characters or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const subjects = await wkManager.querySubjectsByCharacters('日', (updated, cached) => {...});
   * // blocking, waits for API response
   * const subjects = await wkManager.querySubjectsByCharacters('漢字');
   **/
  async querySubjectsByCharacters(characters, onUpdate) {
    const materials = await this.storage.getMaterialsByCharacters(characters);
    const ids = materials.map((m) => m.id);
    return this.getSubjectsById(ids, onUpdate);
  }
  /**
   * Queries cache for subjects containing given meaning.<br>
   * This method requires at least the materials to already be cached.
   * Subsequent calls to the Wanikani API are made to check for updates.
   * 
   * @param {string} meaning - Meaning to search for in subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects containing the meaning or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const subjects = await wkManager.querySubjectsByMeaning('moon', (updated, cached) => {...});
   * // blocking, waits for API response
   * const subjects = await wkManager.querySubjectsByMeaning('honeymoon');
   * **/
  async querySubjectsByMeaning(meaning, onUpdate) {
    const materials = await this.storage.getMaterialsByMeaning(meaning);
    const ids = materials.map((m) => m.id);
    return this.getSubjectsById(ids, onUpdate);
  }
  /**
   * Queries cache for subjects containing given reading.<br>
   * Accepts both kana and romaji readings.<br>
   * This method requires at least the materials to already be cached.
   * Subsequent calls to the Wanikani API are made to check for updates.
   * 
   * @param {string} reading - Reading to search for in subjects.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subjects containing the reading or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:subjects
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const subjects = await wkManager.querySubjectsByReading('にほんご', (updated, cached) => {...});
   * // blocking, waits for API response
   * const subjects = await wkManager.querySubjectsByReading('nihongo');
   */
  async querySubjectsByReading(reading, onUpdate) {
    const materials = await this.storage.getMaterialsByReading(reading);
    const ids = materials.map((m) => m.id);
    return this.getSubjectsById(ids, onUpdate);
  }
  /**
   * Get future assignments.<br>
   * This method already handles caching, so there is no need to call `updateFutureAssignments` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * @see [futureAssignmentsSummary()]{@link WanikaniAPIManager.futureAssignmentsSummary} for details on the return data structure.
   * 
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of future assignments or error object.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#get:assignments:future
   * @example
   * // non blocking (only for cache access), cache returns immediately
   * const futureAssignments = await wkManager.getFutureAssignments((updated, cached) => {...});
   * // blocking, waits for API response
   * const futureAssignments = await wkManager.getFutureAssignments();
   */
  async getFutureAssignments(onUpdate) {
    return this._readFromStorage(
      () => this.updateFutureAssignments(),
      () => this.storage.getFutureAssignmentsSummary(),
      onUpdate
    );
  }
  /**
   * Fetch only the subject materials (without assignments) of a specific type or all types if no type is specified.<br>
   * This method already handles caching, so there is no need to call `updateMaterials` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of subject materials or error object.
   * @fires WanikaniAPIManager#get:materials
   * @throws {Error} If the API request fails.
   */
  async getMaterials(type, onUpdate) {
    if (!_WanikaniAPIManager._validType(type)) return [];
    return this._readFromStorage(
      () => this.updateMaterials(type),
      () => this.storage.getMaterials(type),
      onUpdate
    );
  }
  /**
   * Fetch assignments of a specific type or all types if no type is specified.<br>
   * This method already handles caching, so there is no need to call `updateAssignments` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of assignments or error object.
   * @fires WanikaniAPIManager#get:assignments
   * @throws {Error} If the API request fails.
   */
  async getAssignments(type, onUpdate) {
    if (!_WanikaniAPIManager._validType(type)) return [];
    return this._readFromStorage(
      () => this.updateAssignments(type),
      () => this.storage.getAssignments(type),
      onUpdate
    );
  }
  /**
   * Get assignments by SRS stage.<br>
   * Data includes subject information, assignments and reviews stats.
   * This method already handles caching, so there is no need to call `updateAssignmentsBySRSStage` manually.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {number} srsStages - SRS stage or an array of SRS stages to filter assignments.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of assignments at the specified SRS stage or error object.
   * @fires WanikaniAPIManager#get:assignments
   * @throws {Error} If the API request fails or if the SRS stage is invalid.
   */
  async getAssignmentsBySRSStage(srsStages, onUpdate) {
    srsStages = Array.isArray(srsStages) ? srsStages : [srsStages];
    if (!srsStages.length || srsStages.some((stage) => isNaN(stage) || stage < -1 || stage > 9)) {
      console.error("[WaniKaniAPI] Invalid SRS stages:", srsStages);
      return [];
    }
    return this._readFromStorage(
      () => this.updateAssignmentsBySRSStage(srsStages),
      () => this.storage.getAssignmentsBySRSStage(srsStages),
      onUpdate
    );
  }
  /**
   * Fetch reviews statistics of subjects of a specific type or all types if no type is specified.<br>
   * This method already handles caching.<br>
   * This method can behave both synchronously and asynchronously.
   * @see [Subject Fetch Behavior]{@link WanikaniAPIManager#SubjectFetchBehavior} for more details and usage examples.
   * 
   * @param {string} [type] - Optional subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<Array>} Array of reviews or error object.
   * @fires WanikaniAPIManager#get:reviews
   * @throws {Error} If the API request fails.
   */
  async getReviews(type, onUpdate) {
    if (!_WanikaniAPIManager._validType(type)) return [];
    return this._readFromStorage(
      () => this.updateReviews(type),
      () => this.storage.getReviews(type),
      onUpdate
    );
  }
  /**
   * Manually cache and update subject materials and assignments, either for all types or a specific type.
   * 
   * @param {string} [type] - Optional subject type to update (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#update:subjects
   * @example
   * await wkManager.updateSubjects('radical');
   */
  async updateSubjects(type) {
    await this.updateMaterials(type);
    await this.updateAssignments(type);
    await this.updateReviews(type);
    this.events.emit("update:subjects", type);
  }
  /**
   * Manually cache and update subject materials from the WaniKani API.
   * This is only subject information, not its assignments.
   * 
   * @param {string} [type] - Optional subject type to update (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#update:materials
   * @example
   * await wkManager.updateMaterials('radical');
   */
  async updateMaterials(type) {
    await this._updateContent(
      "materials",
      type,
      this.baseUrl + "subjects" + (type ? `?types=${type}` : ""),
      this.storage.saveMaterials.bind(this.storage)
    );
  }
  /**
   * Manually cache and update assignments from the WaniKani API.
   * 
   * @param {string} [type] - Optional subject type to update (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#update:assignments
   * @example
   * await wkManager.updateAssignments('radical');
   */
  async updateAssignments(type) {
    await this._updateContent(
      "assignments",
      type,
      this.baseUrl + "assignments" + (type ? `?subject_types=${type}` : ""),
      this.storage.saveAssignments.bind(this.storage)
    );
  }
  /**
   * Manually cache and update review statistics from the WaniKani API.
   * 
   * @param {string} [type] - Optional subject type to filter reviews (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails.
   * @fires WanikaniAPIManager#update:reviews
   * @example
   * await wkManager.updateReviews('radical');
   */
  async updateReviews(type) {
    await this._updateContent(
      "reviews",
      type,
      this.baseUrl + "review_statistics" + (type ? `?subject_types=${type}` : ""),
      this.storage.saveReviews.bind(this.storage)
    );
  }
  /**
   * Manually cache and update a specific subject material and assignments by its ID or an array of IDs.
   * 
   * @param {number|Array<number>} ids - ID or array of IDs of the subjects to update.
   * @returns {Promise<Object>} Resolves with the updated subject data or an error object.
   * @throws {Error} If the API request fails or if no IDs are provided.
   * @fires WanikaniAPIManager#update:subjects
   * @example
   * await wkManager.updateSubjectsById(12);
   * await wkManager.updateSubjectsById([20, 21, 35]);
   */
  async updateSubjectsById(ids) {
    ids = Array.isArray(ids) ? ids : [ids];
    let result = await this.updateMaterialsBySubjectId(ids);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    result = await this.updateAssignmentsBySubjectId(ids);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    result = await this.updateReviewsBySubjectId(ids);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    this.events.emit("update:subjects", ids);
  }
  /**
   * Manually cache and update a specific subject by its ID or an array of IDs.
   * This is only subject information, not its assignments.
   * 
   * @param {number|Array<number>} ids - ID or array of IDs of the subjects to update.
   * @returns {Promise<Object>} Resolves with the updated subject data or an error object.
   * @throws {Error} If the API request fails or if no IDs are provided.
   * @fires WanikaniAPIManager#update:materials
   * @example
   * await wkManager.updateMaterialsBySubjectId(12);
   * await wkManager.updateMaterialsBySubjectId([20, 21, 35]);
   */
  async updateMaterialsBySubjectId(ids) {
    await this._updateContentById(
      "materials",
      "subjects?ids=",
      ids,
      this.storage.saveMaterials.bind(this.storage)
    );
  }
  /**
   * Manually cache and update a specific assignment by its subject ID or an array of IDs.
   * 
   * @param {number|Array<number>} ids - ID or array of IDs of the assignments' subjects to update.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no IDs are provided.
   * @fires WanikaniAPIManager#update:assignments
   * @example
   * await wkManager.updateAssignmentsBySubjectId(12);
   * await wkManager.updateAssignmentsBySubjectId([20, 21, 35]);
   */
  async updateAssignmentsBySubjectId(ids) {
    return await this._updateContentById(
      "assignments",
      "assignments?subject_ids=",
      ids,
      this.storage.saveAssignments.bind(this.storage)
    );
  }
  /**
   * Manually cache and update reviews statistics by subject ID or an array of IDs.
   * @param {number|Array<number>} ids - ID or array of IDs of the subjects to update reviews.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no IDs are provided.
   * @fires WanikaniAPIManager#update:reviews
   * @example
   * await wkManager.updateReviewsBySubjectId(12);
   * await wkManager.updateReviewsBySubjectId([20, 21, 35]);
   */
  async updateReviewsBySubjectId(ids) {
    await this._updateContentById(
      "reviews",
      "review_statistics?subject_ids=",
      ids,
      this.storage.saveReviews.bind(this.storage)
    );
  }
  /**
   * Manually cache and update subject materials and assignments by level.
   * 
   * @param {number|Array<number>} levels - Level or array of levels to update subjects for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:subjects
   * @example
   * await wkManager.updateSubjectsByLevel(5);
   * await wkManager.updateSubjectsByLevel([1, 2, 3]);
   */
  async updateSubjectsByLevel(levels) {
    levels = Array.isArray(levels) ? levels : [levels];
    if (!levels.length) {
      console.error("[WaniKaniAPI] No levels provided for subject update.");
      return;
    }
    let result = await this.updateMaterialsByLevel(levels);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    result = await this.updateAssignmentsByLevel(levels);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    const materials = await this.storage.getMaterialsByLevel(levels);
    if (!materials || !materials.length)
      result = await this.updateReviews();
    else {
      const ids = materials.map((m) => m.id);
      result = await this.updateReviewsBySubjectId(ids);
    }
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    this.events.emit("update:subjects", levels);
  }
  /**
   * Manually cache and update subject materials by level.
   * 
   * @param {number|Array<number>} levels - Level or array of levels to update materials for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:materials
   * @example
   * await wkManager.updateMaterialsByLevel(5);
   * await wkManager.updateMaterialsByLevel([1, 2, 3]);
   */
  async updateMaterialsByLevel(levels) {
    return await this._updateContentDynamic(
      "materials",
      (levelsToFetch) => this.baseUrl + "subjects?levels=" + levelsToFetch.join(","),
      () => this.storage.checkCacheForTypeAndLevel(null, levels, "materials"),
      () => this.storage.setCacheForTypeAndLevel(null, levels, /* @__PURE__ */ new Date(), "materials"),
      this.storage.saveMaterials.bind(this.storage),
      { levels }
    );
  }
  /**
   * Manually cache and update assignments by level.
   * 
   * @param {number|Array<number>} levels - Level or array of levels to update assignments for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:assignments
   * @example
   * await wkManager.updateAssignmentsByLevel(5);
   * await wkManager.updateAssignmentsByLevel([1, 2, 3]);
   */
  async updateAssignmentsByLevel(levels) {
    return await this._updateContentDynamic(
      "assignments",
      (levelsToFetch) => this.baseUrl + "assignments?levels=" + levelsToFetch.join(","),
      () => this.storage.checkCacheForTypeAndLevel(null, levels, "assignments"),
      () => this.storage.setCacheForTypeAndLevel(null, levels, /* @__PURE__ */ new Date(), "assignments"),
      this.storage.saveAssignments.bind(this.storage),
      { levels }
    );
  }
  /**
   * Manually cache and update subject materials and assignments by type and level.
   * 
   * @param {string} type - Subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {number|Array<number>} levels - Level or array of levels to update subjects for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:subjects
   * @example
   * await wkManager.updateSubjectsByTypeAndLevel('kanji', 5);
   * await wkManager.updateSubjectsByTypeAndLevel('vocabulary', [1, 2, 3]);
   */
  async updateSubjectsByTypeAndLevel(type, levels) {
    levels = Array.isArray(levels) ? levels : [levels];
    if (!levels.length) {
      console.error("[WaniKaniAPI] No levels provided for subject update by type and level.");
      return;
    }
    let result = await this.updateMaterialsByTypeAndLevel(type, levels);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    result = await this.updateAssignmentsByTypeAndLevel(type, levels);
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    const materials = await this.storage.getMaterialsByTypeAndLevel(type, levels);
    if (!materials || !materials.length)
      result = await this.updateReviews(type);
    else {
      const ids = materials.map((m) => m.id);
      result = await this.updateReviewsBySubjectId(ids);
    }
    if (result === "missing-args" || result === "no-items-to-fetch") return;
    this.events.emit("update:subjects", { type, levels });
  }
  /**
   * Manually cache and update subject materials by type and level.
   * This is only subject information, not its assignments.
   * 
   * @param {string} type - Subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {number|Array<number>} levels - Level or array of levels to update materials for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:materials
   * @example
   * await wkManager.updateMaterialsByTypeAndLevel('kanji', 5);
   * await wkManager.updateMaterialsByTypeAndLevel('vocabulary', [1, 2, 3]);
   */
  async updateMaterialsByTypeAndLevel(type, levels) {
    return await this._updateContentDynamic(
      "materials",
      (levelsToFetch) => this.baseUrl + "subjects?types=" + type + "&levels=" + levelsToFetch.join(","),
      () => this.storage.checkCacheForTypeAndLevel(type, levels, "materials"),
      () => this.storage.setCacheForTypeAndLevel(type, levels, /* @__PURE__ */ new Date(), "materials"),
      this.storage.saveMaterials.bind(this.storage),
      { type, levels }
    );
  }
  /**
   * Manually cache and update assignments by subject type and level.
   * 
   * @param {string} type - Subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {number|Array<number>} levels - Level or array of levels to update assignments for.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if no levels are provided.
   * @fires WanikaniAPIManager#update:assignments
   * @example
   * await wkManager.updateAssignmentsByTypeAndLevel('kanji', 5);
   * await wkManager.updateAssignmentsByTypeAndLevel('vocabulary', [1, 2, 3]);
   */
  async updateAssignmentsByTypeAndLevel(type, levels) {
    return await this._updateContentDynamic(
      "assignments",
      (levelsToFetch) => this.baseUrl + "assignments?subject_types=" + type + "&levels=" + levelsToFetch.join(","),
      () => this.storage.checkCacheForTypeAndLevel(type, levels, "assignments"),
      () => this.storage.setCacheForTypeAndLevel(type, levels, /* @__PURE__ */ new Date(), "assignments"),
      this.storage.saveAssignments.bind(this.storage),
      { type, levels }
    );
  }
  /**
   * Manually cache and update assignments by SRS stage.
   * 
   * @param {number} srsStages - SRS stage or an array of SRS stages to filter assignments.
   * @returns {Promise<void>} Resolves when the update is complete.
   * @throws {Error} If the API request fails or if the SRS stage is invalid.
   * @fires WanikaniAPIManager#update:assignments
   * @example
   * await wkManager.updateAssignmentsBySRSStage(1);
   */
  async updateAssignmentsBySRSStage(srsStages) {
    return await this._updateContentDynamic(
      "assignments",
      (srsStagesToFetch) => this.baseUrl + "assignments?srs_stages=" + srsStagesToFetch.join(","),
      () => this.storage.checkCacheForSRSStages(srsStages),
      () => this.storage.setCacheForSRSStages(srsStages),
      this.storage.saveAssignments.bind(this.storage),
      { srsStages }
    );
  }
  /**
   * Update future assignments summary in storage.
   * This method implicitly fetches all assignments and calculates the summary.<br>
   * 
   * @returns {Promise<void>}
   * @fires WanikaniAPIManager#update:assignments:future
   */
  async updateFutureAssignments() {
    const assignments = await this.getAssignments();
    this.storage.setFutureAssignmentsSummary(_WanikaniAPIManager.futureAssignmentsSummary(assignments));
    this.events.emit("update:assignments:future");
  }
  async updateSubjectsForReview() {
    const url = this.baseUrl + "assignments?immediately_available_for_review";
    const lastUpdated = this.storage.getFetchTimestamp(`subjects_for_review`);
    const response = await this._fetchAllPages(url, lastUpdated);
    if (response.error) {
      console.error("[WaniKaniAPI] Error fetching assignments for review:", response.error);
      return { error: response.error };
    }
    await this.storage.saveAssignments(response);
    this.storage.setFetchTimestamp(`subjects_for_review`, /* @__PURE__ */ new Date());
    this.storage.setSubjectsForStudy(response, "review");
  }
  async updateSubjectsForLessons() {
    const url = this.baseUrl + "assignments?immediately_available_for_lessons";
    const lastUpdated = this.storage.getFetchTimestamp(`subjects_for_lessons`);
    const response = await this._fetchAllPages(url, lastUpdated);
    if (response.error) {
      console.error("[WaniKaniAPI] Error fetching assignments for lesson:", response.error);
      return { error: response.error };
    }
    await this.storage.saveAssignments(response);
    this.storage.setFetchTimestamp(`subjects_for_lessons`, /* @__PURE__ */ new Date());
    this.storage.setSubjectsForStudy(response, "lessons");
  }
  /**
   * Get curated information about request entries from the storage.
   * This method builds a structured overview of the requests made to the WaniKani API, including the endpoint, response size, and timestamp.
   * 
   * @param {number} [timeOffset=60] - Optional time offset in minutes to filter requests.
   * @returns {Promise<Object>} An object containing request entries with their details.
   * 
   * @example
   * // Get requests history from the last 120 minutes
   * const requestsHistory = await wkManager.getRequestsHistory(120);
   */
  async getRequestsHistory(timeOffset = 60) {
    const requests = await this.storage.getRequestEntries(timeOffset);
    return this._buildRequestEntriesInfo(requests);
  }
  /**
   * Clears the entire database. Both IndexedDB and local storage.
   * 
   * @example
   * wkManager.clearDatabase();
   */
  async clearDatabase() {
    await this.storage.delete();
  }
  /**
   * Clears only user information and avatar from storage.
   * 
   * @example
   * wkManager.clearUserInfo();
   */
  clearUserInfo() {
    this.storage.deleteUserInfo();
  }
  // ====== STATIC METHODS ======
  /**
   * Filters assignments passed as argument and returns a summary of the next assignments.<br>
   * This method provides a structured overview of those assignments, including the next available date and time for reviews.
   * 
   * @static
   * @param {Array} assignments - Array of assignment objects.
   * @param {Date} [targetDate] - Optional date to filter results up to this date.
   * @return {Object} An object containing future reviews, their schedule, and the next assignments.
   * 
   * @example
   * // get future assignments summary up to a specific date
   * const futureAssignments = wkManager.futureAssignmentsSummary(assignments, new Date('2025-08-17'));
   * 
   * @example
   * // Return example:
   * {
   * 	reviews: [assignment1, assignment2, ...],
   * 	schedule: {
   * 		"Fri Nov 14 2025": {
   * 			"12:00": [assignment1, assignment2],
   * 			"14:00": [assignment3],
   * 			...
   * 		},
   * 		...
   * 	},
   * 	nextReviews: {
   * 		date: "Fri Nov 14 2025",
   * 		hours: "12:00",
   * 		dateTime: "2025-11-14T12:00:00Z",
   * 		reviews: [assignment1, assignment2],
   * 		timestamp: 3600000,
   * 		time: "1 hour"
   * 	}
   * }
   */
  static futureAssignmentsSummary(assignments, targetDate) {
    if (targetDate < /* @__PURE__ */ new Date())
      return {};
    const futureReviews = _WanikaniAPIManager._filterAssignmentsByTime(assignments, /* @__PURE__ */ new Date(), targetDate);
    const futureReviewsSchedule = _WanikaniAPIManager.assignmentsSchedule(futureReviews);
    const closestDate = Object.keys(futureReviewsSchedule).sort((a, b) => new Date(a) - new Date(b))[0];
    const closestHours = Object.keys(futureReviewsSchedule[closestDate] || {}).sort((a, b) => new Date(a) - new Date(b))[0];
    const closestDateTime = /* @__PURE__ */ new Date(`${closestDate} ${closestHours}`);
    const nextAssignments = futureReviewsSchedule && closestDate && closestHours ? futureReviewsSchedule[closestDate][closestHours] || [] : [];
    const timeUntilNext = closestDateTime - /* @__PURE__ */ new Date();
    return {
      reviews: futureReviews,
      schedule: futureReviewsSchedule,
      nextReviews: {
        date: closestDate,
        hours: closestHours,
        dateTime: closestDateTime,
        reviews: nextAssignments,
        timestamp: timeUntilNext || null,
        time: timeUntilNext ? _WanikaniAPIManager._readableDuration(timeUntilNext) : null
      }
    };
  }
  /**
   * Divides assignments into groups of dates based on their available date.
   * 
   * @static
   * @param {Array} assignments - Array of assignment objects.
   * @return {Object} An object where keys are dates and values are arrays of assignments available on that date.
   * 
   * @example
   * // Return example:
   * {
   * 	"Fri Nov 14 2025": {
   * 		"12:00": [assignment1, assignment2],
   * 		"14:00": [assignment3],
   * 		...
   * 	},
   * 	...
   * }
   */
  static assignmentsSchedule(assignments) {
    const calendar = {};
    assignments.forEach((assignment) => {
      const date = new Date(assignment.available_at).toDateString();
      if (!calendar[date]) calendar[date] = {};
      const time = new Date(assignment.available_at).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" });
      if (!calendar[date][time]) calendar[date][time] = [];
      calendar[date][time].push(assignment);
    });
    return calendar;
  }
  // ====== PRIVATE METHODS ======
  /**
   * Auxiliar function to handle more generalized cache, fetch and storage of data from WaniKani API.
   * 
   * @private
   * @param {string} source - Source type (e.g., 'materials', 'assignments', 'reviews').
   * @param {string} type - Type of data to fetch (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @param {string} url - API endpoint URL to fetch data from.
   * @param {Function} save - Function to save the fetched data to storage.
   */
  async _updateContent(source, type, url, save) {
    const storeId = `${source}${type ? "_" + type : ""}`;
    console.log(storeId, url);
    const lastUpdated = this.storage.getFetchTimestamp(storeId);
    const { shouldFetch, timeSinceLastUpdate } = this._cacheIntervalClear(lastUpdated, source);
    if (!shouldFetch)
      return;
    await this._fetch(url, lastUpdated, save);
    this.storage.setFetchTimestamp(storeId, /* @__PURE__ */ new Date());
  }
  /**
   * Auxiliar function to handle cache, fetch and storage of specific content by ID or array of IDs.
   * 
   * @private
   * @param {string} source - Source type (e.g., 'materials', 'assignments', 'reviews').
   * @param {string} endpoint - URL endpoint with filter parameter to pass the IDs (e.g., 'subjects?ids=').
   * @param {number|Array<number>} ids - ID or array of IDs to fetch.
   * @param {Function} save - Function to save the fetched data to storage.
   */
  async _updateContentById(source, endpoint, ids, save) {
    ids = Array.isArray(ids) ? ids : [ids];
    if (!ids.length) return "missing-args";
    return await this._updateContentDynamic(
      source,
      (idsToFetch) => this.baseUrl + endpoint + idsToFetch.join(","),
      () => this.storage.checkCacheForIDs(ids, source),
      () => this.storage.setCacheForIDs(ids, /* @__PURE__ */ new Date(), source),
      save,
      { ids }
    );
  }
  /**
   * Auxiliar function to handle dynamic content updates. This includes situations with many ids or levels, to fetch.
   * 
   * @private
   * @param {string} source - Source type (e.g., 'materials', 'assignments', 'reviews').
   * @param {Function} urlBuilder - Function to build the URL based on items to fetch.
   * @param {Function} cacheChecker - Function to check the cache state and return items to fetch.
   * @param {Function} cacheUpdater - Function to update the cache state after fetching.
   * @param {Function} save - Function to save the fetched data to storage.
   * @param {Object|Array} eventObject - Object or array of IDs for the event emission.
   * @returns {Promise<string|null>} Resolves with an error message if any, or null if successful.
   */
  async _updateContentDynamic(source, urlBuilder, cacheChecker, cacheUpdater, save, eventObject) {
    let error = null;
    let { itemsToFetch, itemsToBypassCacheInterval, lastUpdated } = await cacheChecker();
    const { shouldFetch, timeSinceLastUpdate } = this._cacheIntervalClear(lastUpdated, source);
    if (!shouldFetch) {
      itemsToFetch = [];
      lastUpdated = /* @__PURE__ */ new Date("1970-01-01");
      error = "cache-interval-not-cleared";
    }
    itemsToFetch = [...itemsToFetch, ...itemsToBypassCacheInterval];
    if (!itemsToFetch.length)
      return error || "no-items-to-fetch";
    await this._fetch(urlBuilder(itemsToFetch), lastUpdated, save);
    await cacheUpdater();
    this.events.emit(`update:${source}`, eventObject);
    return error;
  }
  /**
   * Template function to fetch data from the WaniKani API and save it to storage.
   * 
   * @private
   * @param {string} url - API endpoint URL to fetch data from.
   * @param {Date} lastUpdated - Timestamp of the last update to only fetch new data.
   * @param {Function} save - Function to save the fetched data to storage.
   */
  async _fetch(url, lastUpdated, save) {
    const response = await this._fetchAllPages(url, lastUpdated);
    if (response.error) {
      console.error("[WaniKaniAPI] Error fetching data:", response.error);
      return { error: response.error };
    }
    if (save) await save(response);
  }
  /**
   * Auxiliar function to read data from storage and update it if necessary.
   * If `onUpdate` is provided, it will be called first with the cached data,
   * and then with the updated data after the update is complete.
   *
   * @private
   * @param {Function} update - Function to update the data.
   * @param {Function} read - Function to read the data.
   * @param {Function|null} onUpdate - Optional callback function to handle the updated data.
   *                                  If null, it will block until the update is complete and return the data.
   *                                  If a function, it will be called with the state and data.
   * @returns {Promise<any>} The cached data or the updated data.
   */
  async _readFromStorage(update, read, onUpdate) {
    if (onUpdate === null) {
      await update();
      return await read();
    }
    const cached = await read();
    if (typeof onUpdate === "function") onUpdate({ state: "cached", data: cached });
    update().then(async () => {
      const updated = await read();
      if (typeof onUpdate === "function") onUpdate({ state: "updated", data: updated, old: cached });
    }).catch((error) => {
      if (typeof onUpdate === "function") onUpdate({ state: "error", data: error });
    });
    return cached;
  }
  /**
   * Auxiliar function to read subjects from storage.
   * 
   * @private
   * @param {string} type - Subject type to filter (e.g., 'radical', 'kanji', 'vocabulary', 'kana_vocabulary').
   * @returns {Promise<Array>} Array of subjects or error object.
   */
  async _readSubjects(type) {
    const assignments = await this.storage.getAssignments(type);
    const reviews = await this.storage.getReviews(type);
    const material = await this.storage.getMaterials(type);
    return _WanikaniAPIManager._mergeSubjects(material, assignments, reviews);
  }
  /**
   * Auxiliar function to read complete subjects data (materials, assignments, reviews) by passing an array of materials.
   * This method is usefull when you need to get assignments or reviews filtered by properties only present in the materials data.
   * 
   * @private
   * @param {Array} materials - Array of subject materials objects to filter subjects.
   * @returns {Promise<Array>} Array of subjects or error object.
   */
  async _readSubjectsByMaterials(materials) {
    if (!materials || !materials.length) return [];
    const subjectsIds = materials.map((subject) => subject.id);
    const assignments = await this.storage.getAssignmentsBySubjectId(subjectsIds);
    const reviews = await this.storage.getReviewsBySubjectId(subjectsIds);
    return _WanikaniAPIManager._mergeSubjects(materials, assignments, reviews);
  }
  /*
   * Auxiliar function to read subjects by ID from storage.
   *
   * @private
   * @param {number|Array<number>} ids - ID or array of IDs to filter subjects.
   * @returns {Promise<Array>} Array of subjects or error object.
   */
  async _readSubjectsById(ids) {
    const assignments = await this.storage.getAssignmentsBySubjectId(ids);
    const reviews = await this.storage.getReviewsBySubjectId(ids);
    const materials = await this.storage.getMaterialsById(ids);
    return _WanikaniAPIManager._mergeSubjects(materials, assignments, reviews);
  }
  /**
   * Merge subject materials and assignments into a single array.
   *
   * @private
   * @param {Array} subjects - Array of subject materials objects.
   * @param {Array} assignments - Array of assignment objects.
   * @returns {Array} Merged array of subjects with their assignments.
   */
  static _mergeSubjects(subjects, assignments, reviews) {
    const assignmentMap = new Map(
      assignments.map((assignment) => [assignment.subject_id, assignment])
    );
    const reviewsMap = new Map(
      reviews.map((review) => [review.subject_id, review])
    );
    return subjects.map((subject) => {
      const assignment = assignmentMap.get(subject.id) || {};
      const reviews2 = reviewsMap.get(subject.id) || {};
      return { ...subject, assignment, reviews: reviews2 };
    });
  }
  /**
   * Validate the subject type.
   * 
   * @private
   * @param {string} type - Subject type to validate.
   * @returns {boolean} True if the type is valid, false otherwise.
   */
  static _validType(type) {
    if (!type) return true;
    if (!["radical", "kanji", "vocabulary", "kana_vocabulary"].includes(type)) {
      console.error("[WaniKaniAPI] Invalid subject type:", type);
      return false;
    }
    return true;
  }
  /**
   * Filter assignments based on the current date and an optional cap date.
   * If capDate is in the future, it filters assignments that are available from currentDate to capDate.
   * If capDate is in the past, it filters assignments that were available before currentDate.
   * If capDate is null, it returns all assignments with dates greater than today.
   *
   * @private
   * @param {Array} list - List of assignment objects to filter.
   * @param {Date} currentDate - The current date to filter assignments against.
   * @param {string|null} [capDate] - Optional cap date to filter assignments up to or from.
   * @returns {Array} Filtered list of assignments based on the specified criteria.
   */
  static _filterAssignmentsByTime(list, currentDate, capDate) {
    const date = capDate ? new Date(capDate) : null;
    if (date) {
      if (date.getTime() > (/* @__PURE__ */ new Date()).getTime()) {
        return list.filter((assignment) => new Date(assignment["available_at"]).getTime() >= currentDate.getTime() && new Date(assignment["available_at"]).getTime() <= date.getTime());
      } else {
        return list.filter((assignment) => new Date(assignment["available_at"]).getTime() <= currentDate.getTime() && new Date(assignment["available_at"]).getTime() >= date.getTime());
      }
    }
    return list.filter((assignment) => new Date(assignment["available_at"]).getTime() > currentDate.getTime());
  }
  /**
   * Convert a timestamp to a human-readable duration string.
   *
   * @private
   * @param {number} timestamp - The timestamp in milliseconds to convert.
   * @returns {string} A string representing the duration in days, hours, minutes, and seconds.
   */
  static _readableDuration(timestamp) {
    const days = Math.floor(timestamp / (1e3 * 60 * 60 * 24));
    const hours = Math.floor(timestamp % (1e3 * 60 * 60 * 24) / (1e3 * 60 * 60));
    const minutes = Math.floor(timestamp % (1e3 * 60 * 60) / (1e3 * 60));
    const seconds = Math.floor(timestamp % (1e3 * 60) / 1e3);
    let duration = "";
    if (days > 0) duration += `${days}d `;
    if (hours > 0) duration += `${hours}h `;
    if (minutes > 0) duration += `${minutes}m `;
    if (seconds > 0) duration += `${seconds}s`;
    return duration.trim();
  }
  /**
   * Check if given timestamp is older than the cache interval for a specific type.
   * 
   * @private
   * @param {string} cachedTimestamp - The timestamp of the last cache update.
   * @param {string} type - The type of data to check (e.g., 'subjects', 'assignments', 'reviews').
   * @returns {boolean} True if the cache should be cleared, false otherwise.
   * @example
   * const shouldFetch = this._cacheIntervalClear(lastUpdated, 'assignments');
   * if (shouldFetch)
   *   await this.updateAssignments();
   */
  _cacheIntervalClear(cachedTimestamp, type) {
    const fetchInterval = this.cacheIntervals[type];
    const timeSinceLastUpdate = cachedTimestamp ? /* @__PURE__ */ new Date() - new Date(cachedTimestamp) : null;
    const shouldFetch = !cachedTimestamp || timeSinceLastUpdate > fetchInterval;
    const timeUntilNextFetch = shouldFetch ? 0 : fetchInterval - timeSinceLastUpdate;
    console.table([{
      "type": type,
      "fetch": shouldFetch,
      "last update": _WanikaniAPIManager._readableDuration(timeSinceLastUpdate),
      "next fetch": !timeUntilNextFetch ? "now" : _WanikaniAPIManager._readableDuration(timeUntilNextFetch),
      "interval": _WanikaniAPIManager._readableDuration(fetchInterval)
    }]);
    return { shouldFetch, timeSinceLastUpdate, timeUntilNextFetch };
  }
  async _fetchPage(url, updated, filter = true) {
    if (!this.apiKey) throw new Error("API key is missing");
    return await fetchPage(this.apiKey, url, updated, filter, this._saveRequest.bind(this));
  }
  async _fetchAllPages(url, updated, filter = true) {
    if (!this.apiKey) throw new Error("API key is missing");
    return await fetchAllPages(this.apiKey, url, updated, filter, this._saveRequest.bind(this));
  }
  /**
   * Save a request entry to the storage.
   * 
   * @private
   * @param {number} status - HTTP status code of the response.
   * @param {string} endpoint - The API endpoint that was requested.
   * @param {number} responseSize - The size of the response in bytes.
   * @fires WanikaniAPIManager#update:requests
   * @returns {void}
   */
  _saveRequest(status, endpoint, data) {
    if (status == 429) {
      this.events.emit("error:rate-limit", { endpoint, data });
      return;
    }
    if (!endpoint || isNaN(data)) {
      console.error("[WaniKaniAPI] Invalid arguments for _saveRequest:", endpoint, data);
      return;
    }
    this.storage.setRequestEntry(endpoint, data).then(async (entry) => {
      const requests = await this.storage.getRequestEntries();
      this.events.emit("update:requests", {
        request: entry,
        info: this._buildRequestEntriesInfo(requests)
      });
    });
  }
  /**
   * Build an object containing information about the request entries.
   * @param {Array} entries - Array of request entries.
   * @returns {Object} An object containing the requests, and some other statistics about the requests.
   * @private
   */
  _buildRequestEntriesInfo(entries) {
    const totalResponseSize = entries.reduce((sum, entry) => sum + entry.responseSize, 0);
    const totalRequests = entries.length;
    const averageResponseSize = totalRequests > 0 ? totalResponseSize / totalRequests : 0;
    return {
      requests: entries,
      totalResponseSize,
      totalRequests,
      averageResponseSize,
      rateLimit: this.RATE_LIMIT,
      nextReset: new Date((/* @__PURE__ */ new Date()).getTime() + 60 * 1e3),
      // reset every minute
      remainingRequests: this.RATE_LIMIT - totalRequests
    };
  }
};
export {
  WanikaniAPIManager as default
};
/*! Bundled license information:

wanikani-api-manager/dist/wanikani-api-manager.es.js:
  (**
   * @file src/api.js
   * api.js
   * 
   * Provides functions to directly interact with the WaniKani API.
   * 
   * @author Diogo Correia <digas_correia@hotmail.com>
   * @license MIT
   *)
  (**
   * @file src/storage.js
   * storage.js
   * 
   * Provides a storage manager for the WaniKani API Manager.
   * 
   * @requires Dexie
   * 
   * @author Diogo Correia <digas_correia@hotmail.com>
   * @license MIT
   *)
  (**
   * @file src/client.js
   * client.js
   *
   * Provides a high-level interface to interact with the WaniKani API.
   * Offers strategically designed methods to fetch bundled data
   * 
   * @requires src/api.js
   * @requires src/storage.js
   * 
   * @author Diogo Correia <digas_correia@hotmail.com>
   * @license MIT
   *)
*/
//# sourceMappingURL=wanikani-api-manager.js.map
